# LIAVO — Roadmap Été 2026

> **Rédigé le 18/06/2026** — Issue d'un audit exhaustif code × docs.
> **🚀 MISE À JOUR 04/09/2026 — RUN #40 « DEVIS SUR SÉJOUR REJOINT + NOTIF AUTO ORGANISATEUR » LIVRÉ EN PROD** (merge `5c48ce7`, 5 commits `4f8e7fd`→`026d754`, 0 migration, 2 déploiements Scalingo verts ; détail complet SESSION_STATE 04/09). **PROCHAINE PRIORITÉ PRODUIT ACTÉE (Théo) — UNIFICATION DE L'UI HÉBERGEUR « Devis & Facturation »** : exigence TEXTO « exactement la même expérience utilisateur pour l'hébergeur quel que soit le format du séjour » (l'hébergeur ne connaît PAS les statuts direct/collab/rejoint) → une seule interface **À LA SOURCE**, modifiable EN UNE FOIS ; fin de la duplication `{isDirect &&}` / `{!isDirect &&}` (~800 lignes chacun) dans `TabDevisFacturation.tsx`. Inclut : bouton « Envoyer/Renvoyer le devis » ABSENT sur le rejoint + **lever la garde back `envoyerDevisDirect` (`modeGestion!=='DIRECT'`)**. Backlog connexe : viewer PDF `80vh` dupliqué (enterre la signature), message succès qui disparaît en 1.5s (`setTimeout`), badge « Brouillon » trompeur sur devis transmis. Note FACTUELLE : le « cockpit riche » est gaté sur le STATUT du devis, PAS le mode (EN_ATTENTE = vue réduite quel que soit le mode ; une fois signé le rejoint = même cockpit qu'un collab pur). Prompt de reprise donné dans une conversation dédiée le 04/09 ; **census read-only + diagnostic commun/divergent/cause AVANT toute spec**. **AUTRE BUG À TRAITER (chantier séparé) — invitation collaborative** : `getPendantesPourUser` filtre `date_fin >= aujourd'hui` → (a) invitation EXPIRÉE quand le séjour change de dates (bannière « Rejoindre » absente), (b) impossible de rejoindre sur un séjour en cours/passé (régularisation), (c) pas de gestion d'invitation côté hébergeur (« Renvoyer » ne corrige pas la date).
> **🧩 SUJET OUVERT (consigné 26/08/2026, non tranché, NE PAS coder à chaud) — FACTURATION MULTI-ORGANISATION (≠ multi-centre intra-org).** Constat FACTUEL (lu 26/08 sur code réel) : le montant se calcule PAR ORGANISATION (`calculerMontantAbonnementCents(plan, freq, nbCentresACTIVE de l'org)`), 1 customer Mollie = 1 org, 1 subscription = 1 org → un compte rattaché à N organisations obtient N subscriptions, chacune facturant le plan de base, et le supplément +39/centre ne mutualise QU'À L'INTÉRIEUR d'une org. **Ce n'est PAS un bug** (le code applique correctement la règle de prix actuelle) mais une **DÉCISION PRODUIT de pricing NON TRANCHÉE** pour le cas « 1 client, plusieurs entités juridiques ». **INCERTAIN / À VÉRIFIER avant tout dev** : (a) la logique EXACTE de regroupement centre→org — rattachement par SIREN et/ou membership VALIDE lu PARTIELLEMENT dans `centre.service`/`claim.service`, PAS confirmé exhaustivement ; (b) le PARCOURS DE SOUSCRIPTION FRONTEND jamais audité (déjà noté roadmap) ; (c) pour Tereva (14 centres) : une seule société (1 SIREN → 1 org → supplément s'applique, possible NON-SUJET) OU plusieurs entités juridiques (→ N orgs → N plans de base) — **RÉPONSE INCONNUE**, à obtenir côté commercial (Pierre). ORDRE : d'abord réponse (c), puis décision de prix (plan par entité = comportement actuel / tarif de groupe / remise volume cross-org ?), puis census front. **NON URGENT** : aucun compte multi-org payant aujourd'hui, le cas ne mord sur personne en prod.
> **🎯 PROCHAINS CHANTIERS (ordre acté 03/09/2026, après livraison #38)** — (1) **CHANTIER INVITATION, les deux faces du même flux — ✅ LIVRÉ EN PROD ET RECETTÉ 03/09 (#39, merge `8a0f7a8`, cf. entrée datée ci-dessous + SESSION_STATE 03/09 (3)).** Reliquats au backlog #39 (non-bloquants, à froid) : gate (a) « rattaché à soi-même » à affiner ; date « depuis le… » de l'état rattaché (ajout backend séparé) ; séjour test « test tt » à supprimer. (2) **Magic link « nouveau devis » → atterrir sur le séjour** (`consommerMagicLink` redirige tout magic link vers le dashboard `?onboarding=true` ; petit techniquement — param `next` dans le callback — mais touche l'auth : point à part avec sa propre recette, après (1)). (3) **Dettes de test sur `main`** : jest ne charge pas `backend/.env` → setup dotenv ; `mollie.client.ts` instancie Mollie à l'import (init paresseuse) ; mock `facture.service.spec.ts` à compléter (`centreHebergement.findUnique`) — baseline actuelle 4 rouges, `main` poussé avec des tests rouges depuis un moment. (4) **Refonte des statuts** (`docs/ANALYSE_STATUTS_SEJOUR_DEVIS_2026-09-02.md` §5.3 : S1 catalogue→CONVENTION sans devis, S3 `updateStatus` libre pour SIGNATAIRE/AUTORITE, S4 `soumettreAuRectorat` inatteignable, S5 DECLARE_TAM perd l'accès, S6 gardes CONVENTION strict, doublon DRAFT/SUBMITTED↔`appelOffreStatut`, FACTURE_* legacy, champs `*Directeur`, T4) — POST-fenêtre commerciale de septembre, migration lourde. (5) **Backlog #38 v1** : badge non-lus sur Échanges (endpoint organisateur à créer) ; état vide « invitation en attente » (info à remonter du layout) ; critère CTA démotés par `hebergementSelectionneId` ; wording PDF « signé par la direction » ; `sendSejourApprouve` → lien `/dashboard/teacher` mort. (6) **Miroir hébergeur « où en est mon enseignant »** (progression des blocs vue côté hébergeur) — après (1). (7) **Refonte de la saisie client à la création de séjour** (remontées terrain Anne/Maeva 03/09 : recherche pro peu intuitive, contact et établissement confondus dans l'étape Client de `CreateSejourModal`) — cadrage `docs/CHANTIER_SAISIE_CLIENT_CREATION.md`, NON figé, à instruire à froid APRÈS #39 : séparer établissement (ce qu'on facture) et contact (personne), un seul niveau de type, la recherche devient l'établissement sélectionné ; Particulier déjà bon ; CP recherche obligatoire vs optionnel NON tranché. Inclut deux dettes à traiter au passage : le **bug backend affichage siège vs établissement** (`organisations.service.ts` `mapResults` ignore `matching_etablissements` → cherche 11, affiche Épinal 88 ; confirmé doc API + issue #171) et la **consolidation des 3 composants de recherche** divergents (`RechercheOrganisation`/`OrganisationSearch`/`StructureSearch`).
> **Dernière mise à jour : 04/09/2026 — 🏷️ DETTE DE NOMMAGE ACTÉE : `Sejour.createurId` est un FAUX AMI (= « organisateur rattaché, ou null »), à renommer avant onboarding dev. RIEN CODÉ.** Constat FACTUEL lu ce jour sur code réel (`sejour.service.ts` `createDirect`/`create`/`creerDepuisCatalogue` ; `invitation-collaboration.service.ts` `accepter`) : `createurId` ne désigne PAS « qui a créé le séjour » mais « le compte ORGANISATEUR rattaché, ou `null` s'il n'y en a pas ». DIRECT pur (créé par l'hébergeur) → `createurId = null` — l'hébergeur n'est JAMAIS créateur en base ; collab pur → l'enseignant ; rejoint → `accepter()` ÉCRASE le `null` vers l'organisateur (`data: { createurId: user.id, modeGestion: 'COLLABORATIF' }`). INVARIANT fiable : l'hébergeur = toujours `hebergementSelectionneId → centre.userId` ; `createurId != null` ⇔ « un organisateur est rattaché » (seule question dont dépend le routage d'envoi du devis). Le nom induit en erreur (confusion Théo confirmée en session), piège garanti pour un dev recruté à la commercialisation. DÉCISION : renommer la sémantique en `organisateurId` (nullable) + commenter — chantier DÉDIÉ post-fenêtre commerciale (migration Prisma rename colonne + toutes réf. services/front/types ; même classe de migration lourde que la refonte des statuts (4) de 🎯 PROCHAINS CHANTIERS). EN ATTENDANT, dans le chantier d'unification de l'onglet « Devis & Facturation » en cours : aliaser en local (`const organisateurId = sejour.createurId` / `const aUnOrganisateur = organisateurId != null`) SANS renommer la colonne, pour que le nouveau code se lise juste. Détail : SESSION_STATE 04/09.
> **Dernière mise à jour : 03/09/2026 — CHANTIER #38 « DASHBOARD ORGANISATEUR + ESPACE ENSEIGNANT » LIVRÉ EN PROD (merge `005fb7f`, 38 commits, 0 migration, 2 déploiements verts, recette prod hébergeur + parcours invité bout-en-bout).** Parti du constat « LIAVO a deux enseignants — celui qui vient seul (appel d'offres / catalogue) et celui qui est INVITÉ par son hébergeur — et l'interface n'était conçue que pour le premier ». Lecture du code : l'invité atterrissait sur un dashboard qui lui mentait (« création du séjour », « votre demande a été envoyée », 3 CTA hors sujet), dans un espace à 10 onglets sans guidage après la signature, et **sa tâche principale (inscrire ses élèves, envoyer aux parents, accompagnateurs, prix par élève) vivait sur une page satellite introuvable** ; en prime, le prix par élève n'était JAMAIS enregistrable sur un séjour signé (garde `update()` DRAFT-only, bug bloquant confirmé). **Décisions** : dashboard unique adaptatif ; l'invité atterrit sur son séjour ; pages satellites absorbées puis supprimées ; espace enseignant = 6 blocs en 2 phases (« Je monte mon dossier » Réservation/Pédagogie/Budget, « J'organise le séjour » Inscriptions/Sur place, + Échanges) avec états fait/en cours/à faire, une seule action en évidence, « Tout est prêt ✓ » ; deux phases séparées par la signature mais JAMAIS verrouillées (confirmation orale = on avance), signature = badge permanent, pas une porte ; vocabulaire affiché réduit à l'axe d'engagement (en attente de signature / signé / annulé) ; inscriptions familles ET saisie manuelle disponibles en complément (autorisations papier), ajout d'élève sans mail + envoi groupé choisi ; clôture des inscriptions remontée dans le bloc Inscriptions (était cachée dans Sur place › Chambres) ; Budget « fait » = bouclé (solde ≥ 0) ; lien du journal envoyable aux familles (jeton par famille, JAMAIS de lien unique par séjour — photos de mineurs). Zéro changement de rendu hébergeur / signataire / accompagnateur (vérifié à l'écran). **Méthode** : run CC autonome sur branche isolée piloté par doc-prompt (1h15, SC0→SC7) + 2 passes d'ajustement après recette visuelle ; qualité jugée sur rapport + git, jamais sur récap. Cadrage `docs/CADRAGE_DASHBOARD_ORGANISATEUR_2026-09-02.md`, analyse `docs/ANALYSE_STATUTS_SEJOUR_DEVIS_2026-09-02.md`. Détail : SESSION_STATE 03/09. Prochains chantiers : bloc 🎯 ci-dessus.
> **Dernière mise à jour : 02/09/2026 (2) — PENSE-BÊTE « DÉTAILS SÉJOUR » SUR LES PAGES DEVIS (création + modification) : LIVRÉ + POUSSÉ (7 commits, `origin/main` = `ea96f2f`, 0 migration).** Tient l'engagement pris à Anne (Choucas) et Yves (Pôle Montagne) : les 8 champs de qualification saisis au wizard hébergeur / appel d'offres (`niveauClasse`, heures, transport aller/sur place, activités souhaitées, budget max/participant, `noteDateFlexible`) sont désormais affichés — et éditables en DIRECT — dans un panneau latéral escamotable sur `/hebergeur/devis/nouveau` et `/hebergeur/devis/[id]/modifier`, là où l'hébergeur fabrique le devis. Ferme le backlog « pense-bête détails séjour dans DevisEditor » du lot 01/09. **Census (2 gros services lus en entier via MCP)** : `getSejourInfo` = include large → les champs descendent déjà (0 backend pour `/nouveau` DIRECT) ; `getDevisById`/`getDemandeInfo` = selects restrictifs à étendre ; `updateInfosSejour` additif hors cascade dates/client. **Décisions** : champ libre = `noteDateFlexible` seul ; édition DIRECT / lecture seule COLLAB (l'enseignant est maître de sa demande) ; budget strictement facultatif (vide→null, 0 reste une valeur) ; panneau ouvert par défaut. **Composant neuf** `DetailsSejourPanel.tsx` + constante partagée `TRANSPORT_ALLER_OPTIONS` (`DEJA_/BESOIN_TRANSPORTEUR` humanisés). 2 fix post-livraison : budget effaçable (`7d1a3ac`, retrait `@Type(() => Number)` du DTO pour ne pas coercer null en 0) ; transportAller humanisé + panneau ouvert (`ea96f2f`). Reste : recette de l'effacement budget (vider → 200 → F5 → ligne disparaît) non faite proprement ; `transportSurPlace` null→false après 1ère édition (cosmétique). Détail complet : SESSION_STATE 02/09 (2).
> **Dernière mise à jour : 02/09/2026 — AVOIR PARTIEL PAR QUANTITÉ + RÉ-ÉMISSION APRÈS ANNULATION TOTALE : LIVRÉ EN PROD (5 commits `88305fb`→`82a21bd`, 0 migration).** Déclencheur = incident terrain Sauvageon (Maeva) : facture de solde à 63 pensions au lieu de 60, avoir plein émis, puis blocage pour ré-émettre — résolu à chaud en SQL prod (suppression des 2 factures + rembobinage des séquences + ré-émission à 12 144 €). L'incident a révélé 2 trous produit : la modale d'avoir ne faisait que du tout-ou-rien par ligne (pas de correction d'effectif), et aucune ré-émission n'était possible après annulation (impasse exigeant du SQL — intenable sur un client payant). Fix global : **B1** — quantité éditable par ligne dans l'avoir (prorata sur totaux d'origine, PDF « Avoir partiel » vs « Annule et remplace », reste dû recalculé) ; **B2** — notion de « facture active » (non intégralement annulée par avoir) appliquée aux gardes d'émission, au routage des versements et à tout l'affichage, ré-émission possible avec nouveau numéro + section « Documents annulés ». Backend `emettreAvoir` acceptait déjà les quantités partielles (0 modif DTO). 1-1 conservé, formule reste dû « ce qui est facturé ». Réserves hors scope : avoir plein sur solde-avec-acompte rejeté par le garde `montant ≤ montantFacture` (cas « facturer le total » non concerné) ; devis legacy `FACTURE_*` hors garde de ré-émission (aucun concerné). Reste : recette prod des scénarios B2 + `chore(test)` mock `validerAcompte` (4 rouges pré-existants). Détail : SESSION_STATE 02/09.
> **Dernière mise à jour : 01/09/2026 — LOT « INVITATION + SIGNATURE + CRÉATION DE SÉJOUR EN 3 ÉTAPES » LIVRÉ, RECETTÉ LOCAL, POUSSÉ (18 commits `286a9be`→`0823ae0`). Déploiement Scalingo à vérifier + recette prod à faire. Tranche #41 et #40, rend #2 caduc. Doc de cadrage = `docs/CADRAGE_INVITATION_SIGNATURE_2026-09-01.md`.** Parti de « le dashboard organisateur n'est pas intuitif », la lecture du code a révélé que **« Rejoindre » valait acte contractuel** : `accepter()` posait `CONVENTION` et auto-sélectionnait le devis SANS signature (la dette 19/08 (3)/(4)). **Décisions produit figées** : un seul chemin d'invitation (depuis un séjour existant — la page « Inviter » sans séjour est SUPPRIMÉE, pas fusionnée) ; **rejoindre ≠ signer** (rejoindre = accès collaboratif, séjour reste `OPTION`, devis `EN_ATTENTE` ; `CONVENTION` uniquement à la signature) ; les 3 gestes de signature (signer en ligne / envoyer à la direction / uploader) disponibles **depuis l'espace connecté** via des endpoints JWT (ownership R4 + identité tracée « compte vérifié ») en plus du lien email tokenisé — **deux niveaux de garantie, une seule UI** (`SignatureDevisPanel` partagé) ; CGV = l'acceptation actuelle suffit, la CONVENTION post-signature est le document formel ; création de séjour hébergeur en **wizard Client → Séjour → Détails (optionnel)** avec recherche gatée en tête, niveau/classe/âge en texte libre, 7 champs de qualification (thématiques laissées à l'enseignant), libellé « / participant ». **Bugs préexistants corrigés au passage** : l'onglet « Devis » de l'espace affichait « Aucun devis sélectionné » à l'organisateur (fetch hébergeur-only, 403 avalé) ; la carte du dashboard n'avait aucune action pour un séjour `OPTION`. **Non touché (T4)** : `updateStatut` = sélection COLLAB appel d'offres sans signature (le directeur signe ensuite) — cohérent, à border après. **Recette locale complète** (invitation → rejoindre → signer depuis l'espace, lien public, délégation direction, wizard 4 cas). **Méthode** : 2 cascades attrapées HORS census (`getBudgetData` seule source de l'onglet Devis organisateur ; carte OPTION) → règles gravées : lister TOUS les endpoints lus par le front du chemin + censer les conditions d'affichage par statut ; un push `main` redéploie les 2 apps (vérifié). **CHANTIER SUIVANT = #38 big picture dashboard organisateur** (matrice parcours × stade × écran). Backlog né : T3 #36, T4, wording PDF « par la direction », « Option » vs « À confirmer », pense-bête détails séjour dans `DevisEditor`, CTA compte après signature publique, specs `accepter()`/signatures. Détail : SESSION_STATE 01/09.
> **Dernière mise à jour : 31/08/2026 — IDENTITÉ CLIENT SUR LES DOCUMENTS : établissement éditable + bascule CONTACT + verrou email d'invitation. LIVRÉ/DÉPLOYÉ/RECETTÉ.** Le chantier « propriété client » avait basculé l'ÉTABLISSEMENT (`clientOrganisation`) vers `sejour.client*` sur les documents, mais le CONTACT-personne (nom/email « à l'attention de ») était resté sur le COMPTE organisateur (angle mort révélé en recette du Lot 3 : bug « Tété » — établissement OK, contact = « Maëva » = compte). Trois volets : (1) Lot 3 `clientOrganisation` éditable hébergeur DIRECT+COLLAB (`8f21ed8`+`0b3bfe0`+`8e000c9`, recetté Nogent) ; (2) BASCULE CONTACT — règle UNIQUE `sejour.client* ?? compte` étendue au contact sur les 3 résolveurs (facture/convention/devis PDF Bloc B) + split édition SejourHeader (établissement tous modes, contact DIRECT/ex-direct seulement, collab-pur = organisateur maître ; critère « présence de client* » auto-discriminant, 0 migration) (`52ca104`+`5998c8a`+`3fdedc2`, recetté « Tété » sort sur devis+convention) ; (3) VERROU EMAIL d'invitation — `accepter()` refuse si compte ≠ email d'invitation + register verrouille l'email + filet `consommerInvitationCollabPending` (ForbiddenException terminal, anti-rejeu) (`4b07e2f`+`717e651`). Aucun résolveur d'établissement touché, 0 migration. TODO : test refus mismatch `accepter()` ; commit 2b (pré-remplir email invitation depuis séjour direct) différé ; contrôle négatif collab-pur en recette ; `docs/CHANTIER_ETAPE4_CLIENT_HEBERGEUR.md` untracked à archiver (cadrage dépassé). Piste future : Étape 5 « payeur à la facture » (2ᵉ contact mairie/Trésor Public — amorce via devis complémentaire, NE PAS créer de champ ad hoc). ⚠️ Incident méthode : 2 CC en parallèle sur le même working tree = piles git divergentes (fichiers disjoints, pas de corruption, mais à éviter). Détail : SESSION_STATE 31/08.
> **Dernière mise à jour : 27/08/2026 (2) — FIX « FAUX ESSAI » CLIENT VIREMENT (prédicat canonique estEnEssai) LIVRÉ + DÉPLOYÉ + RECETTÉ PROD.** Choucas (payant Pilotage mensuel virement, actif jusqu'au 31/12) était affiché « Essai gratuit — 126 jours restants » sur son dashboard ET sa page Abonnement, « (trial) » en santé clients admin, et pouvait demander une extension d'essai. SELECT prod : facturation OK (ACTIF/VIREMENT/mandat null), c'était un FAUX BADGE d'affichage, pas un trou argent — aucun UPDATE. Cause : le prédicat « en essai » était dupliqué à la main en 2 versions (certains sites excluent VIREMENT, d'autres non) + `trialStartedAt` jamais effacé à la conversion virement. Fix À LA SOURCE (`b5c7789`, backend-only, 0 migration, 0 type durci) : helper pur `estEnEssai(org) = trialStartedAt≠null && mollieMandatId=null && modePaiement≠VIREMENT` dans `trial.helper.ts`, branché sur les 3 sites fautifs (`getStatut`, `santeClients`, garde `demanderExtension`), avec `modePaiement:true` ajouté aux 3 selects (sinon le fix est mort). Non touchés : `activerTrial` (anti-rejeu correct), cron et métriques (déjà VIREMENT-safe), colonne « Trial » admin (historique des dates conservé, décision Théo). +4 tests `estEnEssai`. Recette prod verte : bandeau « Essai gratuit » disparu du dashboard ET de la page Abonnement (remplacé par « Plan actif · Pilotage », grille en mode déjà-abonné), dégradation propre. Un client virement n'est plus jamais vu en essai. Note méthode : le census CC portait un faux verdict (« getMetriquesAbonnements sur-compte les virements » — FAUX, exclusion `AND:[horsVirement]` déjà présente), corrigé avant go. Détail : SESSION_STATE 27/08 (2).
> **Dernière mise à jour : 27/08/2026 — TARIFICATION MULTI-CENTRE : SUPPLÉMENT PAR PLAN + BLOCAGE Découverte/Essentiel (LIVRÉS, DÉPLOYÉS).** Le supplément par centre supplémentaire n'est plus un plat 39€/390€ mais dépend du plan : Complet +29€/mois (+290/an), Pilotage +49€/mois (+490/an) ; Découverte et Essentiel = **un seul centre** (multi-centre réservé au plan effectif ≥ Complet, limite par organisation, l'essai Pilotage autorise). Backend : grille par plan (`249ec30`/`06c93a7`, déjà en prod) + point de calcul unique `montantRecurrentOrganisationCents` avec seam « prix négocié ». Enforcement (`origin/main` = `1749727`, Scalingo back+front verts) : porte 1 `a4e63c5` (`createCentre` + `claimFromCatalogue`, avec exclusion `id≠existingCentreId` pour ne pas s'auto-bloquer sur une re-revendication), porte 2 `6e1d264` (`souscrire`/`facturerCentre` refusent Essentiel ≥2 centres), front `37aeebe` (cadenas + bandeau + guard), et fix d'ordre `1749727` (un downgrade Essentiel refusé ne casse plus la subscription Mollie — count + porte 2 remontés avant le cancel). Tous relus diff sur code réel par Claude. **Réconciliation** : la croyance « MONETISATION_PLAN dit 39/59/79 » était fausse (le fichier dit 29/49/69 depuis le 25/06 ; le 39/59/79 est barré en historique) — ce qui était périmé = le supplément +39 plat, corrigé ce jour dans le fichier. Reste (affichage) : PricingTable + CGV + commentaires backend, prompt CC lancé. Impact prod nul (aucun multi-centre payant). Détail : SESSION_STATE 27/08.
> **Dernière mise à jour : 26/08/2026 (2) — VOLET MOLLIE MULTI-CENTRE FERMÉ EN PROD (2 fixes argent) + MAJ PULSE.** Audit de bout en bout de la facturation d'abonnement (souscription + webhook + activation de centre + resync) lu sur code réel. DEUX trous distincts fermés. **Trou B — sous-facturation multi-centre (`ed1cc96`)** : `resyncMontantOrganisation` n'était appelé QUE par le webhook de paiement → un centre activé pour un hébergeur déjà payant ne réalignait le montant de la subscription qu'au prochain prélèvement (1 cycle de retard ; jusqu'à ~1 an sur un annuel). Fix = appel `resyncMontantOrganisation` fire-and-forget (no-op sans subscription) aux 3 points d'activation : `admin.activerCentre` (LE cas — hébergeur validé qui ajoute un centre), `claim.validerClaim` + `admin.validerHebergeur` (défensifs, no-op car 1ère validation = pas encore de subscription). CHEMIN VIREMENT déjà correct (`facturerCentre`/`facturerCentrePeriode` recalculent à chaque facture). **Trou A — double prélèvement (`d4f54c7`)** : dans `souscrire`, le `cancel` de l'ancienne subscription avant re-création était en try/catch avalant → si cancel échoue + create réussit = 2 subscriptions actives. Fix = annulation BLOQUANTE : si l'échec ne prouve pas que l'ancienne est déjà inactive (404 `MollieApiError` en `instanceof`, ou `get` de vérité renvoyant `status === canceled` / 404), on `throw ServiceUnavailableException` AVANT tout write DB / création → jamais de 2e subscription sur un état incertain. `annuler` (cancel seul) intact. Les 2 commits relus diff-par-diff sur fichier réel par Claude, `tsc`/`build` verts, poussés (`origin/main` = `d4f54c7`), **2 déploiements Scalingo verts**. **DÉCISION PRODUIT ACTÉE — prorata annuel = STATU QUO (option A)** : le supplément d'un centre ajouté en cours d'année s'applique à la PROCHAINE ÉCHÉANCE, pas de prorata immédiat (le levier admin `facturerCentre` reste dispo pour ré-émettre au cas par cas). Pas « 11 mois offerts » : l'abonnement est déjà payé, seul le +39/+390 € du centre supp attend l'échéance. **Miroir désactivation = confirmé inutile** (aucun chemin ne sort un centre ACTIVE du facturable). **PULSE (MAJ terrain 26/08)** : Louise a créé le 2e centre — PULSE SPORTS CAMPUS CHAMBERET existe (plus de SQL à faire) ; les 2 Pulse (Chamberet + Valloire) sont en trial Pilotage EXPIRÉ depuis le 08/08/2026, 0 séjour / 0 devis → #77 (contre-test multi-centre) débloqué sur l'existence des 2 centres mais PAS encore sur un cas payant (les fixes B/A ne touchent que les comptes avec subscription Mollie ; un trial = no-op). **RESTE (non bloquant)** : recette manuelle du chemin nominal de A (re-souscription changement plan/fréquence sur compte test) — chemin succès inchangé, à faire à froid ; pas de test auto du chemin « cancel échoue » (à ajouter en mockant `get`). Détail : SESSION_STATE 26/08 (2).
> **Dernière mise à jour : 26/08/2026 — CHANTIER GATING UI COLLABORATEUR (PAGE SÉJOUR) CLOS DE BOUT EN BOUT : 8 LOTS + 2 FINITIONS.** Ferme le « 🔴 POLISH UI (non bloquant) » laissé ouvert par 19/08 (5) — masquer les commandes d'écriture visibles au collaborateur READ sur la page séjour. Règle actée (PRODUIT, pas config d'un client) : gater les 18 combinaisons (6 modules planning/sejours/devis/crm/facturation/parametres × NONE/READ/WRITE) ; le BACKEND reste l'autorité (verifyAccess / @RequirePermission / assertFacturationWrite — audit 25/08), l'UI = confort (masquage, jamais sécurité ; un clic résiduel = 403 propre). **Socle** : B-1 `37c27db` (backend expose `mesPermissions` dans `getSejourInfo`, additif) → B-2 `e0efc8a` (front bascule tout le gating sur `mesPermissions` et **RETIRE les 5 booléens `monAccesEcriture/monAccesCrm/monAccesGestionPropre/monAccesDevis/monAccesFacturation`**) → B-3 `d833330` (backend retire les `monAcces*` morts). ⚠️ **INVARIANT MIS À JOUR : les entrées 19/08 (5) et 24/08 (5) qui citent `monAccesEcriture` sont PÉRIMÉES — le drapeau canonique est `sejour.mesPermissions.<module> === 'WRITE'`.** ⚠️ ORDRE DÉPLOIEMENT (respecté) : B-1 backend AVANT B-2 front (sinon `mesPermissions` undefined → owner en lecture seule). **Gating** : E `350668d` (bandeau écran équipe) ; V1 `58eba6c` (onglet Devis masqué si devis:NONE, Notes si crm:NONE + `activeTab` dérivé, plus de flash) ; G3 `d29aad1` (actions devis/facturation par MODULE via `peutEcrireDevis`/`peutEcrireFacturation`/`peutVoirFacturation` ; 2 croisements gérés — « Annuler ce devis »=devis logé dans le pipeline, « Facturer le total » complémentaire=facturation) ; G1 `f016dde` (éditabilité planning `peutEcrire = isHebergeur && sejours:WRITE`, READ = vue organisateur, PDF préservé) ; G2 `894198f` (gestion groupes `peutGererGroupes` + attribution chambres `peutEcrire`, **distincts de `peutGererEnPropre`** qui reste l'axe DIRECT/affectation). **Finitions** : `11316c6` (planning lecture seule n'ouvre plus la modale d'édition) + `c2e955b` (libellé bandeau équipe invariable, fin du bug « les devis se gère »). Tous relus diff-par-diff sur fichier réel avant push, `tsc`/`build` verts, front autonome. **RESTE (non bloquant)** : (1) pages devis AUTONOMES `/hebergeur/devis/{nouveau,[id]/modifier}` non gatées UI → **rattaché au chantier « écrans hébergeur hors page séjour » (25/08)** ; niveau `devis` hors séjour = `usePermissions` (centre courant), pas `mesPermissions` ; back bloque la sauvegarde → à cadrer après lecture des pages + du hook. (2) **CHOIX (a) ASSUMÉ** : « Annuler ce devis » inaccessible si `facturation:NONE` (bouton dans le pipeline masqué), cas rare, non corrigé ; à rouvrir sur besoin terrain seulement. Détail : SESSION_STATE 26/08.
> **Dernière mise à jour : 25/08/2026 — AUDIT SÉCURITÉ ENDPOINTS HÉBERGEUR : 3 TROUS FERMÉS + FIX UI MANDAT (LIVRÉ).** Audit read-only de tous les contrôleurs backend (motif : un `@Roles(HEBERGEUR)` seul ne garde pas un collaborateur de centre, qui est un User `Role.HEBERGEUR`). Fermés : versements facture (`575abe1`, facturation:WRITE), acceptation mandat Chorus Pro réservée au propriétaire (`1401fe3`), suppression de l'endpoint `simuler` qui octroyait un plan payant sans Mollie (`6068b5f`). Fix UI en cohérence : bouton d'acceptation du mandat masqué au collaborateur + erreur 403 rendue visible dans la modale (`7119552`). Détail : `docs/audits/PLAN_REMEDIATION.md` LOT 7 + SESSION_STATE 25/08 (3). RESTE au backlog : chantier « écrans hébergeur hors page séjour » (18 écrans non gatés UI ; premier item `inviter-enseignant`, bouton actif → 403 non géré pour un collaborateur lecture).
> **Dernière mise à jour : 24/08/2026 (6) — LOT 2 (Étape 4) CLÔTURÉ : recetté-PAR-LECTURE (pas de comptes de test). Cartographie du fallback membership + dette bornée consignée.** Les 4 surfaces du Lot 2 non testées faute de comptes (contrôle négatif, offres #6, PDF signataire #7, PDF budget) ont été recettées par LECTURE de la chaîne de données payload→lecteur sur code réel, plutôt qu'en montant un env de test (comptes `directeur@test.fr` / `enseignant@test.fr` dispo mais recette visuelle rendue superflue). Code local vérifié `main` @ `49c16ba`. VERDICT : la bascule vers `resolveClientEtablissement` est réelle sur toutes les surfaces (DevisCard + offres + signataire depuis le séjour RACINE), et `clientOrganisation` est chargé dans les 8 payloads d'identité SANS exception → l'établissement s'affiche correctement sur tous les dossiers réels (le backfill 21/08 a aligné clientOrganisation). DÉCOUVERTE : le fallback membership du canonique est hétérogène — VIVANT sur 6 payloads, MORT sur 2 (`getComparatif`/offres #6, `getDevisById`/modifier #9, dont les selects ne chargent pas les memberships ; nuance Prisma `include` racine ⇒ scalaires implicites vs `select` explicite ⇒ champs listés only). IMPACT TERRAIN NUL : le fallback ne diverge de clientOrganisation que pour un dossier « membership présent + clientOrganisation NULL », population vidée par le backfill. DÉCISION Théo = C' : clore le Lot 2 recetté-par-lecture, NE PAS fixer maintenant. DETTE : « fallback membership mort offres/modifier — se réveille si un COLLAB naît sans clientOrganisation (DIRECT sans client basculé COLLAB via accepter() rattachement + enseignant avec membership) → établissement vide sur ces 2 surfaces ». FIX prêt le jour venu = option A' (selects additifs memberships sur les 2 payloads, pattern Lot 1, additif, zéro cascade). Observation annexe transverse : les `buildPdfProps` des PDF devis mettent `adresseDestinataire = ville` seule (jamais rue/CP) — pré-existant, à border si besoin conformité. ⚠️ Doublon `FL-2026-001` = TRANCHÉ NON-DOUBLON (couple facture+devis LÉGITIME Neuilly-Plaisance : Nora avait la facture, la mairie exige un devis pour le bon de commande ; DL-2026-002 émis a posteriori, même période/montant exprès) — retiré du backlog, le ⚠️ des entrées (3)/(5) est éteint. Détail complet : SESSION_STATE 24/08 (6).
> **Dernière mise à jour : 24/08/2026 (5) — ÉTAPE 4 CLOSE EN PROD : LOT 2 (bascule des 15 lecteurs vers `resolveClientEtablissement`) + LOT 3 MINIMAL (édition des coordonnées client par l'hébergeur en COLLAB) LIVRÉS / POUSSÉS / DÉPLOYÉS. `origin/main` = `49c16ba`.** LOT 2 en 4 groupes tous relus diff-par-diff avant push : G4 `524eaeb` (invitation-direction), G1 `9e23959`/`7809a4d`/`0efeca2`/`0f30845` (DevisCard + recherche/tri + 4 pages devis), G3 `26de15b` (signataire écran+PDF, résolu depuis le séjour RACINE — census #7 rectifié), G2 `65be173` (pédago onglet + 2 PDF ; +8 champs au type local `ProjetPedagogiqueData` pour lever un weak-type TS2559) + `fcd0091` (budget). `findOpen` reste OLD (interdiction client* permanente, §4bis). LOT 3 MINIMAL `49c16ba` : `SejourHeader` — `peutEditerClient = isHebergeur && monAccesEcriture` remplace la garde `isDirect` sur les 3 sites d'édition client → les 7 coordonnées (`clientNom/Prenom/Email/Telephone/Adresse/CodePostal/Ville`) éditables en COLLAB comme en DIRECT ; backend `updateInfosSejour` déjà prêt, aucune modif back. **`clientOrganisation` reste NON éditable** (hors DTO) — Lot 3-bis SI besoin terrain seulement. **DÉCLENCHEUR = remontée Anne** (Choucas, WhatsApp 24/08) : sur séjour confirmé COLLAB (NDL classe de neige 2027) impossible de corriger email/tél du client → cause = garde `isDirect` → fix = Lot 3 minimal. RECETTE : Lot 2 partielle mais **CŒUR VALIDÉ** (captures Nogent `ee807f71` : établissement+contact identiques en-tête / PDF devis DEV-2026-0014 / convention ; contact = enseignant Laure) ; contrôle négatif + surfaces organisateur/signataire NON testés (pas de comptes de test) ; recette du Lot 3 confiée à Anne (elle possède le dossier), message WhatsApp rédigé. ⚠️ Rappel intendance inchangé : vérifier doublon `FL-2026-001` (Choucas Pilotage 14/08, 345€) AVANT facture-période Neuilly-Plaisance. Détail : SESSION_STATE 24/08 (5).
> **Dernière mise à jour : 24/08/2026 (3) — DEVIS LIAVO SUR PÉRIODE N MOIS livré + poussé + recetté prod (devis d'Anne envoyé).** Besoin terrain Anne (Choucas) : la mairie de Neuilly-Plaisance exige un devis identique à la facture pour son bon de commande, or le devis LIAVO n'existait qu'en fréquence (mensuel/annuel), pas en période — seule la facture l'avait. Ajout d'un devis DL- sur période N mois (miroir de `facturerCentrePeriode`), en FERMANT 2 dettes de l'audit 24/08 au lieu d'en créer : (1) double source de prix → source unique `abonnement.constants.ts` ; (2) triple copie du bloc params PDF → helper unique `buildLiavoPdfParams`. 3 commits, `origin/main` = `c74b7c9` (push sélectif `git push origin c74b7c9:main` — l'Étape 4 `b4dfcf7` de l'autre session reste locale). Recette prod : DL-2026-002, Neuilly-Plaisance, Pilotage 5 mois = **345 € TTC**, envoyé à Anne. Le devis est éphémère (0 persistance) ; la facture FL- n'a pas été testée en prod. ⚠️ **À VÉRIFIER AVANT D'ÉMETTRE LA FACTURE-PÉRIODE** : une FL-2026-001 (Choucas, mensuel, 14/08, 345 €) existe déjà — s'assurer de ne pas double-facturer un mois. Détail technique complet : SESSION_STATE 24/08 (3).
> **Dernière mise à jour : 24/08/2026 (4) — CHANTIER ÉTAPE 4 : CADRAGE VALIDÉ + CENSUS CONTRE-VÉRIFIÉ + LOT 1 LIVRÉ EN PROD.** Doc de cadrage `docs/CHANTIER_ETAPE4_CLIENT_HEBERGEUR.md` validé (Option A : util frontend `resolveClientEtablissement()` + selects backend ADDITIFS ; décisions E1-E5 + §4bis). Census Phase 1 CC (`docs/audits/CENSUS_ETAPE4_LOT1_2026-08-24.md`, gitignored) **contre-vérifié par Claude 10/10 sur code réel**. Découvertes : liste réelle = 11+6 lecteurs (E-a…E-f) ; 3 lecteurs DÉJÀ SOMBRES (#6 offres/getComparatif, #7-PDF signataire/getDevisAValider, #9 modifier/getDevisById : payload sans le champ lu → vide aujourd'hui, passeront vide→rempli au Lot 2) ; trou census comblé par Claude : `getFacturesAcompte` ajouté au lot. DÉCISIONS THÉO : E-a/E-b/E-e/E-f INTÉGRÉS (→ **15 lecteurs à basculer au Lot 2**) ; E-c/E-d (`hebergeur/demandes` via findOpen) **EXCLUS — interdiction permanente de tout champ client* dans findOpen** (RGPD masquage par plan + demande ouverte = pas encore de propriétaire hébergeur). **LOT 1 LIVRÉ** : `b4dfcf7` (backend — selects additifs, 10 méthodes / 4 fichiers, les 8 client* + compléments contact createur/enseignant) + `be8aead` (frontend — `src/lib/client-etablissement.ts` fonction pure + spec vitest 8/8 + extensions de types OPTIONNELLES devis.ts/collaboration.ts/sejour.ts ; `demande.ts` INTACT volontairement). Gates verts (tsc 0, builds, 407 tests back, vitest front). Diffs relus par Claude 10/10 sur fichiers réels + contrôles négatifs prouvés (findOpen, getDevisForSejour, 3 include-racine, résolveurs documents, accepter(), parcours signature — tous intacts caractère pour caractère). Poussés (`c74b7c9..be8aead`), déployés (2 apps), **recette d'invisibilité VERTE** (NDL prod identique ; bonus capture : panneau Modifier du header COLLAB = AUCUN champ client → preuve « avant » pour la recette Lot 3). ÉCART consigné : `invitation-direction/[token]` type local `devis: any` → rien à étendre (census optimiste), à durcir au Lot 2 ; découverte : runner vitest frontend EXISTE (`vitest.config.ts`). ⚠️ Note protocole : le push a été exécuté depuis la session CC (feu vert Claude donné après vérification 100%, zéro dégât) — la règle « CC ne pousse jamais » est à réaffirmer en tête des prochains prompts si l'exception n'était pas voulue. **PROCHAINE ÉTAPE : LOT 2 en SESSION DÉDIÉE** — bascule des 15 lecteurs vers le canonique, prompt découpé par groupes de payloads, changements VISIBLES attendus (7 dossiers graphie/remplissage + 3 surfaces vide→rempli + comportement recherche E-a/E-b), **PRÉVENIR ANNE avant déploiement**. Puis Lot 3 (édition hébergeur COLLAB). Détail : SESSION_STATE 24/08 (4).
> **Dernière mise à jour : 24/08/2026 (2) — COMPLÉMENT dette « signature collab » (19/08 (3)/(4)) + RÉGULARISATION DOSSIER NOGENT (SQL prod).** COMPLÉMENT (vérifié sur code réel `devis.service.ts`) : le passage EN_ATTENTE→SELECTIONNE (auto-sélection `accepter()` branche rattachement, ou sélection organisateur) **VERROUILLE les 3 parcours de signature en ligne** — `signerDevisDirect`, `envoyerADirection` et `envoyerDevisDirect` (renvoi du lien) exigent tous `statut === 'EN_ATTENTE'` → un devis SELECTIONNE non signé n'est plus signable en ligne, plus renvoyable, plus délégable à la direction. Issues restantes : `marquerDevisSigneHebergeur` (SELECTIONNE→SIGNE_DIRECTION, signature hors plateforme) et `emettreAcompte` (SELECTIONNE = facturable SANS signature). Renforce l'option B de 19/08 (3) ; à traiter dans la même branche hybride d'`accepter()` que le chantier propriété client. NOGENT (`ee807f71`, Choucas — cas terrain type de cette dette : rattaché 23/08 08:55, CONVENTION forcé, devis DEV-2026-0014 auto-SELECTIONNE) régularisé ce jour en SQL prod : compte doublon `laure.abratkiewicz@ac-amiens.fr` supprimé (stub jamais vérifié, 0 donnée métier, census 12 tables avant DELETE, double garde id+email, tx) ; titre « MAIRIE DE NOGENT SUR MARNE »→« MAIRIE DE NOGENT SUR OISE » sur `sejours` ET `demandes_devis` (synchro F1 respectée — `updateInfosSejour` relu avant : sa synchro inclut les demandes FERMEE, seul ANNULEE exclu) ; contact posé (Laure Abratkiewicz) ; `client_organisation` = COMMUNE DE NOGENT SUR OISE déjà correct, NON touché. Devis reste SELECTIONNE non signé, 0 facture émise — sans nuisance. Détail session : SESSION_STATE 24/08 (2).
> **Dernière mise à jour : 24/08/2026 — AUDIT « SOURCES DE VÉRITÉ DUPLIQUÉES » (run CC Fable lecture seule, 21/08 soir) CONSIGNÉ.** Objectif : cartographier les doublons de règle de vérité (une même donnée métier vivant à 2 endroits qui peuvent diverger) pour ne plus « réparer un endroit et découvrir après coup qu'il faut en réparer un autre ». Brief + 2 livrables dans `docs/audits/` (`AUDIT_DOUBLE_SOURCE_BRIEF.md`, `RAPPORT_DOUBLE_SOURCE_2026-08-21.md`, `INDEX_CHAMPS_2026-08-21.md`). ⚠️ **`docs/audits/` est GITIGNORED → ces fichiers ne sont PAS dans git, uniquement sur le disque local. À sauvegarder.** RÉSULTAT : 11 concepts (2 🔴 / 7 🟡 / 2 ⚪). 🔴 #1 effectif = la dualité effectif déjà connue (21/08 (3)). 🔴 #2 identité client COLLAB = confirme et rend EXHAUSTIF le lot « bascule secondaire » déjà identifié (TabBudget, DevisCard, dashboards, PDF, page tokenisée) — PAS neuf, mais l'audit fournit la liste complète des ~12 lecteurs + a montré (vérifié Claude sur code réel) que `DevisCard.resolveEtablissement` PRÉFÈRE l'ancienne source. Divergence LATENTE aujourd'hui (backfill = 2 sources égales), ACTIVE dès qu'on édite l'identité (Étape 4). NEUF : #3 émetteur (cascade 4 sources, priorité nom/adresse incohérente), #4 totaux Decimal/Float (→ 🔴 quand TVA sur marge multi-taux activée), #5 abonnement (centre gelé/org vivante + commentaire schéma INVERSÉ = piège doc à corriger), #6 versements (2 caches), #8 CRM (3 modèles), #10 placesRestantes = champ MORT. **Les 11 se regroupent en 4 FAMILLES de racine commune** (apport Claude) : F1 dualité séjour/demande (effectif/dates/métadonnées — 1 seule décision « qui fait foi »), F2 identité dupliquée (client/émetteur/CRM), F3 caches de sommes (totaux/versements), F4 isolés. Détail complet + recoupements : SESSION_STATE 24/08. **PROCHAINE ACTION avant tout dev : SELECT prod pour savoir si la divergence identité client #2 est déjà active ou latente.** Rien ne bloque le dev sauf potentiellement #2 selon ce SELECT.
> **Dernière mise à jour : 21/08/2026 (3) — 3 FIXES TERRAIN CHOUCAS LIVRÉS (items 1/4/6 de 19/08 (4)) + SUJET D'ARCHI « DUALITÉ EFFECTIF » OUVERT.** Les 3 petits fixes vérifiables de l'entrée 19/08 (4) sont en prod : #1 la liste des séjours affiche les accompagnateurs (« 50 pers. (45 + 5) ») ; #4 la durée d'une activité catalogue est reportée au drop sur le planning (fallback 60 min) ; #6 l'effectif est pré-rempli dans « Modifier le devis » d'un séjour DIRECT (+ la quantité d'une ligne catalogue s'aligne sur l'effectif). 3 commits séparés (fix 1 d7a5dcc, fix 4 95bd4cb, fix 6 e38a200), gates verts, recette prod OK en DIRECT (contrôle COLLAB différé sur NDL, risque quasi nul). Fix 6 : découverte que le champ effectif alimente montantParEleve — pas cosmétique, corrige un calcul faux persisté en DIRECT. Détail technique complet : SESSION_STATE 21/08 (3). RESTENT de 19/08 (4) : #2 suppression de documents (décision produit : supprimer vs remplacer), #5 couleur par activité (à border : par activité catalogue vs par groupe) — non traités, à cadrer. #3 = non-bug classé.
> 🏗️ SUJET D'ARCHI OUVERT — « DUALITÉ EFFECTIF » (né du fix 6, census sur code réel) : l'effectif d'un séjour vit à DEUX endroits — les champs placesTotales/nombreAccompagnateurs du séjour ET nombreEleves/nombreAccompagnateurs de la demande de devis (COLLAB) — et le code lit demande?.X ?? sejour.X (demande prioritaire). Le bouton « Modifier » du header écrit l'effectif sur le séjour mais ne synchronise la demande que pour titre/dates. CONSÉQUENCE : en DIRECT (pas de demande) l'effectif du séjour est source unique et se propage partout (header, budget, planning, liste, devis) ; en COLLAB, le modifier via le header ne se répercute PAS sur les calculs qui lisent la demande (proposition de groupes, pré-remplissage devis) = désync silencieuse. Les chambres sont une affectation manuelle indépendante de l'effectif dans les 2 modes. Origine = mode DIRECT greffé sur un socle COLLAB, le fallback demande ?? séjour est la couture, « qui fait foi » n'a jamais été tranché. CHOIX INITIAL THÉO = solution A (source unique : le séjour fait toujours foi, on arrête de lire la demande pour l'effectif) — vrai fix durable voulu, PAS la rustine B (synchro à l'écriture). ⚠️ À RECHALLENGER AVANT TOUT DEV, décision non définitive : exige une lecture COMPLÈTE de l'enjeu — audit exhaustif des lecteurs de demande.nombreEleves, garantie que placesTotales est peuplé à la création COLLAB, mesure du risque de régression COLLAB, arbitrage sur la perte de la distinction « demandé » vs « courant ». Session de cadrage dédiée (au même titre que #2 et #5). NON URGENT : activité réelle en DIRECT, la dette ne mord pas aujourd'hui.
> **Dernière mise à jour : 21/08/2026 (2) — DETTE DE TESTS « DESTINATAIRE COLLAB » SOLDÉE (facture + convention). Ferme l'angle mort de tests noté en 21/08 (1).** 3 commits atomiques (`8196027` + `be68e1a` facture, `1b4a44e` convention), gates verts, **POUSSÉS + déployés** (`origin/main` = `a63e6ae`). ✅ **BACKFILL Op1/Op2 CLOS — ne plus en parler** : Étape 3 + backfill en prod depuis le matin du 21/08, contrôle prod = 5 dossiers COLLAB vides résiduels tous légitimes (DRAFT/OPTION/SUBMITTED + 1 TEST), aucun dossier client réel impacté. Facture : tests de la branche COLLAB de `construireDestinataire` à travers `emettreAcompte` (nom = `clientOrganisation`, adresse sérialisée `||` Factur-X, SIRET null ; fallback nom enseignant si org absente). Convention : extraction fonction pure `resoudreEtablissement` + 7 cas figeant les fallbacks DISTINCTS DIRECT (3 niveaux) / COLLAB (2 niveaux) contre toute harmonisation silencieuse. **RESTE (septembre, même chantier propriété client)** : (1) garde-fou de l'`include` Prisma — les tests figent la logique de choix du nom, PAS le chargement des champs `client*` sous `demande.sejour` ; un `include` resserré = tests verts / prod cassée, rehearsal local seul filet ; (2) bascule des lectures secondaires (TabBudget, DevisCard, dashboards organisateur/signataire, page publique tokenisée) ; (3) Étape 4 (édition `clientOrganisation` côté hébergeur). Détail technique complet : `LIAVO_SESSION_STATE.md` (entrée 21/08 (2)).
> **Dernière mise à jour : 21/08/2026 — CHANTIER RACINE « PROPRIÉTÉ CLIENT » : ÉTAPE 3 + BACKFILL LIVRÉS EN PROD.** Bascule lecture COLLAB (devis PDF + convention + facture lisent désormais les champs `client*` du séjour au lieu de l'org du créateur) — 2 commits `bbfe454` (backend) + `ebf8531` (frontend), zéro migration, déployés verts. Backfill prod exécuté AVANT le push (ordre correct, aucune fenêtre de vide) : Lot A 8 dossiers (projection membership `is_primary`) + Lot B 3 dossiers saisis à la main (Collège Lucien Herr, École Bilingue Maria Montessori, CROC'KIDS ASBL). Recette prod OK : Loué → « Collège Saint-Michel » sur les 3 documents. **Découverte structurante** : `createur_id` est écrit par l'acceptation d'invitation collab (`accepter()`), donc DIRECT-basculé et COLLAB-pur sont mélangés sous `createur_id NOT NULL` — le backfill touchait 11 dossiers réels (dont les Sauvageon), pas 3. **Reste ouvert** : Étape 4 (édition `clientOrganisation` côté hébergeur), bascule des lectures secondaires (TabBudget, DevisCard, dashboards, page publique tokenisée), angle mort de tests sur le chemin destinataire COLLAB, soft-delete DRAFT Lucien Herr 2. Détail complet dans `LIAVO_SESSION_STATE.md` (entrée 21/08).
> **Dernière mise à jour : 20/08/2026 (2) — CHANTIER RACINE « PROPRIÉTÉ CLIENT » : dette dotenv soldée (`4f6f1cd`) + ÉTAPE 2 PROJECTION COLLAB LIVRÉE EN PROD (`994bcfa` + `1649154`, gates verts, zéro migration). Backfill NON exécuté, Étape 3 NON commencée. Détail opérationnel complet + census prod des 18 dossiers dans SESSION_STATE 20/08 (2) ; source de vérité du chantier = `docs/CHANTIER_PROPRIETE_CLIENT_PLAN_CC.md`.**
> Projection = les nouveaux séjours COLLAB-pur (`creerDepuisCatalogue` + `accepter()` branche DRAFT) copient désormais l'org principale de l'enseignant dans leurs champs `client*` (Option A : résolu hors tx ; A2 pour DRAFT : org prioritaire, `invitation.etablissement*` en fallback). **Rien de visible n'a changé** : les 18 dossiers existants restent `client_organisation` NULL et les documents lisent toujours `getOrganisationPrincipale` tant que la bascule (Étape 3) + backfill ne sont pas déployés ensemble. **INVARIANT : Étape 3 jamais déployée tant que backfill Op1 (10 projetables) ET Op2 (3 CONVENTION sans org : Lucien Herr, CROC KIDS, Montessori) pas faits.** PROCHAINE ACTION = census Étape 3 (bascule lecture : convention devis.service + construireDestinataire facture.service [piège include à élargir, déjà identifié] + TabDevisFacturation front), puis rehearsal local groupé. Décision Théo : séquençage A2 (grouper 2+3+backfill), inclure les 3 CONVENTION en Op2 séparée.
> **Dernière mise à jour : 19/08/2026 (5) — CHANTIER « ACCÈS COLLABORATEUR D'ÉQUIPE » CLOS DE BOUT EN BOUT (8 commits en prod).** Les collaborateurs d'équipe (`CollaborateurCentre` accepté) étaient rejetés de tout l'espace séjour par la garde propriétaire-seul de `verifyAccess` (`centre.userId === userId`, sans consulter les permissions) — confirmé 3/3 en prod (Anne/Choucas, Valentin/Sauvageon READ, Thomas/Pôle Montagne WRITE) ; la LISTE des séjours était collaborateur-aware mais pas le DÉTAIL, d'où des séjours visibles mais inouvrables. DÉCISIONS DE RÉFÉRENCE (ne pas relitiger) : #1 gate `sejours` global (READ=GET, WRITE=mutations) ; #2 CRM (notes/activités/rappels) sur module `crm` ; #3 `sejours:WRITE` ⇒ écriture complète (chambres/groupes/rooming inclus, bornée au DIRECT géré en propre). LIVRÉ (C1-C5, `241e35a`→`40dae5b`) : verifyAccess collaborateur-aware + `requiredLevel`, verifyHebergeur/crm, updateInfosSejour, resoudreAccesRooming, participants (helper `peutEcrireSejourEnPropre`), flags serveur `monAccesEcriture`/`monAccesCrm`/`monAccesGestionPropre` + gating UI. AUDIT AUTHZ (censuses dédiés #2 `fce8e8b`, #3 `51b28b1`) : `PermissionGuard` + `@RequirePermission` posés sur sejour/activites-client/invitation-collaboration/demande — les niveaux n'y étaient pas enforced (un collab READ pouvait supprimer un séjour via l'API). Reste vérifié exhaustivement safe (abonnement/collaborateur owner-gated en service, pilotage/users reads, organisations claim) — aucun gap résiduel. 🔴 À FINIR — POLISH UI (non bloquant) : masquer les boutons d'écriture encore visibles au collaborateur READ sur messages/planning/journal/documents/budget (gate via `monAccesEcriture` ; aujourd'hui 403 propre côté serveur, purement cosmétique). Détail technique complet : SESSION_STATE 19/08 (4).
> **Dernière mise à jour : 19/08/2026 (4) — 📝 RETOURS TERRAIN CHOUCAS (Anne/Nora via Théo) + ⚠️ PRÉCISION SIGNATURE COLLAB. RIEN CODÉ — backlog à trier, aucun cadré.**
>
> **RETOURS TERRAIN (6 items en vrac, tri par nature, à instruire sur le code avant tout dev) :**
> 1. **Séjour — accompagnateurs non affichés** : on voit le nombre de participants mais PAS le nombre d'accompagnateurs. Donnée `nombreAccompagnateurs` existe (utilisée ailleurs). Fix d'affichage probable. À LOCALISER : quel écran exactement (header page séjour / liste séjours / dashboard) — le fix dépend de l'endroit. Vérifiable sur code.
> 2. **Documents (paramètres) — suppression impossible** : les documents sont figés, pas de bouton supprimer. À TRANCHER (décision produit) : suppression DÉFINITIVE vs simple REMPLACEMENT ; ⚠️ prudence si un doc est rattaché à un séjour/convention (effets de bord). Lire le composant Documents côté paramètres avant.
> 3. **CROC KIDS — RAS (non-bug, pour info)** : même établissement qui vient 2 semaines de suite, il a bien SÉLECTIONNÉ LES DEUX devis. Confirmé Théo. Rien à faire, retiré de la liste des tâches (c'était le cas multi-semaines légitime déjà connu, pattern F3).
> 4. **Récup activité catalogue → planning — durée non reportée** : quand on récupère une activité depuis « activités proposées », sa durée ne correspond PAS à la durée enregistrée sur le catalogue. Report de champ manquant au drop. À lire : drag catalogue dans TabPlanning + `genererPlanningIA`. Vérifiable sur code.
> 5. **Planning — couleur par activité** (feature affichage, pas un bug) : demande d'une couleur différente par activité pour la lisibilité visuelle. À BORDER : couleur par ACTIVITÉ du catalogue (chaque activité sa teinte) vs par GROUPE (existe déjà en partie) — les deux logiques coexistent mal si mal pensées, cadrer avant de coder.
> 6. **Devis brouillon envoyé — effectif absent de la modale Modifier** : le nombre de participants est bon EN HAUT et dans « Voir » le devis, mais dans la modale « Modifier » le champ effectif n'est pas pré-rempli. Fix ciblé : la modale modifier ne lit pas l'effectif que l'affichage connaît. À lire TabDevisFacturation (modale modifier). Vérifiable sur code.
>
> **CLASSEMENT (à confirmer Théo)** : petits fixes probables vérifiables = 1, 4, 6 ; à cadrer (décision produit) = 2 (supprimer vs remplacer), 5 (couleur par activité vs groupe) ; non-bug = 3.
>
> **⚠️ PRÉCISION SIGNATURE COLLAB (Théo, complète et renforce 19/08 (3)) — PROBLÈME RÉEL, PAS un simple angle mort UX** : quelqu'un est invité à collaborer SANS avoir reçu le devis. Il clique « rejoindre le séjour » → `accepter()` fait passer le séjour en COLLABORATIF + **CONVENTION** ET auto-sélectionne le devis EN_ATTENTE → **SELECTIONNE**. FACTUEL (vérifié sur code `accepter()`) : `sejour.update` pose statut CONVENTION + createurId + modeGestion COLLABORATIF ; `devis.update` pose statut SELECTIONNE ; **AUCUN champ de signature n'est écrit** (pas de `signatureDirecteur`, pas de `nomSignataireDirecteur`, pas de `dateSignatureDirecteur`, pas d'IP, pas de hash). **CONSÉQUENCE : l'hébergeur n'a JAMAIS de devis SIGNÉ** — juste un devis SELECTIONNE + le clic « accepter » comme seule trace. INTERPRÉTATION : l'engagement est OK (SELECTIONNE ∈ STATUTS_DEVIS_ENGAGEANTS + RETENUS → acompte facturable, convention générable), MAIS la preuve est FAIBLE (un clic ≠ une signature tracée) et l'hébergeur n'a AUCUN document signé à conserver. C'est le MÊME trou que 19/08 (3) sous un autre angle : le parcours collab **substitue « clic accepter » à « signature »**, en silence. → Renforce le besoin de trancher les 3 options (A/B/C de 19/08 (3)), et pose une QUESTION supplémentaire à border : à l'acceptation d'une invitation, faut-il router vers une VRAIE signature (au lieu d'auto-sélectionner en silence) OU au moins offrir à l'hébergeur un geste « marquer signé » / récupérer un document signé a posteriori ? NON TRANCHÉ.
>
> **Dernière mise à jour : 19/08/2026 (3) — 🔍 ENQUÊTE SIGNATURE DEVIS CÔTÉ ORGANISATEUR : chaîne complète lue sur le code réel, TROU IDENTIFIÉ (espace connecté sans signature riche), 3 OPTIONS POSÉES, NON TRANCHÉ. RIEN CODÉ.**
>
> **DÉCLENCHEUR** : inquiétude Théo « on recule au lieu d'avancer » sur le dossier NDL/Jocelyne — au tout début de l'app, un enseignant qui reçoit un devis (appel d'offres OU invitation directe hébergeur→organisateur) devait bien pouvoir SÉLECTIONNER ET SIGNER. Vérification exigée sur le code avant toute affirmation à un client (ne PAS écrire à Nora « Jocelyne signe depuis son espace » sans preuve). Lecture intégrale effectuée : `devis-public.controller.ts`, `devis.controller.ts`, `devis.service.ts` (updateStatut / signerDevis / signerDevisDirect / envoyerADirection / uploadSignaturePublic / getDevisAValider), `demande.service.ts` (getComparatif / findOpen), `invitation-collaboration.service.ts` (accepter), pages front `organisateur/page.tsx` + `organisateur/sejours/[id]/offres/page.tsx` + `TabDevisFacturation.tsx`.
>
> **CONSTAT FACTUEL — DEUX MONDES DE SIGNATURE, séparés par construction :**
> - **Espace organisateur CONNECTÉ (page Offres `/dashboard/organisateur/sejours/[id]/offres`)** : le SEUL geste est **« Sélectionner »** (bouton affiché ssi `d.statut === 'EN_ATTENTE'` → `updateDevisStatut(id, 'SELECTIONNE')`). `updateStatut` (statut SELECTIONNE) fait TOUT le workflow : écarte les autres devis (NON_RETENU), ferme la demande, rattache l'hébergeur, passe le séjour en CONVENTION, notifie l'hébergeur. **La SÉLECTION EST l'engagement** — pas de « signature » riche, pas de nom/IP/hash. AUCUN bouton « signer soi-même » ni « envoyer à ma direction » dans l'espace connecté.
> - **Lien tokenisé PUBLIC (`/devis/signer/[token]`, envoyé par email)** : porte les 3 vrais gestes de signature — `signerDevisDirect` (signe soi-même, avec nom+IP+userAgent+hash sha256), `envoyerADirection` (crée une InvitationDirecteur → email au directeur → `/invitation-direction/[token]`), `uploadSignaturePublic` (scan PDF signé). C'est le SEUL endroit avec signature tracée. Recetté 17/08.
> - **Espace SIGNATAIRE connecté (directeur)** : `getDevisAValider` liste les devis EN_ATTENTE_VALIDATION ; `signerDevis` (@Roles SIGNATAIRE) → SIGNE_DIRECTION. Existe, mais suppose qu'un compte directeur existe ET qu'on lui ait routé le devis (via envoyerADirection ou EN_ATTENTE_VALIDATION).
>
> **RÉPONSE Q1 (appel d'offres — l'enseignant peut-il signer lui-même ou est-il obligé de passer par sa direction ?)** : NI l'un NI l'autre. Il n'y a pas de « signature enseignant » sur un appel d'offres — il y a une SÉLECTION qui vaut engagement. L'enseignant peut engager SEUL par la sélection (jamais obligé de déléguer). La délégation au directeur est OPTIONNELLE (passer par EN_ATTENTE_VALIDATION puis signature SIGNATAIRE, pour les établissements qui l'exigent).
>
> **RÉPONSE Q2 (séjour DIRECT ouvert en COLLABORATIF — comment on a pu passer à côté, bloqué signature par lien token uniquement ?)** : `accepter()` (invitation collab sur séjour DIRECT existant) fait DIRECT→COLLABORATIF + CONVENTION ET **auto-passe le devis EN_ATTENTE → SELECTIONNE** : l'acceptation de l'invitation VAUT sélection. MAIS ce parcours ne couvre QUE le cas où le devis existe DÉJÀ à l'acceptation. Le cas « inviter à collaborer AVANT de créer le devis » tombe dans l'angle mort : à l'acceptation il n'y a rien à auto-sélectionner ; le devis créé APRÈS (chemin demande, `create`) naît EN_ATTENTE ; dans l'espace connecté le seul geste offert reste « Sélectionner ». **Les 3 options de signature riche n'ont JAMAIS été construites dans l'espace connecté** — elles vivent uniquement sur le lien public. Ce n'est PAS une régression : c'est un cas jamais traité, rendu visible par le dossier hybride de Jocelyne. (= backlog « 2a » jamais recetté.)
>
> **INCONNUE LEVÉE (getComparatif)** : `getComparatif` fait `findMany({ where: { demandeId } })` — NE filtre PAS sur le statut de la demande. Un devis EN_ATTENTE sur une demande FERMEE (cas Jocelyne : demande `8b585008` FERMEE + devis `59aefa53` EN_ATTENTE) **remonte bien dans la page Offres** ET le bouton « Sélectionner » s'affiche (condition = `statut === 'EN_ATTENTE'`, satisfaite). Donc Jocelyne PEUT sélectionner son devis depuis son espace connecté (l'engager). Ce qu'elle n'a PAS dans l'espace : la signature riche (soi-même tracée / délégation directeur / upload) — uniquement sur le lien email.
>
> **3 OPTIONS POSÉES — NON TRANCHÉ (Théo décide plus tard) :**
> - **Option A — Renommer + ajouter « faire signer par la direction » dans l'espace connecté** (~1j). Garder « Sélectionner » mais le nommer clairement (« Valider et signer ce devis ») + bouton « Faire signer par ma direction » déclenchant `envoyerADirection`. Tue l'impression de recul, réutilise l'existant. LIMITE : la validation organisateur reste un clic simple (pas de nom/IP/hash), plus léger juridiquement que la signature du lien public.
> - **Option B — Câbler les 3 options de signature complètes dans l'espace connecté** (chantier réel : conception + dev + recette). L'espace organisateur afficherait sur un devis EN_ATTENTE les 3 choix du lien public AVEC traçabilité (nom/IP/hash). C'est le vrai « sélectionner ET signer chez soi » de l'intuition Théo, et ce qui scale pour Tereva. = concrétisation du backlog 2a.
> - **Option C — Ne rien changer, router par le lien email** (0 dev). Pour toute signature, l'hébergeur (re)envoie le devis → l'organisateur reçoit le lien tokenisé → 3 options. Marche aujourd'hui. LIMITE : oblige à sortir de l'espace connecté = exactement la friction ressentie comme un recul.
>
> **RECO Claude (non imposée)** : pour Jocelyne MAINTENANT = **C** (lien email, garanti, 0 risque — c'est ce que dit l'email envoyé à Anne/Nora : renvoyer le devis → lien → signature ou délégation) ; pour le PRODUIT = **A court terme** (petit chantier anti-recul) puis **B cadré à froid en septembre** pour Tereva. À trancher : quelle option, et distingue-t-on le cas Jocelyne (urgent) du chantier de fond (septembre) ?
>
> **EMAIL Anne** (dossier NDL) rédigé (2 variantes « Récap + étapes » / « courte »), envoi = action Théo NON exécutée. Contenu sûr : route Jocelyne par le lien email (chemin prouvé), ne promet AUCUNE signature depuis l'espace connecté. ⚠️ Écart destinataire signalé : les notes antérieures visaient Nora (adjointe qui suit Jocelyne) ; cet email est adressé à Anne (directrice) qui peut relayer à Nora.
>
> **Dernière mise à jour : 19/08/2026 (2) — 🎯 DÉCISION D'ARCHITECTURE « PROPRIÉTÉ CLIENT » ACTÉE (modèle C) + 🔴 PRIO 0 DEV IDENTIFIÉE : saisie établissement cassée bloque Anne/Nora sur la création de leurs séjours de janvier. RIEN CODÉ — session de conception, décisions de référence pour ne pas repartir de zéro.**
>
> **CONTEXTE DÉCLENCHEUR** : toute la friction du dossier NDL (SQL pour un téléphone, membership manquant, header gaté, établissement absent du devis) remonte à UNE ambiguïté jamais tranchée — *dans un dossier collaboratif, qui possède l'identité du client : l'hébergeur ou l'organisateur ?* Le produit répond « l'organisateur » PAR ACCIDENT d'implémentation (les documents lisent `createur.memberships[0].organisation`, l'édition client est gatée `isDirect`). Décision Théo : **l'hébergeur est propriétaire** — c'est lui qui vend, facture, entretient la relation ; le compte organisateur est un PORTAIL d'accès au dossier, pas le propriétaire de l'identité commerciale. Preuve terrain : un dossier né chez l'hébergeur puis rattaché (NDV = cas nominal), c'est Nora qui a saisi nom/adresse/tél — le gate collab lui confisque alors l'édition de SA propre donnée.
>
> **MODÈLE CIBLE C — « trois étages, une projection »** (validé Théo) : (1) *Référentiel* (organisation / client CRM avec SIRET·UAI) = source réutilisable, sert à CHERCHER un client connu (ta réponse : la fiche unique réutilisée sur N séjours a de la valeur) ; (2) *Champs client du séjour* = copie de travail de l'hébergeur, PROJETÉE une fois depuis le référentiel à l'invitation/import, éditable par l'hébergeur, **lue en priorité par TOUS les documents** (devis, facture, page signature, header) ; (3) *Snapshot facture* = figé à l'émission (pattern immuable existant). Trois niveaux de gel croissant. VERTU : `construireDestinataire` se SIMPLIFIE (lire le séjour, point) au lieu de se complexifier — la cible réduit la dette. **La projection est un TAMPON ENCREUR one-shot, jamais un fil vivant** : si l'organisation change plus tard, le séjour ne bouge pas (le devis émis ne doit pas muter sous l'hébergeur). Rafraîchir = bouton explicite, jamais auto — recâbler un lien vivant ramènerait le problème actuel.
>
> **DÉCISIONS FERMES** : (a) SIRET/UAI **optionnel à la création, OBLIGATOIRE à l'émission de la facture** (scolaire/structure only ; un mariage = particulier, jamais de SIRET) — ne pas bloquer la création en plein rush janvier pour une info légalement due seulement à la facture ; (b) le SIRET est **snapshotté dans le séjour** (option a, PAS de pointeur vivant vers l'organisation — l'option b « document lit l'org » est du modèle-actuel déguisé, REJETÉE) ; (c) **école (UAI) ≠ payeur (SIRET)** — l'UAI identifie l'établissement EN, le SIRET identifie le payeur qui n'est PRESQUE JAMAIS l'école : Ville de Neuilly-Plaisance (mairie) pour le Choucas, OGEC (asso loi 1901, SIRET propre) pour NDL — un référentiel « une école = un SIRET » fabrique des factures légalement fausses sur le payeur (= backlog « SIRET OGEC null sur Factur-X ») ; (d) migration PROPRE des collab existants (~3 dossiers : NDL, NDV, poignée) = projeter l'org dans les champs séjour, cheap maintenant / cher à 200 — mais « migré » ≠ « facturable » (SIRET OGEC absent → drapeau « payeur manquant », pas blocage).
>
> **INVARIANT D'ORDRE NON NÉGOCIABLE** : bascule de la LECTURE (documents → champs séjour) AVANT ouverture de l'ÉDITION en collab. Ouvrir l'édition avant que la lecture ait basculé = recréer exactement le bug de ce soir (Nora édite une donnée que les documents ne lisent pas → mensonge silencieux). Seul ordre imposé ; le reste est parallélisable.
>
> **POINT DE SAISIE — sélecteur type-first (forme validée, à livrer par tranches)** : à la création depuis le planning, un **sélecteur de type** (École / Collège-Lycée / Association / Entreprise / Mairie / **Autre**) EN AMONT de la recherche. Le type = ROUTEUR DE SOURCE (pas un filtre) : scolaire → annuaire Éducation nationale (clé UAI), le reste → SIRENE, « Autre » → toutes sources. Il SÉPARE aussi type et nom dans l'UI — répare le bug de modèle mental observé en live (« je tape *collège* sans le nom → liste de collèges parasites » : le champ unique mélange *quel genre* et *lequel*). Placeholder DYNAMIQUE selon le type (« Nom du collège (ex. Jean Moulin) ») + micro-copie 1 ligne indiquant OÙ l'on cherche — **PAS de paragraphe explicatif** (si l'UI a besoin d'un pavé d'aide, elle n'est pas assez évidente ; personne ne le lit en rush). « Autre » DISPONIBLE mais en RETRAIT visuel (pas le chemin par défaut = piège de la flemme) et NON bloquant (recherche unifiée dégradée, jamais zéro). Une seule case « Autre » couvre « hors cases » ET « je ne sais pas » (l'hébergeur sait presque toujours si c'est une école ou une entreprise).
>
> **🔴 PLANCHER PRIO 0 — SAISIE MANUELLE STRUCTURÉE PERSISTÉE** (la douleur qui bloque Anne/Nora MAINTENANT) : quand la recherche ne trouve pas, l'hébergeur DOIT pouvoir saisir **type de structure + nom établissement + adresse + CP + ville + nom/prénom du contact + tél + email**, et que ça soit **correctement enregistré comme ENTITÉ** (organisation `source:MANUAL` réutilisable), PAS comme une chaîne volatile. AUJOURD'HUI `StructureSearch.buildFreeText(nom)` fabrique un objet où TOUT est null sauf le nom → sur le séjour n'apparaît que le CONTACT, jamais l'établissement (bug vécu). Ce plancher est INCOMPRESSIBLE (même API EN branchée, il restera des structures absentes) et INDÉPENDANT de tout chantier externe (dédoublonnage contre la base LIAVO locale, pas SIRENE). **Couplage dur** : créer l'organisation MANUAL ne suffit pas — il faut AUSSI la bascule-lecture de C (documents → champs séjour), sinon le collège saisi n'atteint jamais le devis d'un dossier collab. Étape 1 saisie-structurée ET bascule-lecture = LE MÊME LIVRABLE.
>
> **ORDRE DES CHANTIERS (argumenté)** : **Étape 1 (PRIO 0)** = saisie manuelle structurée + persistée + dédoublonnage LOCAL + bascule-lecture documents → champs séjour (rendement CERTAIN et borné, débloque Anne/Nora). **Étape 2** = brancher l'annuaire Éducation nationale comme 2ᵉ source de `searchExternal` (aujourd'hui SIRENE SEULE — cause racine du « référentiel rate les écoles » : une école publique n'a pas de SIREN propre, une privée existe sous son OGEC au nom DIFFÉRENT → SIRENE ne la remonte jamais ; ce N'EST PAS un fix d'une ligne, c'est une 2ᵉ source à intégrer/mapper/fusionner) + sélecteur type-first pour le routage — rendement ÉLEVÉ mais chantier à instruire. **Étape 3 (transverse)** = distinction école/payeur captée à la facturation.
>
> **RÉSERVES EXPLICITEMENT OUVERTES (à instruire, NON tranchées)** : (i) modélisation payeur/école — une entité à deux identifiants, ou deux entités liées ? = modèle de données, SA PROPRE SESSION ; (ii) census de l'API Éducation nationale (faisabilité, format, stratégie de fusion SIRENE+EN, dédoublonnage inter-sources quand un collège public est dans les DEUX) ; (iii) distribution réelle des types de clients (si ~90% scolaire, type-first quasi gratuit ; si mix, vérifier que le clic n'ajoute pas plus de friction qu'il n'aide) et taux de recours « Autre » (s'il explose = cases mal choisies) — À MESURER en septembre sur volume réel. **Backend `/organisations/search` lu ce soir : 1 seul appel à `recherche-entreprises.api.gouv.fr` (SIRENE), cache 5min, timeout 3s ; les badges EN/APIDAE du front sont des COQUILLES MORTES (jamais renvoyés).**
>
> **CE QUI N'EST PAS DANS C** : pas la fin du modèle organisation (reste le référentiel réutilisable), pas de migration big-bang (collab existants dérivent en fallback org jusqu'à leur prochaine édition/migration), rien à coder cette semaine sur les dossiers déjà corrects (NDL/NDV OK pour janvier, la signature n'attend pas ce chantier). Réf. détail conception : SESSION_STATE 19/08 (2).

> **Dernière mise à jour : 19/08/2026 — DOSSIER NDL/CHOUCAS REMIS D'APLOMB DE BOUT EN BOUT (3 commits déployés+recettés `da02cfa`/`6cc793b`/`998b8d3` + 2 interventions SQL prod) — établissement enfin visible sur devis/facture, doublon éteint.** DIAG (captures compte Nora + code réel MCP) : le séjour « NOTRE DAME DE LOURDES » (Option, 50 pers) était un DOUBLON du « Classe de neige 2027 » (Convention, devis DEV-2026-0003) même école/dates 17-22/01/2027 → fausse alerte capacité « 83/88 déficit 45 » ; PDF devis objet « Séjour — — du… » ; tél NDL = celui de NDV (copier-coller). RACINE établissement-manquant : la branche COLLAB du PDF/facture lit `createur.memberships[0].organisation` (filtre `isPrimary:true`) — **Jocelyne n'avait AUCUN membership** (séquelle funnel 17/08) alors que Valérie/NDV en a un → NDV affiche son école, NDL non. **FIX RACINE = DONNÉE, pas code** : organisation ECOLE NOTRE DAME DE LOURDES créée (`b24f607d-4261-4ee1-a786-9938837b4028`, UAI 0561013X, 6 route de Plouay 56650 Inzinzac-Lochrist, source MANUAL) + membership Jocelyne (user `3563909c`, PROPRIETAIRE, is_primary=true) via bloc SQL idempotent autonome (INSERT…WHERE NOT EXISTS). SQL prod aussi : UPDATE séjour `861f22d0` (titre « NOTRE DAME DE LOURDES — Classe de neige 2027 », client=ECOLE, tél/adresse corrects, 45 élèves + 5 accompagnateurs = 88/88 pile) + soft-delete doublon `0485a614` (jumeau `a4affd28` déjà supprimé 25/06). **3 COMMITS CODE (généraux, pas des patchs)** : `da02cfa` backend (getBudgetData select séjour + `lieu`/`niveauClasse` — 2 insertions, 387 tests verts) ; `6cc793b` frontend (`DevisPDF.tsx:221` objet composé `['Séjour',titreSejour,lieuSejour].filter(Boolean).join(' — ')` — `titreSejour` était déclaré mais JAMAIS rendu ; 8 appelants vérifiés passant un vrai titre) ; `998b8d3` frontend (`SejourHeader.tsx` ligne client affichée dès `isDirect || clientNom || clientOrganisation` + adresse/tél — visible en collab, plus seulement DIRECT). **RECETTE PROD VERTE (captures)** : objet « Séjour — NOTRE DAME DE LOURDES — Classe de neige 2027 — Sixt-Fer-à-Cheval du 17/01/2027 au 22/01/2027 » + bloc DESTINATAIRE école sous Jocelyne ; NDV OK aussi. ⚠️ **FORME HYBRIDE du devis `59aefa53` (demande_id `8b585008` FERMEE + sejour_direct_id `861f22d0`) = CANONIQUE, posée volontairement 17/08 — NE JAMAIS LA DÉFAIRE** (Claude avait proposé mode_gestion DIRECT + demande_id NULL, RÉTRACTÉ après lecture SESSION_STATE : Jocelyne utilise son compte, séjour COLLABORATIF via creerDepuisCatalogue). RESTE MÉTIER (relance Nora jeudi 20/08) : repousser validité devis (morte 25/07 — DÉCISION Théo « on s'en fout », résidu affiché accepté) OU pas → renvoyer le lien de signature à Jocelyne depuis l'onglet Devis (possible car EN_ATTENTE, garde `33be375`) → Jocelyne signe via lien tokenisé `/devis/signer/[token]` (recetté 17/08, pas besoin de login) → acompte. BACKLOGS NÉS : devis expiré signable (bloquer ou avertir, à froid) ; fallback destinataire collab sans membership (ceinture, plus le fix principal depuis le membership Jocelyne) ; import mort `getBudgetData` dans `signataire/page.tsx` ; email « infos mises à jour » à liste vide si édition purement client (`updateInfosSejour`) ; bannière « essai gratuit 134j » chez Choucas pourtant facturé FL-2026-001 → **vérif SQL abonnement Choucas** (organisations/centres) ; CA confirmé 0€ dashboard (périodes année civile excluent séjours 2027 — refonte sélecteur année/saison en septembre) ; badge sidebar 6 vs carte 9 devis (métriques différentes, UX à clarifier). Réf. décision archi : ROADMAP 19/08 (2). Détail : SESSION_STATE 19/08.

> **Dernière mise à jour : 18/08/2026 (8) — INCIDENT « DEMANDE CATALOGUE BROADCAST » RÉSOLU DE BOUT EN BOUT (SQL prod + fix `8a4136e` déployé) + CHANTIER F3 ANTI-DOUBLON OUVERT.** CAUSE : `creerDepuisCatalogue` créait la DemandeDevis SANS `centreDestinataireId` → forme broadcast, visible via findOpen chez TOUS les centres compatibles. Cas révélateur : demande Jocelyne `8b585008` (née 17/08 05:21 via catalogue, ciblage voulu = Choucas — intention PROUVÉE : séjour+demande même transaction, séjour né CONVENTION rattaché Choucas, bouton « Travailler avec ce centre ») exposée à 6 centres (Florimont, YAKA, Alticlub, PULSE ×2, Sauvageon), 0 devis tiers émis, sans dégât. Diag CC lecture seule complet : B1 = 1 seule fuite en prod, B2 = 0 broadcast légitime en stock. REMÉDIATION SQL prod : `8b585008` → FERMEE + centre_destinataire_id Choucas (garde WHERE, 1 ligne, contre-vérifiée SELECT + onglet Sauvageon vide). FIX CODE `8a4136e` : `centreDestinataireId: centre.id` dans le create — demande née OUVERTE + ciblée (⚠️ PAS FERMEE : devis.service.create EXIGE statut OUVERTE — découverte relecture : OUVERTE = condition de CRÉATION du devis, pas seulement de visibilité). Census Phase 1 CC 6/6 conforme, contre-vérifié par Claude sur code réel ; EN PROD 18/08 ~17h15 (deployments success ×2 apps). RECETTE DIFFÉRÉE : prochain passage catalogue réel → SELECT centre_destinataire_id NOT NULL sur la demande créée. DÉCISION : ceinture 2 (filtre défensif findOpen) ABANDONNÉE — le cycle nominal ferme la demande à la sélection (updateStatut + signerDevis, prouvé census point f) ; l'incident venait d'un rattachement SQL hors chemins nominaux. Corollaire : demande Jocelyne FERMEE ⇒ pas de nouveau devis via demande (garde OUVERTE) — sans impact, devis `59aefa53` EN_ATTENTE modifiable. INCIDENT MÉTHODE : session CC quittée par erreur pendant la Phase 2 — commit présent mais gates sans preuve → gates relancés localement par Théo (tsc 0, 26 suites / 387+2 verts) avant push ; réflexe gravé : session CC interrompue ⇒ `git status` + `git log` avant toute reprise. BONUS SESSION : comptes 18/08 tracés — GARCIA (invitation Choucas `d20fae37`, chaîne bouclée en 24 s) et Wendy Loubatiere (invitation sur w.loubatiere@mairie-cessy.fr, compte créé sous gmail perso, rattachement 22 s post-register) = 1ʳᵉ PREUVE TERRAIN F1 CROSS-EMAIL ; sentinelles : D1 = 0 token pending, D2 = 7 invitations pendantes vivantes (1ʳᵉ expiration 02/10, rhydelnk@hotmail.fr). **CHANTIER F3 « ANTI-DOUBLON » OUVERT** — session de conception dédiée à planifier (question : F3 AVANT D4 ? la vague d'invitations d'octobre = multiplicateur de risque). Arbitrage ACTÉ Théo 18/08 : côté hébergeur, jumeau détecté ⇒ AVERTIR (jamais bloquer) + suggérer « séjour sans dates fixes, dates posées quand le client tranche » ; 2 branches de message : dates identiques = probable doublon (« ouvrir l'existant ? »), dates proches différentes = client indécis. Matrice 3 cas de référence : CROC KIDS (2 invitations même client, dates DISJOINTES = multi-semaines LÉGITIME, ne jamais bloquer) / jumeaux Choucas 25/06 (mêmes dates, double saisie hébergeur) / doublon organisateur type Jocelyne (invitation pendante ignorée + recréation catalogue, mêmes dates). Garde-fou : ne pas écraser les options datées chevauchantes (usage supporté module chambres, argument anti-Venue360) — suggestion, jamais blocage. DÉPENDANCE DURE : découvrabilité « séjour sans dates » (différé n°1 bloc gestion en propre, introuvable onglets Séjours/Planning) à livrer AVANT ou AVEC ce volet. Périmètre restant à cadrer : garde côté organisateur ; sémantique accepter() sur séjour déjà rattaché (fallback créateur de doublon) ; expiration des demandes ciblées OUVERTE sans butoir ; question de fond : utilité de la DemandeDevis sur le parcours catalogue. Détail : SESSION_STATE 18/08 (8).
> **Dernière mise à jour : 18/08/2026 (7) — ENV LOCAL EN SERVICE — premier login réussi (dashboard hébergeur local).** (1) Démarrage backend local : préambule obligatoire de chargement du `.env` dans la fenêtre PowerShell avant `start:dev` (`Get-Content .env | … SetEnvironmentVariable`), car `mollie.client.ts` lit `process.env` AU NIVEAU MODULE, avant ConfigModule — crash « Missing parameter apiKey » sinon. Frontend : `npm run dev` tel quel. (2) DETTE identifiée : lectures `process.env` au niveau module (`mollie.client.ts` bloquant, `email.service.ts` silencieux) — vrai fix = ConfigService, non urgent, sans effet en prod Scalingo. (3) À VÉRIFIER : modale onboarding affiche « 30 jours d'essai Pilotage » sur l'org seedée « offerte » (trialStartedAt null, exp 2099) — texte statique ou trial amorcé au login ? `SELECT trial_started_at, abonnement_actif_jusqua`. (4) Fil git en attente : seed.ts (garde anti-prod localhost) + roadmap (6)+(7).
> **Dernière mise à jour : 18/08/2026 (6) — ENV LOCAL LIAVO OPÉRATIONNEL + 🔴 TROU HISTORIQUE MIGRATIONS DÉCOUVERT (squash = PRÉREQUIS STAGING).** Setup local complet : `backend\.env` (PG17 local port 5433 base `liavo_dev`, clé Mollie test, S3 bucket `liavo-uploads-dev` OVH dédié ; **sans BREVO_API_KEY = kill-switch email**, sans ENABLE_CRON) + `frontend\.env.local`, invisibles au git (vérifié). DÉCOUVERTE (le test « première chaîne complète hors prod » a payé) : `prisma migrate deploy` from scratch **casse à la 34ᵉ/131 migration** `20260323_add_invitation_fields` — P3018/42P01 « invitations_collaboration n'existe pas » : la table a été créée HORS migration à l'époque (db push ou SQL manuel pré-discipline), la prod n'a jamais rejoué depuis zéro donc jamais rencontré ; d'autres trous peuvent se cacher derrière le premier. BASELINE LOCALE POSÉE (aucun fichier de migration touché, zéro SQL sur _prisma_migrations) : reset base → `db push` (schéma complet, **57 tables**) → 131 × `migrate resolve --applied` → contrôle `migrate deploy` = **« No pending migrations to apply »** ✔ — la base locale est prod-like pour toute migration FUTURE, seul le rejeu historique reste cassé. ⚠️ CONSÉQUENCE STAGING : **SQUASH OPTION 2 (migration init consolidée = baseline, historique archivé) devient PRÉREQUIS de liavo-staging** — toute base neuve cassera au même endroit tant qu'il n'est pas fait ; à caler dans la session staging avant fin août. Note outillage : `prisma.config.ts` (Prisma 7) ne charge PAS `.env` — DATABASE_URL passée dans l'env de commande pour tout `npx prisma *` local (le boot Scalingo n'est pas concerné, la variable est dans le process). Reste local : seed + premier `start:dev` (session suivante).
> **Dernière mise à jour : 18/08/2026 (5) — D2 TRIAL HARMONISÉ + RELANCES PLAFONNÉES LIVRÉ, DÉPLOYÉ (commits `dd23e9c`/`09ade17`/`29c031b`/`964d23b`, zéro migration, tests 382→387, deploy 12:37 UTC).** C1 : les gardes anti-doublon bulk/réseau exigent expiresAt > now (une invitation expirée ne bloque plus un réenvoi — fermait le trou résiduel D1 pour la vague d'octobre). C2 AMENDEMENT DE DÉCISION : le chemin invitation pose désormais PILOTAGE + trialStartedAt (remplace « invitation ⇒ COMPLET offert sans trialStartedAt » de L2e-2) — invités = même essai que tout le monde, ciblés par le cron de conversion, extension self-service accessible, garde c anti-2e-essai active ; orgs COMPLET historiques en grand-père, pas de backfill. C3 : TRIAL_EXTENSION_JOURS = 15 (trial.helper, source unique), demanderExtension 14→15 aux 2 endroits + commentaires 44j→45j, garde seuil 40 inchangée, +2 tests dans la spec EXISTANTE (⚠️ 5e récap CC faux : le census affirmait « abonnement.service.spec.ts n'existe pas » — fichier de 12 Ko sur le disque ; règle durcie : toute affirmation d'ABSENCE d'un rapport CC qui conditionne le périmètre est contre-vérifiée par Claude sur le disque avant validation). C4 : envoyerAlertesExpires borné abonnementActifJusquAu ∈ [now−15j, now[ (~2 emails max post-expiration puis silence — sans borne : email tous les 6j à vie, rien ne bascule jamais INACTIF) + JSDoc corrigée (« Relance HÉBERGEUR », pas admin) + tests forme du where (fake timers) ET comportement. RECETTE : logs cron J+1 8h (4 lignes [cronQuotidien] sans erreur) ; trial invitation recettable à la 1ʳᵉ invitation réelle (SELECT plan=PILOTAGE + trial_started_at) ; noté : chemin invitation ne déclenche PAS la notif « Nouveau trial » (ligne D4 si signal voulu). INCIDENT MÉTHODE : prompt D2 collé dans une session CC trail-data → reconnaissance entamée (git log, grep SESSION_STATE) avant Ctrl+C, zéro écriture vérifiée → GARDE PROJET en tête de chaque futur prompt (vérifier répertoire + présence du rapport census dans la conversation). DÉCISION INFRA : staging Scalingo option B (app liavo-staging, branche staging, iso-prod, seul env qui recette les migrations, anonymisation emails si clone prod) — session dédiée avant fin août, D3/D4 recettés dessus ; mitigations immédiates : deploy en soirée + garde frontend reload chunk manquant (backlog ~20 lignes). RESTE FUNNEL INVITATION : D3 (routage cas 2/3 front — devenu confort, bulk = cas 1) → D4 (outillage octobre : email personnalisé, bouton Inviter fiche catalogue, notif trial invités) → D5 (consolidation matrice claim, 3 implémentations divergentes, après données réelles de la vague). Détail : SESSION_STATE 18/08 (5).
> **Dernière mise à jour : 18/08/2026 (4) — D1 DURCISSEMENT FLUX INVITATION HÉBERGEUR LIVRÉ, DÉPLOYÉ, RECETTÉ SQL PROD (commits `dc54104`/`24918e4`/`2c18798`/`979817d`/`c2823e9`, migration `20260818120000` appliquée au boot 13:52, tests 381→382).** Né de l'arbitrage B vs D (L4 quasi no-op : raisonSociale jamais backfillée + zéro prélèvement Mollie actif → trafic webhook nul ; vague d'invitations décalée à octobre → périmètre D complet en séquence). (1) expiresAt NOT NULL sur InvitationHebergement (backfill created_at+30j : les legacy >30j expirent mécaniquement) ; INVITATION_VALIDITE_JOURS = 30 partagée par les 3 créateurs — admin create/renvoyer, bulkInviteApidae, inviterCentreReseau (écart census 1b : 3e créateur découvert, ReseauService extrait le 10/08) ; le NOT NULL Prisma = tout futur créateur sans expiry ne compile pas. (2) bulkInviteApidae pose centreExistantId → invitations bulk = cas 1 déterministe (fin du matching heuristique email/nom au register). (3) Gardes expiration dans register() (Conflict « invitation a expiré ») + checkInvitation() (retour dégradé). (4) 🔴 Bug reconnexion dormant tué : register() pose emailVerifie: true (token par email = preuve d'accès à la boîte) — sans ça, gate 2 de login() bloquait toute reconnexion par mot de passe des comptes invités (1ʳᵉ session OK via JWT, reconnexion impossible). (5) 🔴 Faille de droits fermée : suppression de la route publique POST /invitations (ouverte AUTORITE/SIGNATAIRE = fabrique de comptes hébergeurs ACTIVE + claim VALIDE + abo offert, zéro appelant) + GET accept + findByToken + purge frontend orpheline /inscription-hebergement/[token] (−183 lignes, écart census 1a). RECETTE SQL : colonne NOT NULL posée ; table = 1 seule invitation historique (Choucas 17/06, cas 1, consommée jour même — l'unique compte du chemin bogué, email_verifie=t déjà rattrapé, zéro compte muet) ; 0 expirée non utilisée. Détail : SESSION_STATE 18/08 (4).
> **Dernière mise à jour : 18/08/2026 (3) — L2d LIVRÉ, DÉPLOYÉ, RECETTÉ SQL PROD.** Commits `345c783` (supplément multi-centre sur les 2 chemins VIREMENT : facturerCentre via calculerMontantAbonnementCents + facturerCentrePeriode via nbMois × (mensuel + supp), count canonique aligné souscrire/webhook, maps PRIX_* locales supprimées) + `4c23ca7` (garde engagement refuserHebergeur : CGV OU FactureLiavo → 409, avant email et deletes ; migration `20260818100000` : acceptations_cgv user_id/centre_id nullable + FK SET NULL, organisation_id Restrict conservé). Décisions : volet 1 option B (les 2 méthodes, pas seulement facturerCentre) ; volet 2 option C = challenge roadmap accepté (le SetNull seul aurait retiré le seul garde-fou accidentel contre la suppression d'un client engagé — Devis.centreId est en Cascade). Recette SQL prod : 2 FK SET NULL + org RESTRICT confirmées (pg_constraint), migration appliquée au boot. Supplément non recettable avant une facture multi-centre réelle ; renouvellement Choucas janvier (1 centre, 690€) = non-régression naturelle. Backlogs notés : pré-calcul montant dans DevisLiavoForm avant émission (le jour Tereva 14 centres) ; maps DL- de facture-liavo.service toujours plan sec (chantier devis) ; chemin virement ne crée pas d'AcceptationCgv (process BdC) ; import PRIX_ANNUEL mort dans admin.service (purge opportuniste). RESTE ABO : L4 (destinataire org FactureLiavo) → L5 (front page abo) → L6 (NOT NULL). Détail : SESSION_STATE 18/08 (3).
> **Dernière mise à jour : 18/08/2026 (2) — CHANTIER PDF FACTURELIAVO CLOS, RECETTÉ 4/4 EN PROD.** Commits `18ef1da` (migration snapshot destinataire+date_echeance sur factures_liavo, 4 fixes rendu FacturePDF — type FACTURE, conditionsTitre, « Soldé ✓ »→« Soldé » —, emettre() persiste le snapshot, regenererPdf row-only + route POST /admin/factures-liavo/:id/regenerer-pdf) + `1699d85` (fix libellé période « du X au Y » dans facturerCentrePeriode — écart CC rattrapé : le fix était dans le prompt, sauté, non détecté par la relecture par-fichier). FL-2026-001 corrigée par 2 backfills SQL (snapshot destinataire Ville de Neuilly-Plaisance + description « du…au ») puis régénérée → PDF 4/4 vert = LA version à déposer (Chorus Pro / mairie). Learnings : relecture CC = cocher chaque POINT du prompt, pas chaque fichier (3e récap faux en 2 jours) ; uploadBuffer préfixe un UUID → régénérer crée un NOUVEL objet OVH (pas d'écrasement — affirmation Claude corrigée), orphelins inertes ; regenerer lit la row → les défauts de contenu des pièces émises se corrigent en SQL, pas par le code. Backlog né : téléchargement admin PDF FL impossible (folder S3 privé, brancher generateSignedUrl existant). Prochain : L2d. Détail : SESSION_STATE 18/08 (2).
> **Dernière mise à jour : 18/08/2026 — F2 DÉPLOYÉ COMPLET + RECETTÉ PROD.** Incident GitHub résolu ; manual-deploy backend 06:47 UTC success → 2 apps sur e300dcd (endpoint pendantes + bannière + garde devis 33be375 en prod). Recette verte : bannière « Chalet Le Sauvageon vous invite sur « Test » » affichée au dashboard enseignant-test. Chantier F2 CLOS. Bannière CROC KIDS active chez David (validité devis → relance Choucas jeudi 20/08). Prochain arbitrage : PDF FactureLiavo + regenerer-pdf vs reprise L2d. Détail : SESSION_STATE 18/08.
> **Dernière mise à jour : 17/08/2026 (5) — F2 FUNNEL ORGANISATEUR (invitations pendantes + bannière dashboard) CODÉ, RELU, COMMITÉ, POUSSÉ (`e300dcd` backend + `2625755` frontend) — ⚠️ DÉPLOIEMENT PARTIEL sous incident GitHub majeur du 17/08 : FRONTEND en prod sur `e300dcd` (manual-deploy success 15:10 UTC), BACKEND resté sur `4727456` (endpoint pendantes + garde devis 33be375 PAS en prod ; bannière déployée mais invisible — 404 sur /pendantes → fail-silent, sans risque).** DESIGN acté en session à froid (SQL préalable + census CC Phase 1 5/5 conforme + 2 lectures Claude hors census : pas de relation Prisma Invitation→Sejour → filtre en code sur 2e requête ; anti-spam `inviterOrganisateur` garantit déjà l'unicité pendante par séjour → zéro dédup applicative) : endpoint pull `GET /invitation-collaboration/pendantes` (ORGANISATEUR, email rechargé base, insensible casse — 0 doublon casse vérifié prod —, dateFin≥aujourd'hui, séjour lié encore rattachable sinon exclu, select étroit sans devisDraftJson), déclaré AVANT `@Get(':token')` (piège d'ordre Nest) ; garde `deletedAt` posée dans la branche rattachement d'`accepter()` (soft-deleted → fallback DRAFT, ferme le trou latent D3) ; bannière client montée dans le layout organisateur (couvre TOUT l'espace, y compris le chemin catalogue de Jocelyne), fail-silent intégral, dismiss par token en session, bouton → `/rejoindre/{token}` — JAMAIS d'acceptation depuis la bannière. Périmètre : InvitationCollaboration seule (autres familles équipées ou sans dashboard) ; limites assumées : alias email non couverts, pas de flux « décliner » (backlog REFUSEE), rattachement muet = F2bis. DÉCOUVERTE CROC KIDS (SQL) : pattern multi-semaines réel — semaine 1 (20-26/02/2027, devis 26 056,80 € EN_ATTENTE du 26/06) PENDANTE, invisible pour David depuis 2 mois = le cas nominal que F2 débloque ; sa bannière s'allume au déploiement, accepter auto-SELECTIONNE → confirmer la validité du devis auprès d'Anne/Nora (greffé à la relance de jeudi 20/08). INCIDENT INFRA : webhook push sauté + build-error 429/504 au fetch source ; manual-deploy 15:10 UTC = frontend SUCCESS / backend échec ×2, githubstatus = Major Outage non résolu (raw content ~50% erreurs) ; décision : stop relances, prod stable, reprise demain (manual-deploy BACKEND SEUL → deployments success → recette 1 login enseignant-test). ⚠️ Correctif de statut : la garde 33be375 n'est PAS en prod — l'envoi Sauvageon 16:27 a exercé l'ancien code, recette de non-régression toujours due. Learning : `scalingo deployments` post-push obligatoire — 2e famille de panne attrapée (webhook GitHub saute en silence sous incident). RESTE CHANTIER ORGANISATEUR : déploiement+recette F2 → arbitrage signalétique vs refonte dashboard ; F2bis (notification rattachement) ; F3 (gardes anti-doublon 2 côtés, dont sémantique accepter() sur séjour déjà rattaché = fallback créateur de doublon à requestionner). Détail : SESSION_STATE 17/08 (5).
> **Dernière mise à jour : 17/08/2026 (4) — GARDE `envoyerDevisDirect` LIVRÉE, POUSSÉE (commit `33be375`, 1 fichier ~10 lignes, gates verts).** Le renvoi d'un devis non-EN_ATTENTE (signé/facturé) throw désormais ForbiddenException au lieu de FORCER le retour EN_ATTENTE — l'ancien comportement annulait silencieusement la signature du client (champs signature conservés mais statut menteur, séjour resté CONVENTION, occupations non syncées = incohérence dormante). Ferme la course UI-périmée (le client signe sur la page publique pendant que l'onglet hébergeur affiche encore « Envoyer » — même famille que B1 chambres) + le trou API direct + tout futur bouton « Renvoyer ». CENSUS : unique appelant UI = TabDevisFacturation (bouton déjà gaté `statut === 'EN_ATTENTE'` — DevisCard/modifier/nouveau n'appellent pas l'envoi) → zéro changement de comportement atteignable, le renvoi légitime d'un devis EN_ATTENTE (client a perdu l'email) intact ; unique appelant service = controller ; les 3 chemins publics (signerDevisDirect/envoyerADirection/uploadSignaturePublic) avaient déjà leur garde de statut — c'était LA dernière transition non gardée du flux DIRECT. Message d'erreur pointe le lien public (lecture seule depuis fix 31/07), affiché proprement par extractApiError dans la modale en cas de course. Options écartées : param forceResignature (YAGNI, sémantique re-signature = vrai chantier), UI-gate seul (backend = autorité). Assumé : pas de spec (harnais DevisService inexistant), pas de recette live (garde négative, happy path exercé en continu par le Sauvageon). Flux futur « re-signature après modification d'un devis signé » = action dédiée explicite à concevoir si le besoin émerge, PAS un effet de bord de l'envoi. Détail : SESSION_STATE 17/08 (4).
> **Dernière mise à jour : 17/08/2026 (3) — F1bis LIVRÉ, POUSSÉ, DÉPLOYÉ, RECETTÉ PROD (commits `757f8dd` fix(email) 18 fichiers +194/−4 + `4727456` fix(funnel) 2 fichiers +6/−4) : fin du double CTA « Accéder à la plateforme »→/login collé EN DUR sur tous les emails sendGenericNotification — la porte de fuite du funnel DANS l'email d'invitation est fermée.** HELPER : 6e param `button?: { text; url } | null` (undefined = défaut historique /login compat totale, null = aucun bouton, objet = custom futur), emailLayout/send/sendFactureParEmail INTOUCHÉS, JSDoc sémantique. MIGRATION : 43 sites → null (42 famille A corps porteur d'un <a> — dont les 12 fuites destinataire-sans-compte+lien-tokenisé, invitation-collaboration:147/reset password/registers invités/ordre de mission — + 1 famille C confirmation client direct) ; 11 famille B conservés (corps sans lien, destinataire à compte, /login légitime) ; notifyAdminNewAccount interne intouché ; 3 reclassements par règle objective mailto=<a> (hebergement:246, claim:664, admin:1264) ; effet de bord assumé : branche « devis pré-rempli » d'invitation-collaboration accepter() sans aucun CTA (cosmétique). Spec NEUVE email.service.spec.ts (spy send privée, 3 tests undefined/null/custom, zéro dépendance BREVO). Corps/sujets byte-identiques partout. FRONTEND : verify-email gate `startsWith('/rejoindre/')` (bouton « Se connecter » simple post-F1, /invitation-equipe intouché, purge cookie inconditionnelle) + écran already_accepted de /rejoindre reformulé en ton succès (« ce séjour est déjà dans votre tableau de bord » + bouton accent). RELECTURE Claude sur code réel : helper+spec+3 sites invitation-collab+2 pages frontend intégralement, one-liners par échantillon + tsc filet d'arité (null égaré en 4/5e = type error). RECETTE PROD 2 points VERTS : reset password mono-bouton (prouve backend+famille A sur site critique) + écran rejoindre reformulé (prouve frontend), zéro donnée créée ; gate verify NON exercé en live (assumé : 4 lignes relues, recette au 1er invité réel). BACKLOGS NÉS : 🔴 garde manquante renvoi devis engagé (`envoyerDevisDirect` remet EN_ATTENTE un devis signé si renvoyé) ; route legacy `/dashboard/teacher/hebergements` (hebergement:235) ; branche devis pré-rempli sans CTA. RESTE CHANTIER ORGANISATEUR : F2 (invitations pendantes match email + bannière dashboard, JAMAIS d'auto-acceptation) → arbitrage signalétique vs refonte dashboard ; F3 séparé. Détail : SESSION_STATE 17/08 (3).
> **Dernière mise à jour : 17/08/2026 (2) — F1 FUNNEL ORGANISATEUR LIVRÉ, POUSSÉ, DÉPLOYÉ, RECETTÉ PROD (commits `97d3f06`/`bfad942`/`83b7dec`, push `db8ca8b..83b7dec`) : portage SERVEUR du token d'invitation collaborative — le cookie cross-device n'est plus le seul vecteur, cause racine des 3 incidents funnel (31/07, 13/08, Jocelyne 17/08) FERMÉE avant la rentrée.** DESIGN (census Phase 1 CC GO 7/7, câblage modules vérifié sans cycle par Claude) : champ `User.invitationCollabTokenPending` (miroir `accompagnateurTokenPending`, migration `20260817120000` ALTER nullable sans backfill) ; DTO `invitationCollabToken` `@IsOptional @IsUUID` (whitelist → champ décoré obligatoire, learning 24/07) ; `registerOrganisateur` stocke ; helper privé AUTO-PORTEUR `consommerInvitationCollabPending(userId)` (propre select, ne throw jamais) appelé par `verifyEmail` (APRÈS bloc accompagnateur intact) + **FILET REJOUABLE au `login`** (gaté ORGANISATEUR) ; sémantique échec : NotFound/Conflict = terminal → purge, transitoire → conservé pour le filet ; `accepter()` élargi `Pick<JwtUser,'id'>` (zéro cascade, typage structurel) ; `AuthModule` importe `InvitationCollaborationModule` (déjà exporté). Frontend : le payload register porte le token (variable déjà extraite pour le pré-remplissage, jamais envoyée avant) + bandeau bleu écran succès ; cookie `liavo_post_verify_redirect` conservé en bretelle. Specs 363→367 (mock InvitationCollaborationService + 7 tests). **RECETTE PROD VERTE** (compte test permanent `trochenrc@gmail.com`, séjour « test F1 cross service » soft-deleted après) : bandeau ✔ ; consommation SERVEUR prouvée AVANT toute visite /rejoindre (écran « déjà acceptée » SANS clic humain = le mécanisme indépendant du cookie qui aurait sauvé Jocelyne) ; SQL : pending NULL + email_verifie t, invitation accepted_at posé, séjour COLLABORATIF/CONVENTION créateur = user test. Requête sentinelle croisière : token pending persistant APRÈS login du porteur = échec répété du filet. **2 DÉCOUVERTES RECETTE → F1bis OUVERT (census CC TERMINÉ, 54 call-sites) : (a) 🔴 `sendGenericNotification` colle un CTA « Accéder à la plateforme »→/login EN DUR sous le bouton d'action de TOUS ses emails — 36 sites en double CTA dont 12 vers des destinataires SANS COMPTE avec lien tokenisé (invitation collaborative:147 en tête = porte de fuite du funnel DANS l'email d'invitation, hypothèse plausible du chemin Jocelyne ; aussi reset password, registers hébergeur/signataire invités, ordre de mission) + 3 sites où /login est absurde (bouton à supprimer) ; 12 sites sans lien restent légitimement /login ; fix cible = bouton paramétrable 6e position (compat positionnelle fromName/replyTo, 4 specs mocks insensibles) ; (b) fin de parcours nominale = écran « Invitation déjà acceptée » au ton d'anomalie + bouton verify « Se connecter et rejoindre le séjour » rendu obsolète par F1 → 2 retouches frontend.** Rattachement silencieux correct mais MUET (l'utilisateur découvre un séjour sans explication) = argument F2. **RESTE CHANTIER : F1bis (email CTA + 2 écrans) → F2 (endpoint invitations pendantes match email — alias non couverts assumé — + bannière dashboard « Le centre X vous invite », consentement-safe : JAMAIS d'auto-acceptation sans clic du lien, la branche rattachement auto-SELECTIONNE le devis) → arbitrage lot signalétique vs refonte dashboard organisateur. F3 (gardes anti-doublon 2 côtés) séparé.** Détail : SESSION_STATE 17/08 (2).
> **Dernière mise à jour : 17/08/2026 — INCIDENT DOUBLON NOTRE DAME DE LOURDES (Choucas) RÉSOLU EN PROD (SQL) + LEARNINGS FUNNEL — priorités chantier organisateur revues.** Contexte : 2 comptes organisateurs créés le weekend. Laure Grao (Montessori Versailles) : invitation Choucas acceptée en 2 min, chaîne register→verify→rejoindre complète OK. Jocelyne Jezequel (NDL Inzinzac) : invitation du 13/08 JAMAIS acceptée, compte créé hors lien le 17/08, dossier doublon auto-créé via catalogue en 4 min. État initial : 3 séjours pour 1 voyage réel — 2 jumeaux DIRECT/OPTION créés le 25/06 à 5 min d'écart (doublon de saisie Choucas, PAS le pattern multi-semaines : mêmes dates 17-22/01/2027 partout) + le sien (861f22d0, CONVENTION) ; devis 17 712,75 € (59aefa53, DEV-2026-0003) échoué sur le jumeau invisible 0485a614. Intervention SQL committée : `devis.demande_id=8b585008` + `devis.sejour_direct_id=861f22d0` (forme HYBRIDE canonique de la branche rattachement, éprouvée David/Laure) ; DELETE invitation pendante d5dbf5a2. Recette visuelle page signature publique VERTE (devis complet + 3 onglets signer/direction/upload). Reste HUMAIN Choucas : suppression des 2 jumeaux (bouton « Supprimer », header séjour) + renvoi du lien signature à la cliente — mail envoyé à Anne/Nora le 17/08, relance jeudi 20/08 si silence. LEARNINGS CODE (lectures complètes) : (1) `softDeleteSejour` fait un `devis.deleteMany` EN DUR sur les devis non engageants → toute migration DÉPLACE les devis AVANT suppression de séjour ; (2) `getDevisPublicByToken`/`signerDevisDirect` exigent `sejourDirectId` non nul → un devis collab pur n'a PAS de lien signature public, la forme hybride est la forme robuste ; (3) chaîne redirect invitation = cookie navigateur posé au register (`liavo_post_verify_redirect`) → casse cross-device/app mail, 3e manifestation du trou en 3 semaines (incident 31/07, notes 13/08) ; (4) `registerOrganisateur` ne crée Organisation+Membership que si établissement fourni (champ optionnel) → Laure ET Jocelyne SANS membership, dossier pédagogique + signature-direction dégradés ; (5) `creerDepuisCatalogue` pose CONVENTION sans devis ni engagement + demande sans `centreDestinataireId`. PRIORITÉS CHANTIER ORGANISATEUR REVUES : 1. fiabilité funnel (portage serveur du token — pattern `accompagnateurTokenPending` existant — + matching invitations pendantes à la connexion + garde anti-doublon des 2 côtés) ; 2. refonte dashboard/onboarding. Carte OPTION morte DÉPRIORISÉE : falsifiée comme bloqueur (David et Laure = CONVENTION, aucune victime réelle connue). Détail : SESSION_STATE 17/08.**
> 💰 **14/08/2026 — FL-2026-001 ÉMISE (345 € Choucas / Ville de Neuilly-Plaisance) — premier revenu facturé de LIAVO.** Endpoint + UI « facturation période explicite » livrés (`0fade6f` + `7d94757` + UI, expiration posée, L3c-conforme). Dépôt Chorus Pro tel quel (mentions légales complètes). **Backlog générateur AVANT toute prochaine FL- : type 'FACTURE', libellés ASCII sûrs, titre section conditions contextualisé, endpoint regenerer-pdf FactureLiavo.** Hors machine : n° engagement Chorus à demander + mot écart devis→facture à Anne. Détail : SESSION_STATE 14/08.
> **Dernière mise à jour : 13/08/2026 — L3c LIVRÉ, POUSSÉ, DÉPLOYÉ, RECETTE SQL VERTE (commits 11c1033/260f99c/e8b55fe/a0f23bf). Coupure des 10 writes miroir centre : l'organisation est l'unique porteur de l'état d'abonnement, colonnes centre GELÉES (divergence org↔centre = design désormais, retrait physique L6). Découverte census : le miroir trial.helper était un tampon de dédup de notif → notif « Centre ajouté à l'essai en cours » SUPPRIMÉE (décision anti-spam 13/08), héritage par lecture org. facturerCentre lit l'org + throw si org absente. register vérifié inoffensif côté front. Specs 364→363. Photo T0 SQL : 7 centres exploités alignés. En attente : logs cron J+1 (solde L3b). Fallbacks L3a intouchés → L6. RESTE ABO : L2d → L4 (destinataire FactureLiavo) → L5 (front page abo) → L6 (NOT NULL) ; prioritaire hors séquence : facture Choucas 345€ (Option 2 période explicite, census chaîne facturation AVANT tout code). Détail : SESSION_STATE 13/08.**
> **Dernière mise à jour : 10/08/2026 (4) — EXTRACTION `ReseauService` hors d'`AdminService` LIVRÉE, POUSSÉE, DÉPLOYÉE, RECETTÉE PROD (commit atomique `6d52be7`, 7 fichiers, gates verts tsc 0 / build / 364 tests). Hygiène préalable à L3c.** Le census L3c-0 a révélé qu'`admin.service.ts` mélangeait cœur admin LIAVO + volet réseau partenaire. **Découpage Option 2 (par APPELANT réel)** : les **4 méthodes appelées par un compte RESEAU** (`getReseauStats`/`getReseauDemandes`/`getReseauCentreDetail`/`inviterCentreReseau`) déménagent dans `backend/src/reseau/` (service+controller+module) ; les **5 outils actionnés par l'ADMIN seul** (`syncApidae`/`syncLmdj`/`backfillEducationNationale`/`bulkInviteApidae`/`updateCentreReseau`) + `EN_API` RESTENT dans `AdminService`. Déplacement VERBATIM (byte-identique, relu par Claude méthode par méthode sur code réel). Seul import qui déménage : `STATUTS_DEVIS_RETENUS`. Cascade bornée assumée : `AdminController` injecte `ReseauService` en plus (route `/admin/reseau/:reseau/stats`). 0 comportement observable changé, 0 migration, 0 frontend, 9 sites miroir abo + contenu L3c-0 intacts. **RECETTE VERTE** : onglet Réseaux admin (LMDJ 78/IDDJ 19) + dashboard `/dashboard/reseau` (compte RESEAU) chargent. `admin.service.ts` désormais propre pour L3c. Détail : SESSION_STATE 10/08 (4).**
> **Dernière mise à jour : 10/08/2026 (3) — L3c-0 (4 lectures admin centre→org) CODÉ, en attente relecture/push.** Grep exhaustif backend : les 4 lectures survivantes d'admin.service.ts (`getHebergeurs`/`getCentres`/`getReseauStats`/`getReseauCentreDetail`) étaient les SEULES restantes ; basculées sur `organisation.abonnementStatut`, fallback 'INACTIF', contrat front inchangé (clés plates, `organisation` ne fuit pas), maps introduits clé par clé. SQL prod : **0 centre sans `organisation_id`, toutes populations** → fallback théorique, L6 sans backfill. 9 sites d'écriture miroir confirmés, intouchés. **⚠️ EN ATTENTE POUR L3c : `facturerCentre` lit `centre.abonnementActifJusquAu` (admin.service.ts:1870, base de renouvellement écrite ensuite) — à basculer org EN TÊTE de L3c dans le même commit que la coupure de son write miroir (chemin facturation sensible, alignement Choucas 31/12).** Détail : SESSION_STATE 10/08 (3).
> **Dernière mise à jour : 10/08/2026 (2) — L3a-bis (admin) LIVRÉ, POUSSÉ, DÉPLOYÉ, RECETTÉ PROD (commits `e95d575`/`a83073b`/`0c9b154` + `b6fcebc` docs, push `64148c7..b6fcebc`, code pur 0 migration, boot Scalingo success).** Volet ADMIN du chantier abonnement, resté sur le centre. **(0)** `facturerCentre` écrit aussi l'org (miroir strict 5 champs en `$transaction`, `emettre()` intact, supplément = L2d). **(1)** `getAbonnements` + `santeClients` lus sur l'organisation (fallback org-null DECOUVERTE/INACTIF aligné getMesCentres, contrat front inchangé). **(2)** `getMetriquesAbonnements` réécrit : dédup par ORG, éligibilité payant = mandat Mollie OU virement, null-safe VIREMENT exclu des trials, MRR via `calculerMontantAbonnementCents` (supplément inclus, ANNUEL/12, euros), `totalCentres` inchangé, map locale supprimée. Diffs RELUS par Claude sur code réel, 2 pièges census attrapés (null-safe VIREMENT ; `_count` filtré anti-N+1), gates tsc 0 / build / 362. **RECETTE VERTE** : MRR 69€ (Choucas seul payant), aboPayes 1, trialActifs 2 / trialExpires 1 (**dédup prouvée** : par centre = 3 car Pôle Montagne 2 centres). **SQL diagnostic pré-déploiement** : Choucas + Pôle Montagne + PULSE tous alignés org↔centre → 0 remédiation. **Corrections factuelles** : Pôle Montagne = **2 centres** (pas 3, Nants détaché 13/07) ; Choucas = **PILOTAGE** (pas COMPLET) ; PULSE expiré 08/08. **Alignement Choucas** (SQL, hors code) : abo payant 1er août→31/12/2026 (5 mois ; juin+juillet = trial), `abonnement_actif_jusqua` passé de 17/10 (résidu trial) à 31/12 sur centre+org. RESTE ABO : L3b (cron) → L3c (coupure miroirs) → L2d (`facturerCentre` supplément + FK `acceptations_cgv` SET NULL) → L4 → L5 → L6 ; + recette rooming L3a en attente. Détail : SESSION_STATE 10/08 (2).**
> 💰 **FACTURE CHOUCAS 345€ — À FAIRE (première entrée d'argent réelle LIAVO, prioritaire).** Choucas veut UN paiement de 345€ (5 mois août→déc × 69€ PILOTAGE mensuel), puis annuel 690€ en janvier (= `PRIX_ANNUEL[PILOTAGE]`, 1 clic `facturerCentre(PILOTAGE,ANNUEL)`). `facturerCentre` ne fait que 1 mois OU 1 an par clic → incapable de produire 345€ en une facture. **Décision Théo = dev « Option 2 » (période explicite)** : côté admin, cocher les mois → montant = nb mois × prix + **expiration posée explicitement = fin de période** (pas base+N — le recalcul `max(exp,now)+durée` décaleraient Choucas base 31/12 → mai 2027) + facture « période 01/08→31/12, 5 mois ». ⚠️ Touche `emettre()` = zone conformité (numérotation, Factur-X, mentions, libellé période PDF), chantier facturation « non bouclé ». **Prochaine action = census de la chaîne facturation** (`facture-liavo.service.ts` + PDF + UI admin) avant de coder. Court terme si presse : facture hors plateforme (ne PAS faire 5 clics de 69€ → défait l'alignement 31/12). Détail : SESSION_STATE 10/08 (2).**
> **Dernière mise à jour : 10/08/2026 — FIX « CONDITIONS D'ANNULATION SOURCE UNIQUE » LIVRÉ, POUSSÉ, DÉPLOYÉ (commit `64148c7`, code pur, 0 migration).** Client mariage Perrin (Sauvageon, DEV-2026-0037) refusait de signer : le CONTRAT affichait « 50% du TTC retenu entre 9-6 mois » + acompte figé, alors que le DEVIS affichait sa phrase profil « acompte 30% non remboursable ». CAUSE STRUCTURELLE (pas de timing) : le contrat événement (`contrat-sauvageon.pdf.tsx`) avait son Article 4 hardcodé à 50% + deux « 30% » en dur, jamais branché sur `conditionsAnnulation` — le fix du 05/08 avait traité la convention SCOLAIRE mais explicitement PAS le contrat événement (dette notée à l'époque). La couverture générique autres hébergeurs n'avait AUCUNE section annulation. 3 DÉCISIONS Théo : (1) contrat mariage Article 4 lit `conditionsAnnulation` + acompte dynamique (fin des 30%/50% figés) ; (2) SOURCE HYBRIDE `devis.conditionsAnnulation ?? centre.conditionsAnnulation ?? fallback` (snapshot devis prioritaire, profil centre en relais, fallback = 3 puces historiques) appliquée au contrat ET à la convention ; (3) OPTION 3 autres hébergeurs = la couverture générique affiche une section « Conditions d'annulation » (du profil) SI le centre n'a pas chargé son propre PDF de convention, masquée sinon (flag `afficherConditionsAnnulation`). 4 fichiers (`contrat-sauvageon.pdf.tsx`, `convention-scolaire-sauvageon.pdf.tsx` [flag interface seul], `convention-couverture.pdf.tsx`, `devis.service.ts`), relus par Claude sur code réel diff par diff, gates tsc 0 / build. `buildContratEvenementPdf` + `buildConventionScolairePdf` servent aperçu ET envoi → fix unique remonte partout. ⚠️ POINT MÉTIER : sur l'événementiel Sauvageon, la phrase profil (30%) REMPLACE l'ancien Article 4 (50% retenu entre 9-6 mois) = ADOUCISSEMENT réel des pénalités mariage — voulu (source unique) mais si mariage et scolaire doivent diverger un jour → 2 champs distincts (on sort de « source unique »). RESTE : régénérer le contrat Perrin (renvoyer le devis → écrase le vieux PDF OVH à 50%) puis re-signature. Détail : SESSION_STATE 10/08.**
> **Dernière mise à jour : 07/08/2026 — CHANTIER « FACTURATION PAR ORGANISATION » : L3a EN PROD (`6885206`).** Bascule des LECTURES d'abonnement du CENTRE vers l'ORGANISATION sur 4 points (PlanGuard, rooming `assertPlanCentreComplet`, `demande.findOpen` accesComplet, `centre.getMesCentres`), via un helper commun pur `getPlanEffectif` (fin de la triple copie du calcul de plan effectif). Ferme un BUG LATENT : le gate rooming lisait le miroir du centre du séjour, potentiellement périmé pour une org multi-centre (souscrire ne mirrore que le centre actif) → désormais lu sur l'org. Fallbacks org-null : PlanGuard fail-open, rooming + demande fail-closed (RGPD), getMesCentres INACTIF/DECOUVERTE ; contrôle prod = 0 centre exploité sans org → aucun accès mordu. Code pur (aucune migration), 8 fichiers, relu diff par diff sur code réel, gates tsc 0 / build / 362 tests, build+boot Scalingo success. **AMENDEMENTS DE DÉCISION actés (chantier abonnement) : (1) « une organisation = un essai » REMPLACE « un compte = un essai » (14/07) — le trial est porté par l'organisation, amorcé au login, aligné en multi-société par boucle sur les orgs distinctes ; (2) « invitation admin ⇒ claimStatut VALIDE » — une invitation admin/réseau vaut validation humaine et court-circuite le tunnel justificatif/Kbis (`centre.service.register()`, L2e-2).** Chantier abonnement : L1+L2b+L2a+L2e-1+L2e-2+L3a en prod ; RESTE L3a-bis (admin MRR : bascule org + correction du MRR faux qui somme par centre et ignore le supplément +39€/centre) → L3b (cron sur l'org + tampon `dernierEmailAlerteAt`) → L3c (coupure des writes miroirs centre) → L2d (`facturerCentre` + fix RESTRICT `refuserHebergeur`) → L4 (destinataire `FactureLiavo`) → L5 (frontend page abo) → L6 (`organisation_id NOT NULL`). Détail complet : SESSION_STATE 07/08.**
> **Dernière mise à jour : 05/08/2026 (3) — DEMANDE CIBLÉE → PRÉ-RÉSERVATION OPTION À L'ENVOI DU DEVIS livrée, poussée, déployée, vérifiée prod (commit `cc7945c`, backend).** PROBLÈME : sur une demande DIRECTE ciblée (organisateur vise UN hébergeur via `centreDestinataireId`), le séjour restait `SUBMITTED` non-rattaché même après l'émission du devis → `verifyAccess` renvoyait 403 (« impossible de charger » — symptôme Alticlub) + séjour absent du planning. FIX 3 volets (RELUS par Claude sur code réel, gates tsc 0 / build / 327 tests) : **(1)** `devis.service.ts › create` (COLLAB) — création devis + lignes + bascule séjour dans un `$transaction` (bonus : fin des devis orphelins si `createMany` échoue) ; à l'envoi, si `demande.centreDestinataireId === centre.id` → `sejour.updateMany({where: SUBMITTED + hebergementSelectionneId null, data: OPTION + rattaché})` (filtre idempotent, broadcast non concerné). PAS de `syncOccupationsSejour` (no-op PROUVÉ : cible occupation dérive des devis RETENUS, devis émis EN_ATTENTE = OPTION inchangée, aucune occupation possible avant rattachement car `createOccupations` exige `hebergementSelectionneId===centreId`). **(2)** `collaboration.service.ts › verifyAccess` — `isCreateur` remonté avant le contrôle de statut ; `statutsAutorises = (isHebergeur || isCreateur) ? STATUTS_SEJOUR_DIRECT : STATUTS_SEJOUR_COLLABORATIFS` → l'hébergeur rattaché ET l'organisateur créateur accèdent dès OPTION, quel que soit le mode ; SUBMITTED reste fermé à tous ; accompagnateurs/signataires inchangés ; données privées hébergeur toujours cloisonnées par `verifyHebergeur`. **(3)** migration `20260805193821_backfill_demande_directe_option` — rattrapage borné par `EXISTS(devis ≠ NON_RETENU du centre destinataire)` + SUBMITTED + non-rattaché + non-deleted. ANNULATION : rien à écrire (règle `annulerDevis` existante rétrograde en OPTION). RECETTE PROD (déploiement success 17:45:33) : `ead08238…` → OPTION + `hebergement_selectionne_id = centre_destinataire_id = 4e7757ae…` ✅ ; `b8272511…` (demande ciblée SANS devis = contrôle négatif) → reste SUBMITTED/NULL ✅ (backfill = 1 ligne, conforme). ⚠️ À CONFIRMER CÔTÉ USAGE : Alticlub ouvre ead08238 → plus de 403, séjour au planning, espace collab ouvert aux deux. NON traité (orthogonal, non bloquant) : `appelOffreStatut` gardé + demande reste `OUVERTE` (invisible : ciblée → seul le centre destinataire la voit, déjà répondu) ; séjour orphelin `936eebca…` (SUBMITTED sans demande) toujours hors scope. Ferme le VRAI FIX noté 31/07 (3). Détail : SESSION_STATE 05/08 (3).**
> **Dernière mise à jour : 05/08/2026 (2) — VACANCES SCOLAIRES 2027-2028 ajoutées au référentiel (frontend, POUSSÉ). `frontend/src/data/calendrier-france.ts` : 10 entrées 2027-2028 dans `VACANCES_SCOLAIRES` (Toussaint 23/10→08/11, Noël 18/12→03/01, Hiver B/C/A, Printemps B/C/A, Ascension 24→29/05, Été 05/07) + `VACANCES_DATA_FIN` '2027-09-01'→'2028-09-01'. Dates officielles = arrêté du 21/07/2026 (JO 23/07/2026), vérifiées sur source par Claude (pas inventées). ⚠️ Rotation zones 2027-2028 = B→C→A (inversée vs 2026-2027 C→A→B) ; Été debut 05/07 (fin des cours mardi 04/07, cas rare) ; Printemps C/A reprise un mardi (lundis 1er/8 mai fériés). Jours fériés = calculés algorithmiquement (getJoursFeries, jamais périmés). `isCalendrierPerime` se re-déclenchera ~mars 2028. Diff RELU par Claude sur code réel (intervalles d'années disjoints → pas de télescopage 2026↔2028 ; recouvrement inter-zones = voulu, fusion 'B+C'), gates tsc 0 / build. Reste cosmétique NON fait : commentaire d'en-tête du fichier dit encore '2025-2027'. Détail SESSION_STATE 05/08 (2).**
> **Dernière mise à jour : 05/08/2026 — FIX CONVENTION « dé-Sauvageon-iser » LIVRÉ + POUSSÉ + DÉPLOYÉ (commits `a0a5564` + `761da32`, backend). BUG (remonté par Yves Massard, Pôle Montagne YAKA/Florimont) : la convention scolaire (`buildConventionScolairePdf`, `devis.service.ts`) retombait sur un template ENTIÈREMENT hardcodé Sauvageon (`convention-scolaire-sauvageon.pdf.tsx`, « Maëva Roche-Loison » + conditions Sauvageon en dur) pour TOUT centre sans `conventionPdfUrl`. SQL prod : `conventionPdfUrl` NULL sur les 136 centres (Sauvageon compris) → 100 % des conventions sortaient du template Sauvageon. Brochure NON affectée (par centre, erreur si absente, vérifié code réel). Contrat événement réservé Sauvageon (email hardcodé) — HORS SCOPE, intact. DÉCISION Théo : le Sauvageon GARDE son template généré via un flag ; les autres centres sans PDF → couverture générique LIAVO seule (à leur nom). COMMIT `a0a5564` : flag `conventionTemplateLegacy Boolean @default(false) @map(convention_template_legacy)` sur CentreHebergement + migration `20260805100954` (ALTER + UPDATE … WHERE `email='resa@lesauvageon.com'`) + 3 branches (`if conventionPdfUrl` couverture+fusion / `else if conventionTemplateLegacy` template Sauvageon / `else` couverture seule) + fetch user `representant` remonté avant le branching. COMMIT `761da32` : article 4 lit `devis.conditionsAnnulation` (split `\n` → 1 `Text`/ligne) avec fallback EXACT sur les 3 puces historiques si vide ; `conditionsAnnulation?: string|null` ajouté à `ConventionScolaireData` + `baseData`. RELU par Claude sur code réel (3 branches, `@map`=colonne migration, fallback article 4, migration SQL). Gates tsc 0 / build. VÉRIFIÉ PROD : déploiement passé, `convention_template_legacy=true` UNIQUEMENT sur le Sauvageon, les 135 autres (dont Pôle Montagne) à false → couverture LIAVO seule. Reste : vérif fonctionnelle (preview convention Pôle Montagne vs Sauvageon). Dette « phase 1 mono-centre » restante : contrat événement encore réservé Sauvageon (email + BIC/banque en dur) à généraliser plus tard. Détail : SESSION_STATE 05/08 (1).**
> **Dernière mise à jour : 31/07/2026 (4) — FIX SPAM relance hébergeur LIVRÉ (commit `0d2368b`, backend, non poussé à l'écriture). Le cron `relancerHerbergeurDevisIgnore` (`notifications.service.ts`, 9h) relançait l'hébergeur pour tout devis `EN_ATTENTE` > 30j SANS tampon → mail QUOTIDIEN (Alticlub spammé). Fix : champ dédié `relanceHebergeurAt` (`@map relance_hebergeur_at`, distinct de `relanceEnvoyeeAt`/cron 2) + migration `20260731140000` (ALTER + backfill `NOW()` sur `EN_ATTENTE AND created_at <= NOW()-30j` → neutralise le stock, zéro rafale au boot) + garde WHERE `OR[null, lte 30j]` + update dans le `try` après envoi. Comportement : 1re relance J+30 puis 1×/30j max. Diff + migration + `@map` RELUS par Claude sur code réel (4 verrous tenus), gates tsc 0 / build / test 327. Migration auto via `prisma migrate deploy` au push (Procfile) — Alticlub silencé dès le boot. Recette post-deploy : SQL `relance_hebergeur_at` posé + logs 9h J+1. Point vérifié : annuler un devis = `NON_RETENU` → sort du filtre `EN_ATTENTE` des crons → stoppe TOUTES les relances (pas de soft-delete Devis). Chantier PRÉFÉRENCES NOTIFICATIONS (écran opt-out par centre/type) reste à faire — cadré : 3 crons + transactionnels dispersés (~15 emails), archi centralisée recommandée, décisions à trancher (catégories, non-désactivables abo/facturation, par centre vs user). Détail : SESSION_STATE 31/07 (4).**
> **Dernière mise à jour : 31/07/2026 (3) — INCIDENT ALTICLUB (accès dossier) diagnostiqué, répondu par message, VRAI FIX noté pour plus tard. Cause : ni abo (COMPLET actif jusqu'au 10/09) ni corruption — son séjour vient d'une demande DIRECTE organisateur→hébergeur, statut `SUBMITTED`, SANS centre rattaché (`hebergementSelectionneId` NULL) → `verifyAccess` refuse (403) tant que le devis n'est pas signé → « impossible de charger ». ⚠️ FIX UI « griser le bouton Ouvrir le dossier » ÉCARTÉ (Théo) : il traiterait à tort une demande directe comme un appel d'offres perdu. VRAI FIX À FAIRE À FROID (backend) : sur une demande DIRECTE (un seul hébergeur, jamais de concurrence — modèle confirmé Théo : un orga qui sollicite 2 centres = 2 demandes/séjours SÉPARÉS), le séjour doit être rattaché au centre ET accessible à l'hébergeur DÈS la demande, sans attendre la signature. Deux verrous à lever ENSEMBLE (`verifyAccess` teste rattachement + statut) : (1) poser `hebergementSelectionneId` à la création de la demande directe ; (2) rendre le statut initial accessible à l'hébergeur (ajouter SUBMITTED aux statuts hébergeur, OU faire naître le séjour dans un statut déjà accessible). Prérequis lecture : flux de création d'une demande directe + enum `StatutSejour` (SUBMITTED/DRAFT = statuts ANGLAIS isolés au milieu de statuts français, absents de toutes les listes d'accès → probable vestige d'un ancien flux ; 3 SUBMITTED + 1 DRAFT vivants en base au 31/07). NB : ouvrir l'accès aide l'hébergeur à préparer son dossier mais NE fait PAS signer le client — le déblocage commercial (relancer l'organisateur) est un sujet distinct. EN COURS ce jour (2e demande Alticlub) : chantier PRÉFÉRENCES DE NOTIFICATIONS email par hébergeur (Alticlub veut couper les relances devis en attente ; Sauvageon veut les garder → opt-out PAR centre, PAS global) — écran paramètres hébergeur avec toggle oui/non par type de notif.**
> **Dernière mise à jour : 31/07/2026 (2) — Chantier 1 « accès devis après signature » CODÉ (1 fichier `frontend/app/devis/signer/[token]/page.tsx`, frontend pur, option A : bandeau « ✓ signé » + vue lecture seule, carton félicitation + bouton « Voir mon devis »). Diff RELU par Claude sur code réel (8 points tenus, zéro fuite, sentDirection intouché) + gates verts (tsc 0 / build 61/61). POUSSÉ + DÉPLOYÉ + RECETTÉ EN PROD (confirmé Théo) — devis signé prod = lecture seule + bandeau, fin du cul-de-sac. **Chantier 1 CLOS.** Détail : SESSION_STATE 31/07 (2).**
> **Dernière mise à jour : 31/07/2026 — Chantier 1 « accès devis après signature » cadré (frontend pur, option A, prêt à coder). B5 recadré = entrée mode B2C direct à concevoir dans le cadre PMS (plus un patch email). 2a noté (devis DIRECT signé visible dans l'espace du signataire à compte — anticipe Mme Noire). Nouveau bloc conception « ESPACE ORGANISATEUR » (2b + dashboard peu lisible + escalade organisateur→hébergeur→Théo = bloqueur adoption Tereva). Priorité : 1 → conception (espace orga / B5-PMS / 2a) → B4. Détail : SESSION_STATE 31/07.**
> **Dernière mise à jour : 29/07/2026 (4) — MODULE CHAMBRES/GROUPES — TRIPTYQUE « EN PROPRE » COMPLET : IDOR groupes + remplissage groupes en propre FERMÉS/LIVRÉS, RECETTÉS PROD.** Suite de l'audit (3), 2 items traités. Audit croisé rooming↔groupes → verdict **frontend-only** (backend groupes DÉJÀ ouvert via `verifyAccess` branche DIRECT+hébergeur, contrairement au rooming à garde répliquée). **IDOR groupes** `e92af5b` (🔴 pré-existant : `affecterEleve`/`retirerEleve` ne vérifiaient pas l'appartenance groupe/élève au séjour → cross-séjour ; fix = 2 checks patron updateGroupe, APPLICATIF PUR sans migration, npm test 327 inchangé). **Remplissage groupes en propre** `af27949` (frontend-only : `peutEditer` + `colonneNonAffectesVisible` dans TabGroupes, non-régression COLLAB préservée, libellé suppression étendu « chambre ou un groupe »). Diffs relus par Claude sur code réel, gates verts, recette verte TEST CLEM (drag SANS clôture + non-régression orga). **⭐ TRIPTYQUE COMPLET : l'hébergeur-propre SAISIT + ROOME + GROUPE de bout en bout.** RESTE = B4+S1 (équipe de centre, prioritaire), B5 (email parent inerte : fourche ENTIÈRE — le GROUPAGE est fait, PAS l'email), B3, S2-S4, code mort M1-M7, cosmétiques. Détail : SESSION_STATE 29/07 (4).
> **Dernière mise à jour : 29/07/2026 (3) — MODULE CHAMBRES — AUDIT CC (lecture seule) + relecture Claude → 2 TROUS DU PÉRIMÈTRE ACTUEL FERMÉS, RECETTÉS PROD.** Audit exhaustif post-3c du module chambres/rooming/gestion-en-propre (5 bugs B1-B5, 4 sécu S1-S4, 7 code mort M1-M7). Arbitrage Théo : fermer l'existant d'abord, noter le reste. **B1 🔴** (régression ROUVERTE par 3c : les 2 handlers d'affectation de TabChambres ne rechargeaient que `loadRooming()` → grille périmée `nbAffectations=0` → confirmation « Retirer » sautée → cascade FK silencieuse ; vérifiée par Claude sur code réel) `380c361` = `loadGrille()` après rooming dans les 2 handlers + commentaire A3 corrigé. **B2 🟠** (suppression participant en saisie directe sans confirmation) `bdfa0c6` = `window.confirm` au save (pas au ✕ réversible). Diffs relus par Claude sur code réel, tsc 0 + build 61/61, recette PROD verte TEST CLEM (B1 popup compte juste, B2 cascade + contre-test Annuler). **RESTE = backlog audit consolidé (B4+S1 équipe de centre à trancher avant Tereva / B5 autorisations propre / B3 ordre étages orga / code mort M1-M7 dont Lot 5 / sécu S2-S4 / cosmétiques) — bloc CHANTIER GESTION EN PROPRE ci-dessous, RIEN LANCÉ.** Détail : SESSION_STATE 29/07 (3).
> **Dernière mise à jour : 29/07/2026 (2) — MODULE CHAMBRES — 3c LIVRÉ, POUSSÉ, DÉPLOYÉ, RECETTÉ. Chantier « gestion en propre » CLOS.** L'hébergeur d'un séjour DIRECT gère ses chambres de bout en bout : répartition groupée par étage (vue principale) + « Gérer les chambres » dépliable. 3 commits `03dbcca`/`86fb6e8`/`ee5b813`, recette verte (non-régression orga Maëva + TEST CLEM). Aiguillage via nouveau helper `peutGererEnPropre`. Layout COLLAB inchangé. **RESTE = 3 différés post-3c à prioriser (unification vue hébergeur COLLAB↔DIRECT / PDF liste-vs-planche / lisibilité bouton « Gérer les chambres ») — voir bloc CHANTIER GESTION EN PROPRE ci-dessous.** Détail : SESSION_STATE 29/07 (2).
> **Dernière mise à jour : 29/07/2026 — MODULE CHAMBRES — CHANTIER « GESTION EN PROPRE » (hébergeur = organisateur d'un séjour DIRECT) : socle backend + saisie participants + extraction RoomingEditor LIVRÉS, POUSSÉS, DÉPLOYÉS, RECETTÉS.** Revirement de cible chambres : la « Phase 4 occupants dans les cartes » était une surinterprétation — vraie cible = l'hébergeur qui organise SON séjour DIRECT en propre doit attribuer ses chambres ET roomer ses participants lui-même (Louise/PULSE, Théo/Sauvageon direct). **PIVOT SÉCURITÉ : `createurId` est NULL sur tous les DIRECT** (posé en dur) → tout gate `createurId===userId` cassait la saisie + le rooming pour l'hébergeur-créateur. Fix = **Option A** (nouveau discriminant `peutGererEnPropre = DIRECT && hebergementSelectionne.userId===userId`, helper `common/sejour-ownership.ts`, SANS toucher createurId) — **Option B (backfill createurId) REJETÉE après audit CC** (réveillerait doublon bascule DIRECT→COLLAB + 6 glissements « créateur=orga »). Commits recettés en prod : 1a `c793ab9` / 1b `8b3a762` / 3a `a0a5b6a` / A1a `0f7efb3` (+ A1b, A2, 3b) — @Roles élargis ORGANISATEUR→(ORGANISATEUR,HEBERGEUR) + gardes service via peutGererEnPropre + `RoomingEditor` extrait (présentationnel, drag interne, `groupParEtage?` non branché, comportement orga inchangé). Recettes vertes : saisie participant hébergeur (TEST CLEM), test API affectation `201`, non-régression drag orga (compte Maëva/COLLAB TEST). **PROCHAIN = 3c** (monter RoomingEditor côté hébergeur : `groupParEtage=true` sert de plan, SANS clôture = rooming direct, Attribution en accès secondaire, aiguillage page.tsx sur discriminant DIRECT+hébergeur). **Points différés → bloc dédié dans la section MODULE CHAMBRES ci-dessous.** Banc de test durable : compte orga Maëva + TEST CLEM. Détail : SESSION_STATE 29/07.
> **Dernière mise à jour : 24/07/2026 (2) — LOUISE/PULSE = 1er compte MULTI-CENTRE réel + 2 chantiers PRIORITAIRES.** Louise a créé son 2e centre (Chamberet) en autonomie → vérifié en base : même orga, même plan PILOTAGE/trial, tous 2 ACTIVE (pas de doublon, elle travaille sans blocage). Vue consolidée multi-centre (`/dashboard/hebergeur/global`) vérifiée SOLIDE. Mais 2 manques révélés → **section prioritaire dédiée ci-dessous** : (1) ONBOARDING (notamment par-centre : Chamberet = fiche nue, pas guidée) ; (2) SUIVI SANTÉ CLIENT (admin classe Louise « aucune activité » alors qu'elle a créé un centre le matin même → métrique aveugle au setup, risque de dé-prioriser un lead chaud). Détail : SESSION_STATE 24/07 (2). Backlog technique : plan sidebar dérivé de `cs[0]` (sans effet chez Louise, plans identiques). Angle mort facturation multi-centre = à tester avant qu'elle/Tereva passe payant.
> **Dernière mise à jour : 24/07/2026** — **MODULE CHAMBRES — LOT COUTURE (fixes des 2 diagnostics d'audit) LIVRÉ, POUSSÉ, DÉPLOYÉ.** 2 audits croisés post-SC7 : archi jugée SAINE (modèle 3 couches, cascades, invariants OK), problèmes UNIQUEMENT aux coutures (hébergeur/orga, plan payé/consommé, catégorie/étiquette, stock/personnes) → frontend + exposition de données. Chaque point vérifié sur le code réel. 3 commits, review Claude sur diffs. **`e2fd475`** : rooming lisible par l'HÉBERGEUR (lecture seule, `@Roles(ORGANISATEUR,HEBERGEUR)` sur GET seul + `resoudreAccesRooming` élargi ; **bonus DIRECT lisible**) + `getRooming` SOFT sur le plan (fin du message mensonger « votre hébergeur doit d'abord… ») + `nbAffectations` (GET grille, `_count` local, partagés intacts) + `statut` occupation. **`b5930f5`** : DTO validation saisie directe — ⚠️ ValidationPipe global `whitelist:true` SANS `forbidNonWhitelisted` = STRIP SILENCIEUX → `ParticipantDirectDto` EXHAUSTIF 14/14 champs, `@IsIn` sur `hebergementCategorie` (fin du 500 Postgres) + `BatchDirectDto` `@ValidateNested`, test sentinelle `toEqual`. **`9364af3`** : confirmation « Retirer » avec count (🔴 fermé, plus de désaffectation silencieuse) + dimensionnement places/personnes (3 états, pas d'alarme à 0 chambre) + pastille catégorie F/G/A au rooming. **2 🔴 fermés + 🟠 traités.** Backlogs assumés : drag masqué pour LECTURE, clôture réservée EDITION, rapprochement catégorie/étiquette V2, badge A_REPLACER orga, MaxLength à recouper, 3 duplications sécu. **LEARNING : `whitelist` sans `forbidNonWhitelisted` → DTO exhaustif obligatoire.** **PROCHAIN = LOT 4 PDF** (⭐ 1ʳᵉ action : lire l'outil PDF existant). Détail : SESSION_STATE 24/07.
> **Dernière mise à jour : 23/07/2026 (soir)** — **MODULE CHAMBRES — SOUS-CHANTIER 7 (rooming espace collab) LIVRÉ, POUSSÉ, DÉPLOYÉ (back+front `success`).** Geste ORGANISATEUR = glisser participants→chambres attribuées par l'hébergeur. 6 commits, review Claude sur diffs réels : pré-lot data `hebergementCategorie` (FILLE/GARCON/AUTRE+null, RGPD nommé par finalité) `4dcc596`+`ff2d5d9` ; compteur `getRoomingStats` `e031ca4`+`957288e` ; directives étiquettes palette NEUTRE (teal/amber/violet/slate) `76b08ed` ; affectation backend `e8a0437` (capacité DURE D7, XOR élève/encadrant, aucune condition signature D14, move sans double-compte) ; **fix accès `76672cc`** (`resoudreAccesRooming` — aligné sur `verifyAccess` des groupes : un accompagnateur-collaborateur édition peut chambrer comme il groupe) ; front `TabRooming` `4b632e2` (drag&drop HTML5 natif calqué TabGroupes, garde-fous chambres-vides + inscriptions-non-clôturées, sans X-Centre-Id) ; **fix cascade `22994ad`** ('chambres' ajouté à `ACCOMPAGNATEUR_TABS`, sinon le fix accès restait inopérant côté UI). Clôture = geste existant (`POST /collaboration/:sejourId/cloturer-inscriptions`, ONE-WAY). Back 314→318 verts, gates tsc+build à chaque commit. Recette ÉCRITURE reportée septembre (compte ORGANISATEUR requis, pas hébergeur). **PROCHAIN = LOT 4 : vue plan lisible + PDF** — plan PUR (« qui dort où », zéro donnée sensible = RGPD), **Option C** (vue écran imprimable, un seul build), **générique multi-hébergeurs** (rend `getRooming`, PAS de gabarit Sauvageon — correction : aucune feuille Excel de référence n'existe) ; **archi à trancher DEMAIN : LIRE l'outil PDF existant d'abord** (@react-pdf serveur vs `window.print()`+CSS print). Détail : SESSION_STATE 23/07 (soir).
> **Dernière mise à jour : 23/07/2026** — **MODULE CHAMBRES — #6 onglet « Chambres » page séjour LIVRÉ, POUSSÉ, RECETTE LECTURE PROD VERTE.** Frontend pur (4a consommé tel quel — zéro backend, zéro migration, zéro DDL). 2 commits : **`2e63f93`** `fix(api)` — l'interceptor `X-Centre-Id` (`frontend/src/lib/api.ts`) devient **non-destructif** : un header fourni explicitement par l'appelant prime sur `localStorage['liavo-centre-actif']`. Rétrocompatible (grep : seul `api.ts` posait le header) → resservira au #7 et à toute feature multi-centre sur la page séjour (un séjour cible `hebergementSelectionne.id`, pas le centre actif). **`00b4956`** `feat(chambres)` — onglet « Chambres » (`page.tsx` : `type Tab` + `TABS` après participants + 2 filtres → **HEBERGEUR seul, SEJOUR seul** masqué en ÉVÉNEMENT, invisible accompagnateur) + **`TabChambres.tsx`** : GET grille (dates du séjour tronquées + header centre du séjour explicite), POST batch `{sejourId, chambreIds}` (dates omises = défaut séjour), DELETE pour retirer, dérivation « attribuée à ce séjour » via `occupation.sejour?.id === sejourId && source==='SEJOUR'`, récap « N chambres · P places », badges 5 états (+×nbOptions), sous-texte du titre occupant sur les FERME d'un autre séjour **sans bloquer la sélection** (D12 : zéro logique de conflit côté front), 403 `PLAN_INSUFFICIENT` géré globalement. Gates tsc 0 + build à chaque commit. **Recette LECTURE prod verte** (séjour Sauvageon réel) : **26 chambres** rendues (étage 1 / étage 2 / RDC), badges « Libre », récap 0/0, bouton Attribuer grisé → confirme que `getSejourCollabInfo` peuple bien `hebergementSelectionne.id`. Onglet visible EN PROD → **déploiement frontend pris** (leçon HOSTNAME de facto satisfaite). ⚠️ **Recette ÉCRITURE (POST attribuer / DELETE retirer) NON faite** — reportée à septembre (usage réel) OU test séjour bidon Sauvageon + soft-delete (la cascade du Lot 5 nettoie les occupations, zéro résidu). **Backlog né (à traiter dans/avec le #7)** : (a) le badge d'une chambre **attribuée** affiche l'état **global** de la chambre (coche pleine + badge « Option »/« Occupée » côte à côte — cosmétique) → raffiner en statut de l'occupation *du séjour* ; (b) « Retirer » supprime la **1ʳᵉ** occupation SEJOUR (multi-occupation même chambre = **non atteignable via l'UI V1**) ; (c) « Retirer » **sans confirmation ni disabled** → OK en #6, mais **au #7 la cascade FK supprimera les affectations élèves** → confirmation requise. **Séquence** : #6 fait → **sous-chantier 7 (prochain)** = onglet Chambres espace collab (affectation participants→chambres, capacité bloquante D7/D14, **rooming PDF spatial D13** — remplace l'Excel) → hygiène Lot 5 (injection inutilisée `SejourService`) greffée au #7 → grille calendaire hébergeur → 4b Lots 3-4.
> **Dernière mise à jour : 22/07/2026** — **MODULE CHAMBRES — Lot 5 cascades séjour LIVRÉ, POUSSÉ (`32e0279..6d43f2e` = 3 commits, backend-only, zéro DDL).** Refactor à froid, review Claude croisée sur les diffs réels. **`cdfbb93`** `redaterOccupationsSejour` (règle A, actée 22/07) : seules les occupations `source='SEJOUR'` en **plage complète** `[ancienDébut, ancienneFin)` suivent le re-datage ; les sous-périodes custom restent **intactes** (comptées `nonSuivies`, log `[redatage]`, la grille les montre déjà) ; `OPTION`→dates seules, `FERME`→re-vérif `chevauchementsFermes` aux nouvelles dates (exclusion du même séjour) → `A_REPLACER` sur conflit (D6, jamais de refus ni de suppression silencieuse), `A_REPLACER`→**reste** `A_REPLACER` (D8, résolution manuelle uniquement), `BLOCAGE`→jamais touché. Extension `chevauchementsFermes` (`exclureSejourId`). **`ea5ee6b`** `updateInfosSejour` **transactionnel** : `$transaction { sejour.update + demandeDevis.update + redater + syncOccupationsSejour(tx) }` — **premier usage réel du `tx?`** réservé aux cascades (`run-chambres-4b.md` §1) ; garde « dates **effectivement** changées » (compare les valeurs, pas la seule présence dans le dto → pas de re-datage no-op à chaque édition du client) ; réponse HTTP **inchangée** (`return updated`, contrat front préservé) ; CRM + email organisateur restent **hors tx, après commit**. Répare au passage le défaut préexistant (3 écritures non atomiques → dates divergentes séjour/demande sur crash). Import `ChambresModule` dans `CollaborationModule` + injection `OccupationsService`. **`6d43f2e`** soft-delete : `occupationChambre.deleteMany` **dans la tx** + **retrait du site 11** (`syncOccupationsSejourSafe` devenu no-op après le deleteMany) ; `AffectationChambre` suit par FK `onDelete: Cascade` ; **amendement archi §3.1** (11 sites → 10 actifs). Gates : tsc 0 · build · **287 → 298 verts**. ⚠️ **Limite connue ACCEPTÉE** : le filet 23P01 est **code mort dans une tx interactive** (`redater` + `sync(tx)`) — une violation d'exclusion met la tx Postgres en *aborted*, le `update→A_REPLACER` du catch échoue (25P02), rollback → **500** pour l'hébergeur. **Zéro corruption** (atomicité préservée ; au retry le check applicatif pose l'`A_REPLACER` proprement). Fenêtre = course concurrente sur la **même** chambre = **quasi nulle** (prod à 0 occupation). Fix futur si besoin = pattern « retry complet » du POST 4a (retirer le catch interne, laisser remonter, rejouer une fois la tx depuis l'appelant). **Dette hygiène née** : `SejourService` conserve une injection `occupations` désormais **inutilisée** (seul usage = ex-site 11) + l'import `ChambresModule` de `SejoursModule` qui va avec → à retirer **ENSEMBLE**, **décidé : greffé au sous-chantier 7** (pas de run d'hygiène isolé). **Séquence actée** : Lot 5 → **sélecteur chambres page séjour (#6, prochain)** → sous-chantier 7 (onglet Chambres collab + rooming PDF). **4b Lots 3-4 = session à froid séparée.** Recette happy-path chambres **reportée à l'usage réel** (septembre, pas de pollution Sauvageon hors saison). Doc : `docs/run-chambres-lot5.md`.
> **Dernière mise à jour : 21/07/2026 (5)** — **MODULE CHAMBRES 4b (transactionalisation des 8 blocs signature) DÉMARRÉ — Lots 1 & 2 LIVRÉS, POUSSÉS, DÉPLOYÉS (front+back `success`).** Refactor à froid, review ligne à ligne **sur fichier réel** par lot. **Design acté (amende §3.3 archi) :** `$transaction` = transitions seules `{devis, séjour, demande, rivaux, invitation}` ; sync/emails/CRM hors tx après commit (le `tx?` de sync = pour les cascades du Lot 5). **Lot 1** `d6e27d0` (`signerDevisDirect` + `uploadSignaturePublic`), **Lot 2** `32e0279` (`uploadSignatureDocument` + `marquerDevisSigneHebergeur` — remontée `updated` via `return`, garde séjour dédoublée). Gates 287 aux 2 lots. **Census : zéro test sur `DevisService`** → recette happy-path **reportée à l'usage réel** (septembre), suite de caractérisation backloguée (avant hiver). Backlog : TOCTOU double-signature (préexistant). **RESTES 4b :** Lot 3 (`annulerDevis` + `updateStatut` NON_RETENU) → Lot 4 (les lourds : `signerDevis` double paquet, `updateStatut` SELECTIONNE, `signerSansCompte` ; emails non protégés à trancher) → Lot 5 cascades. Doc `docs/run-chambres-4b.md`.
> **Dernière mise à jour : 21/07/2026 (4)** — **MODULE CHAMBRES : 4a (affectation backend) + 5.1 (retours recette) LIVRÉS, DÉPLOYÉS, RECETTÉS — 6 sous-chantiers sur 8 en prod en une journée.** **4a** (`ea574b2..37fd2c2` + fix `ada42b6`) : migration etiquette/couleur (D13), `occupations.service.ts` (713 l.) — grille dispo (fenêtre ≤370j, états a_replacer>ferme>bloquée>option×N>libre, exclusion séjours soft-deleted), POST occupations (D12 : naissance A_REPLACER sur conflit, jamais d'échec ; anti-doublon ; retry 23P01), PATCH (marquage D13 sans recalcul / déplacement = résolution A_REPLACER avec 409 parlant), blocages tout-ou-rien, **`syncOccupationsSejour` dérivé `STATUTS_DEVIS_RETENUS` branché aux 11 sites via wrapper `Safe(site)`** (1 ligne/site, aucun email déplacé, aucune transaction — 4b). 287 tests (282→287 avec 5.1). **Review Claude ligne à ligne** : 1 nit corrigé pré-prod (500 brut sur double course blocage → 409 structuré), 2 fragilités acceptées documentées (deriverCible hors tx auto-réparé par idempotence ; détection 23P01 par message). **5.1** (`4941c61`+`f5dbded`) : fix bug ordre D13 (`createChambre` ordre=max+1 + **backfill SQL gardé `bool_and(ordre=0)`**, recetté : étage 1 Sauvageon dans l'ordre du couloir), équipements String[] par chambre (chips, brique Monde 2), liste de lits = source de vérité à la création (compteurs dérivés). **Sentinelle logs `[sync]` : zéro échec** (aucune transition devis depuis le branchement — 1er signal réel à la prochaine signature prod). **Données réelles : référentiel Sauvageon complet saisi — 26 chambres/129 places, la saisie a corrigé le PDF de référence (étiquettes étage 1 = 76 pl., pas 74 ; chambre 26 PMR absente du PDF).** ⚠️ **À AJOUTER (demande Théo 21/07) : étape « Plan des chambres » dans l'onboarding hébergeur frontend** — à intégrer au parcours d'inscription/premiers pas (à cadrer : lien avec le chantier onboarding ex-nihilo du bloc URGENT). RESTE : **4b** (transactionalisation 5 blocs signature + emails hors tx + cascades dates/soft-delete — session dédiée), 6 (grille affectation front), 7 (onglet collab + rooming PDF spatial D13), 8 (recette croisée).
> **Dernière mise à jour : 21/07/2026 (3)** — **MODULE CHAMBRES ÉTAGE 1 : LIVRÉ BOUT EN BOUT, DÉPLOYÉ, EN PROD (backend + frontend `success`).** 5 commits poussés : migration `20260721120000_module_chambres` (4 tables + btree_gist + EXCLUDE partielle, appliquée en prod sans accroc) + module `chambres/` backend (CapaciteService, GET alertes + PATCH acquitter, empreinte de réarmement) `246bc4c..a7240a4` · frontend alertes (composant `AlertesCapacite` deux modes, dashboard + page séjour + badge liste séjours) `329160e..d919e08` · **extension sur-engagement entre signés `eb68edb` — écrite par Claude via MCP (1er chantier code hors CC), relue par CC, `surEngagements[]` dans le GET, pas encore affichée** (micro-run frontend à venir). Gates : tsc 0, build OK, **240 tests** (217 avant). Doc archi **amendé** (RETENUS §3.2, empreinte, STATUTS_SEJOUR_CONFIRMES, extension sur-engagement validée). ⚠️ **Recette visuelle NON faite** — prod actuelle sans alerte à afficher (147/120 de février = options entre elles → silence VOULU, D1) ; **le vrai test = 1ʳᵉ signature sur la semaine de février 2027** : les options restantes doivent passer ⚠️ sur le dashboard — à surveiller ce jour-là. **RESTE module chambres** : affichage `surEngagements` (micro-run) puis sous-chantiers 3→8 (~7 j : référentiel back/front, affectation + `syncOccupationsSejour` avec `STATUTS_DEVIS_RETENUS`, onglet collab, cascades).
> **Dernière mise à jour : 21/07/2026 (2)** — **MODULE CHAMBRES : conception CLOSE, doc de cadrage écrit (`docs/ARCHITECTURE_MODULE_CHAMBRES.md`), zéro code.** Session sparring complète : 12 décisions actées (verrouillage à la signature ; options chevauchantes préservées = l'argument massue anti-Venue360 ; capacité = Σ lits obligatoires ; `A_REPLACER` pour les conflits ; **étage 1 « capacité globale »** découvert en conception — alerte surréservation dès le devis, six mois AVANT l'affectation des chambres, acquittement « prévenu/gardé en attente »). Vérifs prod : `btree_gist` **disponible et créable** (BEGIN/ROLLBACK testé) → contrainte `EXCLUDE` partielle jouable. Recensement `git grep` : **9 sites de promotion + 2 de libération** du statut devis (3 fichiers) → design `syncOccupationsSejour` dérivé/idempotent imposé. ⚠️ **Défaut préexistant découvert : 5 blocs signature écrivent devis + séjour en 2 updates NON transactionnels** (crash entre les deux = devis SELECTIONNE / séjour resté OPTION) — fix embarqué au sous-chantier 4 du module. Découpage : 8 sous-chantiers ~9,5 j, **#2 (capacité globale) autonome et prioritaire**. Prompt CC run 1 (migration + capacité globale backend) préparé.
> **Dernière mise à jour : 21/07/2026** — **Run overnight #2 (Fable 5) LIVRÉ, POUSSÉ, DÉPLOYÉ (backend + frontend `success` Scalingo, commit `fcb8692`).** 3 chantiers, 4 commits, gates verts (217 tests back + 16 vitest) : **2.4 labels** `d73a354` (`formatParticipants` 3e param optionnel rétrocompatible → « X participants (dont Z accompagnateurs) » en HORS_SCOLAIRE ; branché sur page séjour + création devis DIRECT ; **4 sites restants au libellé scolaire = suite éveillée ~0.5j** : signer public, tooltip global, DevisPDF, bannière journal public) · **4.18 badges journal public** `673375d` (backend : `select groupes {id,nom,couleur}` **borné, aucune fuite** sur route non authentifiée + aplatissement au contrat standard ; front : lecture structurée m2m + fallback legacy `— G\d+` exclusif) · **4.9 harness tests frontend** `3a29a1e` (premier framework de test front : **vitest en devDep seule**, config isolée, `build`/`postbuild` HOSTNAME **intacts**, 16 tests de caractérisation du moteur financier `devis-calculs.ts`). Code **relu intégralement par Claude via MCP** : les 3 propres, périmètre tenu, 0 régression. Recette visuelle (labels + badges) **optionnelle, non faite** (code validé en lecture). **⚠️ ANGLE MORT relevé (préexistant, documenté par un test, NON corrigé) : `calculerTotaux` compte les lignes sans description, `mapLignesForApi` les filtre → total AFFICHÉ peut diverger du total ENREGISTRÉ si prix saisi sans libellé. À instruire un jour, non urgent.** Détail : `docs/run-overnight-20260721.md`.
> **Dernière mise à jour : 20/07/2026 (3)** — **Run overnight quick-fixes LIVRÉ (Fable 5) et POUSSÉ — ⚠️ recette visuelle + `scalingo deployments` à faire.** 5 fix, 6 commits : `7b568ab` (4.17 qty 0 → « — » sur PDF devis/facture/page signer, PU TTC conservé) · `7556db9` (4.22 les **4** KpiCard onglet abonnements admin, hex → classes Tailwind, `KpiCard.tsx` intact) · `02a9d3d` (10.4 `build-error*.txt` dans `frontend/.gitignore`) · `d441929` (4.20 alertes trial regroupées par `userId+palier` dans les 2 boucles, tampon par centre après envoi réussi, +3 tests) · `1e64c9a` (10.5 montant renouvellement = source unique `abonnement.constants.ts` en centimes + helper `calculerMontantAbonnementCents`, consommée par souscrire + webhook Mollie + cron, +2 tests). Gates tsc+build verts par commit, **217 tests** (211 avant). CC a corrigé **2 diagnostics faux** seul : 4.20 = alertes envoyées à l'**ADMIN** (pas l'hébergeur) → c'est le bruit admin qui baisse ; 4.22 = lignes 1177-1180 (pas 1038). Détail : `docs/run-quickfixes-20260720.md`. **⚠️ NOUVEL ANGLE MORT découvert en relisant `souscrire` : risque de double prélèvement multi-centre** — voir bloc dédié « FACTURATION MULTI-CENTRE » ci-dessous. **RESTES** : recette visuelle (PDF devis ligne option Alticlub + 4 cartes admin) + `scalingo deployments` + `logs | findstr cron` à J+1.
> *(20/07/2026, fix IP)* — **Fix « IP réelle des signatures » (`trust proxy`) appliqué EN LOCAL — ⚠️ non pushé / non recetté.** Le backend captait des IP internes Scalingo `10.0.0.x` dans `signature_ip_address` (valeur probante nulle sur les 3 autos signées — des tests). Correction d'un **status drift** : la note « ne lit pas X-Forwarded-For » était fausse (`trust proxy` était là mais à `1` = insuffisant). Fix `main.ts` 1 ligne : `trust proxy` → `'loopback, linklocal, uniquelocal'` (dépouille tous les hops privés → 1ʳᵉ IP publique, robuste au nombre de proxies). **Effet de bord positif** : `ThrottlerGuard` rate-limite désormais par vraie IP. **Restes** : push + `scalingo deployments` (leçon HOSTNAME), **recette** (signer 1 auto test → IP publique attendue, seule preuve car 0 signature post-fix), nettoyage des 3 autos de test (+ 1 doc médical OVH à purger). **Angle mort** : `signatureHash` ne couvre ni l'IP ni le contenu signé (chantier séparé si valeur probante critique). Détail : SESSION_STATE 20/07 (2).
> *(16/07/2026, matin)* — **Accès SQL prod RÉSOLU.** Le `db-tunnel` figeait à cause de la **CLI Scalingo 1.44.1 périmée** — mise à jour en 1.47.0, tunnel OK, `SELECT now()` validé en prod. Fausses pistes écartées : NI la box du bureau, NI psql/PATH, NI la clé SSH. ‼️ Le 🔴 « accès SQL cassé » ci-dessous est **CLÔTURÉ** ; la note du 14/07 « pgsql-console fonctionne » était fausse depuis PowerShell. Procédure réutilisable dans SESSION_STATE 16/07. ⚠️ Reste : **régénérer le mot de passe DB** (il a circulé pendant le debug) — Settings PostgreSQL → Reset password.
> *(15/07/2026)* — **Deux fix UX frontend poussés (Scalingo déploie).** (1) Badge « ✓ Devis envoyé le … » sur la page séjour (onglet Devis, DIRECT), stade Brouillon uniquement — comble le trou « on ne sait pas si le devis est parti sans aller dans Notes & suivi ». `Devis.dateEnvoi` déjà en base + déjà renvoyée par l'API, seuls le type front et l'affichage manquaient (réparation à la source, pas un patch). (2) Tooltip du graphe CA mensuel (Pilotage) : double « Encaissé » corrigé (formatter Recharts 3 par `item.dataKey` → libellé Réalisé/Prévisionnel/Encaissé). tsc + build verts. ⚠️ Recette manuelle prod à faire. Voir SESSION_STATE 15/07.
> *(14/07/2026, soir)* — **TVA sur marge : Lot 1 backend LIVRÉ et déployé (`cf8795d`), chantier GELÉ jusqu'à l'hiver.** Endpoint `GET /rentabilite/tva-marge` en prod, `regime_marge_actif = true` sur Sauvageon. ⚠️ **Découverte : ZÉRO facture prestataire en base — le module Rentabilité du 04/06 n'a JAMAIS été alimenté.** Le moteur est testé unitairement, validé sur aucune donnée réelle. Lots 2→5 parkés (voir § TVA SUR MARGE). **Corrections de statut** : ❌ « Node 20 EOL à planifier » → FAIT (24.x en prod) • ❌ « httpOnly cookies reverté » → EN PROD • ❌ « `pgsql-console` cassé » → FONCTIONNE (pipe stdin). Voir SESSION_STATE 14/07 (soir).
> *(14/07/2026, jour)* — **Journée « activation hébergeur » : 9 commits, tout recetté en prod.** ✅ Bloc 🟠 TRIAL RÉSOLU (source unique + garde centre ACTIVE + alignement multi-centre) • ✅ 🟡 DETTE §1 `createCentre` RÉSOLUE — et le bug réel n'était pas celui écrit ici : le parcours **« ajouter un centre »** créait un centre PENDING **invisible des DEUX listes admin**, donc **jamais activable** • ✅ 🟡 §2 **3 WORKFLOWS ADMIN UNIFIÉS** sur l'écran strict (routes permissives supprimées côté serveur) • ✅ **Node 20 → 24** (🟡 §3 résolu) • ✅ `schemeID` Factur-X (bug préexistant, échéance e-invoicing 01/09) • ✅ Backfill SIREN/SIRET prod • ✅ Recette backend T1→T7 sur base jetable • ✅ Recette manuelle prod validée (17h43). ⚠️ **RECTIFICATION** : la note « correction » que j'avais ajoutée au §2 ce matin était **fausse** — la roadmap avait raison, je n'avais lu qu'un des deux écrans admin. Voir §2. Reste : purger `RECETTE 14-07`, purge S3 `kbis/`, accès `pgsql-console` cassé. Voir SESSION_STATE 14/07.
> *(13/07/2026)* — **Bloc 🔴 ex-nihilo RÉSOLU** (10 commits, recette prod de bout en bout : inscription → justificatif → validation admin → devis ENVOYÉ). L'analyse initiale du bloc était partiellement fausse — corrigée ci-dessous. **Nouveau bloc 🔴** : déploiement frontend silencieusement cassé 12→13/07 (HOSTNAME × Next standalone, résolu `88e49ec`, leçon à retenir). Voir SESSION_STATE 13/07.
> *(12/07/2026)* — **Export ZIP des factures PDF LIVRÉ** (§2.8 ci-dessous) : ZIP + index CSV, avoirs inclus dans le ZIP ET dans l'export CSV comptable, **bug multi-centre des exports réparé** (les `<a href download>` court-circuitaient axios → le header `X-Centre-Id` ne partait jamais → tout hébergeur multi-centre exportait son 1er centre, silencieusement). 3 commits poussés (`6023edc`, `f0f96f8`, `b0a1ed3`), recette prod validée (62 PDF Sauvageon, ~10 s). **Statut corrigé** : `28e364a` (fix SIRET) était noté « non poussé » ici — il est **en prod depuis le 09/07** ; en revanche le SIRET **n'est pas réglé** (cf. 4e cause du bloc 🔴). Voir SESSION_STATE 12/07.
> *(08/07/2026, soir)* — Run dette §4 : 4.6 (escapeHtml) et 4.10 (jspdf) constatés déjà faits → actés ; 4.9 partiel (SequenceService couvert par la suite invariants) ; **4.15 LIVRÉ** (bandeau « nouvelle version » non-destructif dans global-error.tsx, jamais de reload auto) ; **4.11 LIVRÉ** (13 vulns → 3 : audit fix + next 16.2.10 + @types/react-pdf mort supprimé ; xlsx = risque ACCEPTÉ ; postcss vendored Next non actionnable) ; **4.7 étape 1 prête** (`docs/AUDIT_FLOAT_DECIMAL.md`, requêtes prod à exécuter par Théo, migration non engagée). Après-midi : chantier 10.1 livré de bout en bout, deadline 26/09 NEUTRALISÉE (cron exclut VIREMENT + Choucas marqué, facture échéance/mention dynamiques, badge Paiement, relance J-30, self-service +14j) ; edge cases onboarding soldés ; compte de test neutralisé. Soir (tard) : fix page abonnement — libellés neutres (amorce §2.4) + bouton « plan actuel / Nous contacter » dérivé du plan courant (`PricingTable`). Matin : onboarding phase 2 (§10.11) + 3 bugs smoke-test corrigés. Voir §2/§4/§10 + SESSION_STATE 08/07.
> *(07/07 : Refonte planning ↔ groupes m2m livrée. Fix crash boot Scalingo P3015. Refactor PDF extraction dynamic imports. Item dette 4.18 ajouté.)*
> *(03/07 : Sécurité verrouillée, Mollie live, Pilotage livré, conventions configurables, contrat événement. Dette 4.1-4.3 livrée. Responsive mobile livré. Diagnostic dette Fable 5.)*
> **Auteur** : Théo + Claude (sparring partner)
> **Ce document remplace** : ROADMAP_POST_DEMO.md, ROADMAP_COMPLETE.md, TIER1_CHANTIERS.md comme source de priorisation.
> **Règle** : les docs ci-dessus restent comme archives de décision. Celui-ci est le seul qui dit quoi faire et dans quel ordre.

---

## 📧 CHANTIER « IDENTITÉ EMAIL HÉBERGEUR » (né 13/08/2026 — retours boîte réelle Sauvageon)

> Trois problèmes d'une même famille : **le client final ne voit jamais l'hébergeur** dans les emails plateforme (sauf en ouvrant la PJ). Constatés sur la facture FS-2026-0070 (mariage BRIENNE) + réponse auto ALM (DEV-2026-0007).
> 1. 🔴 **FROM des factures = « Liavo »** au lieu du nom du centre → un particulier (mariage) suspecte du phishing / classe en spam. Cause (code réel) : `sendFactureParEmail` (email.service.ts) passe `fromName: undefined` → fallback `SENDER_NAME='Liavo'`, alors que le `replyTo` transporte déjà le nom du centre et que le flux DEVIS le fait correctement (preuve : réponse auto ALM adressée « À Chalet Le Sauvageon »). **Fix 1 ligne ÉCRIT le 13/08 (Claude via MCP, validé Théo), gates CC VERTS (tsc 0 / build / 363)** : `fromName = replyTo.name` dans `sendFactureParEmail`. Grep gate : **2 appelants** (pas 1) — `facture.service::envoyerFactureParEmail` (replyTo = nom du centre, vérifié + preuve runtime capture 13/08) ET `facture-liavo.service::emettre()` (replyTo = 'Liavo') → effet sur les factures d'abonnement ACCEPTÉ : 'Liavo'→'Liavo', zéro changement observable (le fallback SENDER_NAME résolvait déjà 'Liavo' en prod), et sens voulu — LIAVO facture ses abonnés sous son nom.
> 2. 🟠 **« Se désabonner » sur les emails TRANSACTIONNELS** (factures, devis) — le code ne pose AUCUN header `List-Unsubscribe` (vérifié `send()` : sender/to/replyTo/subject/html/attachment, rien d'autre) → header **injecté côté Brevo** à l'envoi. Risque réel : un client qui clique = adresse en **suppression list Brevo** → toutes ses factures suivantes non délivrées SILENCIEUSEMENT. TICKET ENVOYÉ puis REFUS Brevo 17/08 (list-help réservé aux comptes Enterprise, motif RGPD/CSA invoqué) — Enterprise écarté (hors de proportion). GARDE-FOUS ACTÉS : (a) surveillance périodique Paramètres > Emails transactionnels > Contacts bloqués ou désinscrits (contrôle 17/08 : 0 désinscrit réel, 12 hard bounces = uniquement fixtures de test mars-mai, aucune action) ; (b) BACKLOG LÉGER (~1h) : webhook transactionnel Brevo événement « unsubscribed » → endpoint backend → alerte admin, transforme l'échec silencieux en signal immédiat ; (c) si incident réel à l'échelle Tereva → rouvrir la question ESP transactionnel dédié. Leçon annexe 17/08 : ne plus envoyer depuis la prod vers des adresses fabriquées (@test.fr, faux gmail) — chaque hard bounce écorne la réputation de contact@liavo.fr ; jeux de démo (LMDJ septembre) sur adresses réelles poubelle. Le désabonnement restera légitime UNIQUEMENT sur les futures campagnes marketing des hébergeurs (fonctionnalité pas encore existante).
> 3. 🟡 **Routage des RÉPONSES AUTOMATIQUES** — les répondeurs (congés/absence) répondent au FROM (`contact@liavo.fr`) en ignorant le Reply-To (comportement standard RFC 3834, anti-boucles) → l'info tombe chez l'admin LIAVO au lieu de l'hébergeur (cas ALM : « fermé jusqu'au 17/08 » = info utile au dossier DEV-2026-0007, perdue si hébergeur ≠ Théo). Inhérent au modèle « From plateforme + Reply-To hébergeur » (seul viable en délivrabilité sans toucher le DNS des hébergeurs). **À l'échelle Tereva (~100 clients/~200 centres) : contact@liavo.fr saturée des réponses auto des clients de TOUS les hébergeurs.** Conception À FROID, pas de patch : options à instruire = transfert auto entrant→Reply-To d'origine (matching par références) / adresse d'expédition par centre sous liavo.fr (infra) / autre. NE PAS coder à chaud.

---

## 🧾 NOTES FACTURATION UX (13/08/2026 — recette FS-2026-0070)

> 1. **Nom de fichier au téléchargement des PDF facture/devis** — le lien pointe l'URL OVH en direct et le navigateur nomme le fichier d'après la clé objet, préfixée UUID (`19079550-…-FS-2026-0070.pdf`) : inutilisable pour archivage / envoi comptable. Attendu : `FS-2026-0070.pdf`. L'attribut `download` de l'ancre est inopérant en cross-origin. 3 options à instruire : (a) **`ContentDisposition: attachment; filename=…` posé à l'upload S3** (storage.uploadBuffer — zéro coût runtime, ne couvre que les PDF générés après le fix) ; (b) fetch blob côté front dans SecureFileLink + `URL.createObjectURL` + nom propre (couvre l'existant, coût bande passante double) ; (c) proxy backend `GET /factures/:id/pdf` streamant avec le header (couvre l'existant, charge serveur). Reco pressentie : (a) + fallback (b) dans SecureFileLink pour l'existant. À faire dans le chantier conformité facturation ou en quick-win isolé.
> 2. **Régénération PDF d'une facture déjà dotée d'un pdfUrl** — aucun chemin UI (le bouton « Régénérer » n'apparaît que si pdfUrl null) ; seule voie actuelle = versement fantôme ajouté/supprimé (déclenche refreshFacturePdf). Constat né du fix croisement du 13/08 : les factures émises avant un fix de layout gardent leur rendu à vie. À trancher CONSCIEMMENT au chantier conformité : immuabilité stricte assumée (vertueux comptablement) OU régénération admin exposée. Pas de décision prise le 13/08.

---

## 🗺️ IDÉE PRODUIT — Carte interactive du catalogue (backlog)

> **Carte interactive du catalogue** — afficher les centres sur une carte (recherche géographique visuelle côté organisateur + actif de démo). Prérequis : coordonnées GPS en base → migration `latitude`/`longitude` sur `CentreHebergement` + backfill depuis l'API Éducation nationale (qui les expose déjà, champs `nom_du_lieu_d_accueil_latitude`/`_longitude`). Bonus : dédup GPS infaillible (2 fiches au même point = même bâtiment). Reporté en **Lot 2bis** du chantier catalogue (décision 20/07 : GPS sorti du Lot 2 pour garder le commit propre — le Lot 2 backfille identifiant EN + avis + thématiques + capacité adultes sans toucher au schéma).

---

## 🛏️ MODULE CHAMBRES — conception close 21/07, exécution à lancer

> **Source de vérité : `docs/ARCHITECTURE_MODULE_CHAMBRES.md`** (12 décisions, modèle, SQL, machine à états, cascades). Ne rien relitiger ici.
> Périmètre Monde 1 : référentiel étages→chambres→lits (profil centre) · affectation chambres→séjour multi-cohabitation avec cloisonnement · onglet Chambres espace collab (drag & drop participants, capacité bloquante) · **étage 1 capacité globale** (alerte surréservation dès le devis, sans aucune chambre saisie).
> Hors scope : PMS week-end, widget résa, tarification, paiement, channel manager OTA (rayé définitivement). Monde 2 = additif garanti (`source` sur la primitive + « fermé par défaut, ouvertures explicites »).
> **État (24/07)** : EN PROD = étage 1 (alertes + acquittement/réarmement + sur-engagement), référentiel back/front, **4a** (occupations + grille + POST), **4b Lots 1-2** (blocs signature publics transactionnels), **cascades séjour** (`32e0279..6d43f2e`), **#6 onglet « Chambres » page séjour** (UI hébergeur d'attribution), **#7 rooming espace collab** (drag&drop participants→chambres côté ORGANISATEUR, compteur + directives étiquettes côté hébergeur, affectation backend + accès aligné sur les groupes — `4dcc596`→`22994ad`), **lot couture** (rooming lisible hébergeur + DTO validation saisie directe + confirmation Retirer + dimensionnement + catégorie au rooming — `e2fd475`→`9364af3` ; 2 🔴 d'audit fermés). **Suite** : **LOT 4 (prochain) = vue « plan » lisible + PDF** — le livrable qui remplace l'Excel et se vend à Tereva. **Plan PUR** (« qui dort où » uniquement, zéro régime/médical = RGPD car le doc sort de l'outil). **Option C** : une vue écran lisible (lecture seule, par étage, sans colonne d'édition) qui s'imprime — un seul build sert écran + PDF. **Générique multi-hébergeurs par construction** (rend `getRooming` trié étage→ordre→nom D13, PAS de gabarit Sauvageon). À border : `etage` nullable (plan à plat), petit vs gros centre (pagination, 8 vs 60 ch. Tereva), avertissement autorisations (`parentEmail!=null && signeeAt==null`). **⭐ ACTION DEMAIN AVANT DE CADRER : lire l'outil PDF existant** (planning A4 paysage, conventions, factures) pour trancher @react-pdf serveur vs `window.print()`+CSS print — `search_files` matche que les noms exacts, localiser les `.pdf.tsx` via `list_directory` sur `frontend/`. → grille calendaire hébergeur (confort, non bloquant démo) → **4b Lots 3-4** (blocs signature restants, session à froid) → recette croisée (#8). Hygiène Lot 5 (injection inutilisée `SejourService`) **PAS encore faite** (n'a pas été greffée au #7 — reste à traiter). Démontrable à Pierre/Tereva en septembre **après recette écriture réelle** (référentiel Sauvageon 26 ch./129 pl. déjà saisi).
> ⚠️ Embarque la **mise en transaction des 5 blocs signature non atomiques** (défaut préexistant, sous-chantier 4).

### 🏠 CHANTIER « GESTION EN PROPRE » (29/07) — 3c CLOS, points différés post-3c

> **Cible** : l'hébergeur qui organise SON séjour DIRECT en propre (Louise/PULSE, Théo/Sauvageon) attribue ses chambres ET roome ses participants lui-même. Socle (saisie participants + accès rooming écriture + extraction `RoomingEditor`) **LIVRÉ et recetté** (voir SESSION_STATE 29/07). Discriminant = **`peutGererEnPropre`** (`DIRECT && hebergementSelectionne.userId===userId`), JAMAIS `createurId` (NULL sur les DIRECT).
>
> **✅ 3c FAIT (29/07 (2)) — chantier « gestion en propre » CLOS.** `RoomingEditor` monté côté hébergeur-créateur (`groupParEtage` branché, sert de plan) ; « Gérer les chambres » dépliable en accès secondaire ; aiguillage `page.tsx` via `peutGererEnPropre` (nouveau helper `src/lib/sejour-ownership.ts`) ; SANS gate clôture ; layout COLLAB inchangé. Commits `03dbcca` (groupParEtage opt-in, orga pixel-identique) / `86fb6e8` (vue propre + `renderAttribution()` interne) / `ee5b813` (4e état erreur anti-spinner-infini). Recette verte : non-régression orga (Maëva/COLLAB) + bout-en-bout TEST CLEM (RDC→1er→2ème). Détail : SESSION_STATE 29/07 (2). **RESTE = 3 différés post-3c à prioriser (aucun lancé, ne pas coder à l'aveugle) : voir le bloc POINTS DIFFÉRÉS ci-dessous (unification COLLAB↔DIRECT / PDF plan / lisibilité bouton).**
>
> **🔎 AUDIT CC 29/07 (3) — 2 TROUS DU PÉRIMÈTRE ACTUEL FERMÉS + RESTE consolidé.** Audit CC lecture-seule exhaustif (backend occupations/rooming/autorisation + front TabChambres/TabRooming/RoomingEditor/RoomingPlanView/TabParticipantsSaisieDirecte) + relecture Claude. **✅ B1 🔴 (régression ROUVERTE par 3c) FERMÉ** `380c361` : dans le layout propre, `handleAffecterParticipant`/`handleRetirerAffectation` ne rechargeaient que `loadRooming()` → la grille gardait `nbAffectations=0` → `handleRetirer` sautait la confirmation → cascade FK (`AffectationChambre onDelete: Cascade`) désaffectait les roomés EN SILENCE (le 🔴 du lot couture rouvert, atteignable par le MÊME user en 3 clics). Fix Voie 1 (Voie 2 « dériver depuis rooming » écartée = 2e source de vérité) = `loadGrille()` après `loadRooming()` dans les 2 handlers + commentaire corrigé. **✅ B2 🟠 (= point 4 ci-dessous) FERMÉ** `bdfa0c6` : `window.confirm` en tête de `handleSave` AVANT `setSaving(true)` (le ✕ marque `deleted` en local, réversible ; le save commit). Diffs relus par Claude sur code réel, tsc 0 + build 61/61, **recette PROD verte TEST CLEM** (B1 popup avec le BON compte, B2 cascade + contre-test Annuler = rien détruit, prouvé F5). **RESTE consolidé (audit CC + Claude) — détail ligne-à-ligne : SESSION_STATE 29/07 (3) :** **(A) DÉCISIONS DE PÉRIMÈTRE à trancher AVANT de coder (ne PAS coder à l'aveugle) :** • **B4+S1 🟡 ÉQUIPE DE CENTRE** (STRUCTURANT avant Tereva = 14 centres = équipes) — `getCentreForUser` accepte les collaborateurs de centre (ils VOIENT la grille, attribuent) MAIS `resoudreAccesRooming` exige `userId` owner-STRICT (rooming.service:127) → pour l'équipe : mode Plan 403, PDF jamais, layout propre inactif. Ouvre-t-on le rooming à l'équipe ? Si OUI → check permission `sejours` sur AffectationController (S1 : ni PermissionGuard ni PlanGuard aujourd'hui) + gate plan sur `retirer`. • **B5 🟡 AUTORISATIONS PARENTALES CÔTÉ PROPRE** — `parentEmail` saisi mais `envoyerInvitations`/import CSV/`getBySejour` restent ORGANISATEUR+créateur-strict → email INERTE (aucune signature déclenchable), impasse UX. Ouvrir `envoyerInvitations` avec `peutGererEnPropre` OU masquer le champ email côté propre. **(B) HYGIÈNE (run à froid groupé) :** code mort M1-M7 — **M2 = hygiène Lot 5 JAMAIS faite** (injection `OccupationsService` inutilisée dans SejourService + import ChambresModule associé, retirer ENSEMBLE) ; M1 re-export ETIQUETTES mort ; M3/M4/M5 commentaires périmés depuis 3a ; M6/M7 dédup (`isPlanInsufficient` ×3, gate `getSejourDuCentre` ×3, discriminant « en propre » ×3) ; **B3 🟡 ordre étages chemin À PLAT orga** (`getRooming` trie alphabétique rooming.service:218 → RDC en dernier chez l'orga ; les chemins groupés OK ; border #1 ne couvre QUE le groupé). **(C) GLISSEMENTS SÉCU/RGPD (arbitrer, backlog) :** S2 `cloturerInscriptions` trop ouvert (hérite @Roles de classe) ; S3 `hebergementCategorie` lisible par l'hébergeur de n'importe quel COLLAB via getRooming (glissement) ; S4 statut séjour JAMAIS vérifié dans `resoudreAccesRooming` → roomer/saisir sur ANNULE/TERMINE possible. **(D) COSMÉTIQUES :** après Annuler-suppression la ligne masquée (`deleted` local) ne réapparaît pas sans reload (donnée INTACTE, prouvé F5) ; + PDF liste/planche & bouton « Gérer les chambres » (déjà 3bis/3ter). **SÉCU AUDIT : aucune faille IDOR, mutations doublement gardées (rôle+propriété), FK composite bloque l'inter-séjours EN BASE, `peutGererEnPropre` fail-closed.** **PRIORITÉ (reco Claude, à valider) : 1. B4+S1 (avant Tereva) → 2. B5 → 3. extensions (groupes puis unification, APRÈS B4) → 4. run hygiène M1-M7 → 5. backlog (B3, S2-S4, cosmétiques).**
>
> **POINTS DIFFÉRÉS (à traiter APRÈS le rooming, NE PAS mélanger à 3c) :**
> 1. **🔴 SÉJOUR SANS DATE = ÉTAT DURABLE ET NÉCESSAIRE côté hébergeur** (arbitrage Théo 29/07) — aujourd'hui **introuvable dans l'onglet Séjours ET le Planning** (trouvable seulement via Devis). Le Planning est normal (pas de date = pas de calendrier). L'onglet **Séjours** doit le rattraper (c'est le hub de navigation censé montrer ce que le planning ne peut pas). → l'app doit gérer l'état « date à définir » **PARTOUT** (liste séjours, recherche, CRM), pas un simple rattrapage dans la liste. **Bloquant-léger** (chemin de secours Devis existe) → à faire avant la démo septembre. NB : l'attribution de chambres est volontairement bloquée sans dates (« Définissez d'abord les dates », garde intentionnelle).
> 2. **✅ GROUPES EN PROPRE — FERMÉ 29/07 (4)** `af27949` (frontend-only) + **IDOR groupes fermé** `e92af5b`. L'hébergeur-créateur peut désormais REMPLIR ses groupes (drag SANS clôture ; `peutEditer` = ORGANISATEUR || peutGererEnPropre ; `colonneNonAffectesVisible` = inscriptionsCloturees || peutGererEnPropre ; non-régression COLLAB préservée ; libellé suppression étendu « chambre ou un groupe »). Le backend groupes était DÉJÀ ouvert (`verifyAccess`, branche DIRECT+hébergeur), contrairement au rooming → verdict frontend-only. IDOR pré-existant fermé au passage (cloisonnement séjour sur affecter/retirer élève). Recette verte TEST CLEM. Détail : SESSION_STATE 29/07 (4).
> 3. **⭐ UNIFICATION de la vue Chambres hébergeur COLLAB↔DIRECT** (arbitrage Théo 29/07 (2), étend l'ex-« fusion vues orga ») — aujourd'hui l'hébergeur voit DEUX écrans différents selon le séjour : répartition groupée + « Gérer les chambres » en DIRECT, ancien toggle Attribution/Plan en COLLAB. Objectif = **même tête d'écran partout** : répartition groupée par étage, ÉDITABLE en DIRECT / **LECTURE SEULE en COLLAB** (l'hébergeur voit le remplissage de l'orga, à jour au chargement — **PAS de temps réel/websocket**, ne pas le promettre), attribution derrière « Gérer les chambres » dans les deux cas. ⚠️ NE PAS montrer `RoomingEditor` avec drag à un hébergeur COLLAB (403 garanti — lie le backlog « drag visible en lecture »). **Options** : A (coquille unifiée + `RoomingPlanView` en lecture pour le COLLAB — réutilise l'existant, ne touche PAS RoomingEditor, zéro risque orga = **reco**) ; B (mode `lectureSeule` de RoomingEditor — écran strictement identique mais touche le composant orga) ; C (unifier AUSSI côté orga = grand nettoyage, supprime les toggles des 2 côtés, recette lourde septembre). Recette COLLAB incomplète hors saison (besoin d'un orga qui roome).
> 3bis. **PDF « plan » = LISTE par étage, pas une planche 2D** — conforme à la spec Lot 4 (plan à plat, pas d'architecture), donc PAS un bug, mais écart visuel avec l'écran (qui est en cartes). À trancher un jour : liste mieux titrée VS planche par étage reprenant les cartes de l'écran. Enjeu Tereva : la planche pagine mieux à 60 chambres qu'une liste continue. Non bloquant.
> 3ter. **Lisibilité du bouton « Gérer les chambres »** — trop discret (lien gris `▸`) + libellé ambigu sur un séjour déjà amorcé (« gérer » ne dit pas quoi). L'état vide « Commencez par choisir vos chambres » (jamais vu en recette — TEST CLEM avait déjà des chambres) est à VÉRIFIER. Reco : clarifier le libellé (+ compteur « Chambres du séjour (N) ») SANS écraser la répartition (geste rare, garder en retrait) — changement de texte/style, pas d'archi.
> 4. **Cascade suppression participant** — `deleteAutorisation` retire silencieusement l'élève de sa chambre (`AffectationChambre onDelete: Cascade`, schema:1809), SANS confirmation côté saisie directe (alors que « Retirer chambre » de TabChambres confirme). Ergonomie destructive. **✅ FERMÉ 29/07 (3) = B2** (`bdfa0c6`) : `window.confirm` au save de la saisie directe — le ✕ marque `deleted` en local (réversible), le save commit → confirmation au point de non-retour. Recetté PROD.
> 5. **Message « Capacité atteinte » (409)** — jamais déclenché à la recette (pas assez de participants pour saturer une chambre de 4). Recette différée, non bloquant (code inchangé, remontée du 409 préservée en 3b).
>
> **LEARNING gravé** : sur un séjour DIRECT, `createurId` est NULL ; le lien hébergeur↔séjour passe par `hebergementSelectionne.userId`. Tout futur gate d'accès sur un DIRECT → `peutGererEnPropre`, jamais `createurId`.

---

## 🚀 CHANTIER PRIORITAIRE — ONBOARDING (multi-centre) + SUIVI SANTÉ CLIENT (cadrer/livrer septembre)

> **Né le 24/07 du cas Louise/PULSE** — 1er compte multi-centre réel (2e centre Chamberet créé en autonomie, activé, vérifié en base). Deux manques révélés, tous deux prioritaires pour la rentrée :
>
> **(1) Onboarding hébergeur — notamment MULTI-CENTRE.** Chamberet a été créé mais sa fiche est **nue** (ni photo, ni description, ni région/GPS → invisible/pauvre côté recherche organisateur, ≠ Valloire enrichie par le catalogue). L'onboarding ne guide pas (ou pas assez) l'**enrichissement par centre**. À cadrer : (a) **auditer le parcours d'onboarding existant** (existe-t-il ? couvre-t-il quoi ?) ; (b) distinguer onboarding **« compte »** (one-shot : profil, 1er centre, plan) de l'onboarding **« par-centre »** (récurrent : photo/desc/chambres/catalogue — CHAQUE nouveau centre repart à zéro, c'est lui qui rend un centre trouvable) ; (c) rattacher la **demande existante** « ajouter l'étape *Plan des chambres* à l'onboarding hébergeur » (notée 21/07) ; (d) enjeu direct **Tereva** : 14 centres multi-régions = 14 fiches à enrichir, l'onboarding par-centre doit tenir à l'échelle.
>
> **(2) Suivi santé client (admin).** Le tableau admin a classé Louise **« Aucune activité »** alors qu'elle a créé un centre **le matin même** — la métrique mesure la **récence d'usage** (séjours/devis récents), elle est **aveugle au setup et à l'engagement**. Risque : **dé-prioriser un lead chaud** en septembre. À cadrer : signal **« setup récent / onboarding en cours / fiche incomplète »** distinct de *actif*/*inactif*, idéalement corrélé à la complétude de fiche du point (1).
>
> Non ouvert aujourd'hui (conception, pas un fix). **Reprise = septembre**, avec la vague de démarchage. Lié au bloc URGENT « onboarding inscription ex-nihilo » et à l'angle mort facturation multi-centre ci-dessous.

---

## 🧭 ESPACE ORGANISATEUR — lisibilité (conception, septembre)

> **Né le 31/07 de retours terrain + de l'incident Mme Noire.** Symptôme : les organisateurs ne comprennent pas leur espace, demandent de l'aide à leur hébergeur, qui ne connaît pas cet écran, donc l'organisateur remonte jusqu'à Théo. **Chaîne d'escalade = bloqueur d'adoption à l'échelle Tereva** (~100 clients / ~200 centres — intenable si chaque org remonte). Trois trous qui n'en font qu'un :
> - **2a — devis DIRECT signé invisible dans l'espace du signataire à compte.** Signer et être rattaché à un compte sont décorrélés (cause profonde du cas Mme Noire). Théo VEUT le faire (rattacher signataire↔séjour, relation/migration probable), éventuellement plus tard.
> - **2b — invitation collaborative PENDING introuvable depuis le dashboard organisateur.** Si l'org rate le mail, aucun moyen de la retrouver connecté. Donnée déjà dispo (`SejourCollabInfo.invitationCollab`). Sous-point : wording `/rejoindre/{token}` faux pour un rattachement de DIRECT existant.
> - **Dashboard globalement peu intuitif.**
> **Ne pas traiter par bouts** (ajouter des vues surcharge l'écran = aggrave). Angles à trancher : (a) onboarding organisateur — il n'en a AUCUN, l'hébergeur si ; (b) simplifier l'IA (un org a besoin de 3 choses : son séjour, son devis, ses participants) ; (c) outiller l'hébergeur pour aider = **déconseillé** (dépend d'humains, ne scale pas). **Conception maintenant, livraison septembre.** Prérequis : lire le dashboard organisateur réel + `/rejoindre/[token]` (non fait au 31/07).

---

## ⚠️ ANGLE MORT — FACTURATION MULTI-CENTRE : risque de double prélèvement (à instruire avant Tereva / Pôle Montagne)

> **Découvert le 20/07 en relisant `abonnement.service.souscrire()` (validation du fix 10.5).** `souscrire()` travaille **par centre** : il pose `mollieMandatId` / `mollieCustomerId` / `mollieSubscriptionId` sur LE centre ciblé et crée une subscription Mollie dont le montant = `plan + supplément pour TOUS les centres actifs du compte` (`calculerMontantAbonnementCents`, comptage `WHERE userId, statut ACTIVE`).
>
> **Le risque (INTERPRÉTATION — parcours de souscription frontend NON lu, à confirmer) :** si un compte souscrit deux centres **séparément** (espace YAKA → souscrire, puis Florimont → souscrire), on obtient **2 mandats + 2 subscriptions**, chacune facturant `plan + supplément total` → **double prélèvement** (ex. 2 centres Pilotage annuel = 2×1080 € au lieu de 1080 €). Le pricing (« +39 €/centre supplémentaire ») décrit **un abonnement par compte** ; le code penche **un par centre**. Cohérents seulement si l'UX **empêche** de souscrire plus d'un centre — non vérifié.
>
> **Impact aujourd'hui = NUL** : aucun compte multi-centre n'a payé via Mollie (Choucas = virement, tous les autres trial/offert). Risque **dormant**, se réveillerait au **1er vrai prélèvement multi-centre**.
>
> **À trancher AVANT** Pôle Montagne (YAKA + Florimont) et surtout **Tereva (14 centres, pricing multi-centre = cœur du deal)**. Session dédiée : lire le parcours souscription frontend + `souscrire`, vérifier s'il existe une garde « ce compte a déjà un mandat », trancher le modèle (1 abonnement/compte vs 1/centre). **Ne pas coder à l'aveugle.** Hors run quick-fixes du 20/07.

---

## ❄️ TVA SUR MARGE — Lot 1 LIVRÉ (14/07), Lots 2→5 GELÉS jusqu'à l'HIVER 2026-2027

### Le problème
Les hébergeurs de montagne **revendent** des prestations achetées à des tiers (ESF, guides, transport, rafting, chiens de traîneau). Ces prestations relèvent du **régime de la marge** (art. 266-1-e CGI) : TVA à 20 % sur `vente TTC − achat TTC`, TVA d'amont **non déductible**. La pension produite en propre reste au régime normal (10 %).

Théo remplit ça **à la main dans un Excel** (3 onglets, ~268 k€ de flux, 15 k€ de marge sur déc→mai) qu'il envoie à son expert-comptable. Objectif : que LIAVO produise ce tableau et que l'Excel devienne un **export**.

### ⚠️ Le piège (à ne jamais oublier)
`rentabilite.getTableau()` calcule `CA du devis ENTIER − charges` = marge **économique**, **pension incluse**. Ce n'est **PAS** la base du régime de la marge. Diviser cette marge par 1,2 = **déclarer de la TVA sur la pension** = sur-imposition. Le trou n'était pas la formule, c'était **le côté vente** : rien ne permettait de dire « cette ligne de devis est une revente tierce ».

### ✅ LOT 1 — Backend (LIVRÉ, `cf8795d`, en prod)
- Migration `20260714120000_tva_sur_marge` : `regime_marge_actif` + `taux_tva_marge` (centre), `revendu_tiers_defaut` + `categorie_marge` (catalogue), `revendu_tiers` + `categorie_marge` (lignes de devis).
- `GET /rentabilite/tva-marge?annee=` → `parMois[12]`, `parPoste[]`, `totaux`, `anomalies[]`. Plan PILOTAGE requis.
- **4 requêtes, aucune en boucle.** TVA dérivée (`baseHT + tva === margeTTC`). Marge négative conservée signée.
- 5 anomalies de données, dont `FACTURE_SOUS_VENTILEE` (**bug existant** : `validateMontantEtVentilations()` ne rejette que la SUR-ventilation — une sous-ventilation crée une charge orpheline en silence).
- 10 tests. `regime_marge_actif = true` sur Sauvageon.

### 🔴 POURQUOI C'EST GELÉ

**Smoke test post-déploiement : `parPoste: []`. ZÉRO facture prestataire en base.**

Le module Rentabilité, livré le **04/06/2026** à la demande de Yves Massard, **n'a jamais été alimenté en production.** Conséquences :
1. Le moteur est testé unitairement, **validé sur zéro donnée réelle**.
2. Le coût réel du projet n'est pas le code, c'est **la saisie des factures prestataires**. Théo ne peut pas la faire avant l'**hiver 2026-2027** (pas de vrais séjours LIAVO au Sauvageon d'ici là).
3. **Aucun client payant n'a demandé cette feature.** Yves / Anne / Alticlub n'ont **jamais été sondés** sur le régime de la marge.

Le Lot 1 est **inerte en prod** : aucun frontend ne le consomme, `regime_marge_actif = false` partout ailleurs. Zéro impact utilisateur, zéro coût à le laisser dormir.

### 🚦 CONDITION DE REPRISE (à vérifier AVANT de coder le Lot 2)
**Poser la question à Yves, Anne et Alticlub** : *« êtes-vous au régime de la marge sur les prestations que vous revendez ? Vous le suivez sur Excel ? »*
- **3/3 oui** → c'est le hook du palier PILOTAGE à 79 €. Prioritaire.
- **1/3** → feature Sauvageon (centre gratuit de Théo). Assumer comme telle, la garder en dernier.

### 📅 LOTS PARKÉS

| Lot | Contenu | Est. |
|---|---|---|
| **2** | **Frontend saisie** : checkbox « revendu (régime marge) » + `categorieMarge` sur le catalogue **et** les lignes de devis. ⚠️ `categorieMarge` doit être un `<input list>` **alimenté par les `typeCharge` déjà utilisés par le centre** — sinon les colonnes vente et achat ne se rejoignent jamais et tout le chantier est inutile. ⚠️ Le flag doit **piloter** la TVA (cocher → force `tva = 0` + mention PDF), jamais l'inverse : deux sources de vérité = TVA fausse sans signal. | 1,5 j |
| **3** | **Export XLSX format EC** (mensuel + par poste, calqué sur l'Excel actuel). C'est **lui** qui fait disparaître le fichier. Le canal `/pilotage/export/*` existe déjà. | 1 j |
| **4** | **Frontend restitution** : onglet « TVA sur marge » + **écran d'anomalies** (non négociable : sans lui, l'Excel ne peut pas disparaître — quand on saisit à la main on VOIT les incohérences, automatisé on ne voit plus rien). | 2 j |
| **5** | **Mention TVA sur les PDF** : le prop `mentionTVA` **existe** dans `FacturePDF.tsx` mais `mapFactureToPdfProps()` ne le renseigne **jamais** → les factures affichent « TVA (0 %) » **sans la mention légale du régime de la marge**. Non fatal aujourd'hui (aucune facture de séjour émise, 1ʳᵉ en décembre), **à corriger avant la 1ʳᵉ facture d'hiver**. | 0,5 j |
| **6 ?** | **OCR facture prestataire** (le PDF est déjà uploadé dans OVH) → pré-remplissage nom/n°/date/montant. Tue la dernière tâche manuelle. **Hors périmètre**, à rediscuter. | — |

### 🔧 BACKFILL (prêt, NON EXÉCUTÉ)
Chez Sauvageon, les lignes revendues sont **déjà saisies à `tva = 0`** (avec mention régime marge sur la facture papier). Donc :
```sql
UPDATE lignes_devis SET revendu_tiers = true WHERE tva = 0;
```
⚠️ **Jamais en aveugle** : `tva = 0` peut aussi désigner une caution, une remise, une taxe de séjour, une erreur. `SELECT` de contrôle obligatoire + écran de vérification derrière.

### 📝 DÉCISIONS DE RÉFÉRENCE (ne pas relitiger)
- **LIAVO ne déclare RIEN.** LIAVO restitue, l'EC déclare. Aucune règle fiscale codée (pas de plancher à zéro, pas de report, pas de compensation inter-mois). Marge négative = conservée signée, pas une anomalie.
- **Rattachement mensuel = `sejour.dateDebut`**, même à cheval sur 2 mois.
- **Ne pas filtrer sur `isComplementaire`** — filtrer sur le flag `revenduTiers`.
- **`categorieMarge` et `typeCharge` en VarChar(50)**, même longueur, clé de rapprochement.
- **Ne PAS toucher à `getTableau()`** : la marge économique reste l'indicateur de Yves. Deux indicateurs, pas un remplacement.

---

## ✅ RÉSOLU 13/07/2026 — Parcours inscription hébergeur ex-nihilo (identifié 09/07, ex-🔴 URGENT)

**Symptôme initial** : un hébergeur qui s'inscrivait seul avec un centre HORS catalogue pouvait finir avec un compte détruit à l'inscription (cas Louise Giard / PULSE SPORTS, rattrapée en SQL), une checklist « Déposer un justificatif » en boucle infinie, et des envois externes bloqués sans issue apparente.

**⚠️ L'analyse initiale de ce bloc racontait une histoire que la session du 13/07 a DÉMENTIE — corrections :**
- ❌ *« ni en self-service, ni par l'admin via l'UI »* : **FAUX**. L'admin POUVAIT déjà valider : le dashboard admin consomme `/centres/admin/claims` → `centreService.validateClaim`, qui **accepte `EN_ATTENTE_DOCUMENT`**. Il n'y a jamais eu de cul-de-sac admin — il y a en revanche trois implémentations divergentes du même workflow (cf. 🟡 DETTE 13/07 §2).
- ❌ *Le SIRET comme cause* : c'était un **SYMPTÔME**. La cause racine était l'**absence d'atomicité** dans `registerHebergeur` : `centre.create` hors transaction → toute erreur en aval laissait un user orphelin et un **email brûlé à vie** (ConflictException à chaque retry). Le SIRET était juste le champ qui a fait tomber Louise.
- ❌ *« 1 CTA cassé »* : il y en avait **TROIS** (OnboardingChecklist + 2 bannières dashboard), tous vers `/documents` — dont l'upload alimente la table `Document`, jamais lue par `getOnboardingStatus`.
- ✅ **5e cause découverte, jamais identifiée** : les emails post-inscription étaient **`await`és après le commit**. Un échec Brevo (timeout, rate-limit) → 500 → compte créé quand même → email brûlé. **Même symptôme que le bug d'atomicité, par une autre porte.** Corrigé (fire-and-forget avec log exploitable).

**Les 5 causes réelles, toutes corrigées le 13/07** :
1. **Atomicité** (`60eb12f`) : transaction interactive user + centre + organisation + membership + consentement RGPD, mapping d'erreurs (P2002 → 409, P2000 → 400 FR actionnable), tous les emails après commit, rollback prouvé sur vraie base.
2. **SIRET `@Length(14,14)`** + strip sur les 3 DTO register/create/update (`b1bbcd4`) — filet de sécurité, PAS la réparation (c'est l'atomicité qui répare).
3. **`motDePasseDefini`** dans `centre.service.register()` (`5e59e28`) — bug dormant corrigé AVANT la correction du routage cas 2 qui l'aurait réveillé.
4. **Écran justificatif** (`ba54a8c`, `c90fd53`, `39aae43`, `6f765fa`, `74adee8`, `81e4315`) : `/dashboard/hebergeur/justificatif` appelle les VRAIS endpoints ((A) `upload-kbis` champ `file` / (B) `upload-justificatif` champ `document`), refetch post-upload (la boucle infinie ne peut plus se reproduire), les 3 CTA rebranchés, boucle résiduelle « centre couvert par un claim en attente » fermée, `/centre/[id]/claim` élargi PDF/JPG/PNG.
5. **Emails non bloquants** (`2a07b7b`) : `sendVerificationEmail` / `sendHebergeurAccountPending` en fire-and-forget, porte de sortie `resendVerification` vérifiée de bout en bout (bouton login inclus).

**Recette prod 13/07, validée de bout en bout** : inscription ex-nihilo (`ZZZ RECETTE 13-07`) → dépôt justificatif (checklist bascule « en cours d'examen » SANS reload) → validation admin → **devis DEV-2026-0001 créé et ENVOYÉ à un destinataire externe**. Le gate `assertEnvoiExterneAutorise` est levé en réel — pas seulement son miroir `envoisBloques`.

**Reste ouvert (hors périmètre du chantier)** : le **routage cas 2** (invitation admin pré-remplie routée vers `/auth/register/hebergeur` → PENDING, au lieu de `/centres/register` → ACTIVE ; le code ACTIVE du cas 2 reste mort côté UI). `motDePasseDefini` a été corrigé en amont précisément pour que ce chantier ne réveille pas le bug de reconnexion. Voir aussi 🟡 DETTE 13/07 §2 (workflow admin) et 🟠 TRIAL 30J.

---

## 🔴 DÉPLOIEMENT FRONTEND SILENCIEUSEMENT CASSÉ 12→13/07 (résolu `88e49ec` — LEÇON À RETENIR)

**Découvert par accident** pendant le chantier ex-nihilo — hors sujet initial, mais bloquait TOUT : plus aucun déploiement frontend ne passait depuis le **12/07 15h42**. Trois deploys consécutifs en `timeout-error` (`ae4c498`, `dc845a2`, `2a07b7b`). **La prod servait du code périmé, SANS ALERTE** : le site répondait 200 (ancien container), les builds réussissaient, seul le boot échouait.

**Cause prouvée** : Next 16 standalone génère `const hostname = process.env.HOSTNAME || '0.0.0.0'` et Scalingo injecte `HOSTNAME=<nom-du-container>`. Next binde donc **uniquement l'IP du container**. Le trafic routé passe (il vise cette IP), mais la sonde de boot Scalingo (loopback) ne voit rien → timeout 60 s → SIGTERM. **Preuve en one-off dans le runtime Scalingo** : `LOOPBACK_REFUSED` / `HOSTNAME_OPEN` ; contre-épreuve `HOSTNAME=0.0.0.0` → `LOOPBACK_OPEN`. (Les 3 commits déclencheurs étaient doc-only : le code n'a jamais été en cause.)

**Fix** (`88e49ec`) : Procfile → `web: HOSTNAME=0.0.0.0 node .next/standalone/server.js`, et les deux `cp -r` sortent du boot (budget 60 s) vers un hook npm `postbuild` (`scripts/prepare-standalone.mjs`, cross-platform). Déploiement suivant : **success**, vérifié.

**⚠️ LEÇON À RETENIR** : un déploiement peut échouer en silence pendant 24 h sans qu'aucun client ne s'en aperçoive — l'ancien container continue de servir — pendant que le backend, lui, continue de se déployer. **Backend et frontend divergent alors sans alerte** (seul signal faible : les erreurs « Failed to find Server Action » des clients au bundle périmé). **VÉRIFIER `scalingo deployments` APRÈS CHAQUE PUSH.**

---

## 🟡 DETTE IDENTIFIÉE LE 13/07 — à traiter à froid (aucun client impacté)

Trois constats issus du chantier inscription ex-nihilo. Aucun ne bloque personne aujourd'hui ; les trois sont des bombes à retardement.

### 1. ✅ RÉSOLU 14/07 — `createCentre` (le bug réel était AILLEURS)

**RÉSOLU par `6b5bb4a` + `9724d2a`.** L'analyse ci-dessous (atomicité) était **exacte mais incomplète** — elle décrivait la moitié du problème. Le vrai bug, trouvé en lisant les deux listes admin :

> Un hébergeur **déjà validé** ajoute son 2e centre. Le SIRET est **optionnel**, il ne le remplit pas. La dédup tombe sur `nom + ville` : le nom du **chalet** ne matche pas le nom de son **organisation** → **organisation neuve** + membership `NON_APPLICABLE`. Le centre PENDING n'apparaît alors ni dans `/admin/claims` (qui ne liste que les `EN_ATTENTE_*`), ni dans `/admin/centres/pending` (qui exige un membership `VALIDE` sur l'org). **Invisible → jamais activable → hébergeur bloqué sans issue.**

**Invariant posé** : *tout centre créé en PENDING est visible dans au moins une liste admin.*

**Fix à la source** (une transaction interactive, calquée sur `registerHebergeur`) :
- l'organisation est résolue **par la DONNÉE, plus par le chemin** : (1) SIRET saisi → dédup SIREN ; (2) sinon membership VALIDE **le plus ancien** (`orderBy claimValidatedAt asc`) ; (3) sinon dédup textuelle / création ;
- `claimStatut` dérivé de la **relation user × organisation résolue** (et non du chemin) ;
- `try/catch` avaleur supprimé → rollback complet ; mapping P2002/P2000 ; email admin via `process.env.ADMIN_EMAIL`.

**Contrôle prod** : **0 centre PENDING en base** → aucun rattrapage. Corollaire : le parcours « ajouter un centre » **n'a jamais servi en production** — le premier vrai utilisateur aurait tout découvert à notre place.

⚠️ **`claimFromCatalogue` porte le MÊME défaut** (membership VALIDE prime sur toute autre résolution, `findFirst` sans `orderBy` → organisation arbitraire si double membership). **NON TOUCHÉ volontairement** : c'est le chemin que Louise emprunte pour Chambéret cette semaine. → **à traiter dans un lot dédié, après recette.**

---

**Analyse d'origine (13/07), conservée pour mémoire :**

`centre.service.ts:295-297` : le `try/catch` autour de `findOrCreateOrganisation` fait `console.error` **puis continue**. C'est exactement le pattern qu'on vient de supprimer de `registerHebergeur` (chantier atomicité du 13/07).

Conséquence : un hébergeur existant qui ajoute un centre peut se retrouver avec un **centre orphelin sans organisation ni membership**. Il n'est pas détruit (le compte existe déjà), mais son centre est dans un état bâtard. Le cas est déjà couvert côté UX par l'endpoint (B) `upload-justificatif`, qui n'exige aucune organisation — mais la donnée reste sale.

**Fix** : même traitement que `registerHebergeur` — transaction interactive `centre.create` + `findOrCreateOrganisation` + `centre.update(organisationId)` + `findOrCreateMembership`. Les helpers acceptent déjà `Prisma.TransactionClient` (fait le 13/07).

### 2. Trois implémentations divergentes du workflow admin de validation

| Source | Méthodes | Exposé par | Règle |
|---|---|---|---|
| `claim.service.ts` | `getClaimsEnAttente`, `validerClaim`, `refuserClaim` | `/admin/claims/*` | **strict** : `validerClaim` refuse `EN_ATTENTE_DOCUMENT` |
| `centre.service.ts` | `getClaimsPending`, `validateClaim`, `getCentresPending`, `validateCentrePending` | `/centres/admin/*` | **permissif** : accepte `EN_ATTENTE_DOCUMENT` |
| `admin.service.ts` | `getCentresPending` (3ᵉ version), `activerCentre` | `/admin/centres/*` | filtre sur `organisation.memberships.some(VALIDE)` |

⚠️ **« CORRECTION 14/07 » — RÉTRACTÉE LE MÊME JOUR. LA ROADMAP AVAIT RAISON, PAS MOI.**

J'ai écrit ici, le 14/07 au matin, que l'affirmation ci-dessus était « FAUSSE », après avoir lu **un seul** des deux écrans admin (`/dashboard/admin/claims`). Je n'avais **jamais lu** `/dashboard/admin/page.tsx`. Claude Code a refusé d'exécuter le lot 3 et a fourni le grep : le dashboard admin PRINCIPAL consommait bien `/centres/admin/*`, via deux onglets entiers (`claims-centres`, `centres-pending`) et 4 helpers de `lib/admin.ts`. **L'affirmation d'origine était exacte.**

→ **Leçon** : une lecture partielle produit une correction fausse, qui corrompt la source de vérité elle-même. Ne jamais « corriger » un doc sans avoir lu **tous** les appelants.

---

## ✅ RÉSOLU 14/07/2026 (après-midi) — UNIFICATION SUR L'ÉCRAN STRICT

**Le problème réel n'était pas « du code mort » : c'était DEUX ÉCRANS ADMIN divergents.**

| | `/dashboard/admin` (principal) | `/dashboard/admin/claims` (sous-page) |
|---|---|---|
| Routes | `/centres/admin/*` (centre.service) | `/admin/*` (claim.service + admin.service) |
| Valider un claim | **PERMISSIF** : accepte `EN_ATTENTE_DOCUMENT` → **validation SANS justificatif** ; `updateMany({ userId, statut: PENDING })` → active **tous** les centres du user, **toutes organisations confondues** | **STRICT** : bouton désactivé tant que le justificatif manque ; limité à l'organisation du membership |

Le justificatif est la **seule barrière anti-usurpation** (revendiquer le centre d'un tiers). L'écran principal permettait de la contourner, sans trace ni avertissement.

**Décision Théo** : un seul écran, le strict. Il confirme n'avoir **jamais** validé de claim sans justificatif → aucune capacité à préserver.

**⚠️ Proposition de CC REFUSÉE** : il suggérait d'« assouplir `claim.service.validerClaim` » pour qu'il accepte `EN_ATTENTE_DOCUMENT` et préserve le comportement existant. **NON** — ce refus n'est pas un bug, c'est le contrôle de sécurité. L'assouplir aurait rouvert le trou fermé cette semaine.

**Livré (3 commits, poussés, recettés en prod) :**
- `3df072c` — **additif** : `admin.service.refuserCentre(centreId, motif?)` + `PATCH /admin/centres/:id/refuser`. **Fix au passage** : le refus d'un centre était **totalement SILENCIEUX** (`validateCentrePending` passait le centre en SUSPENDED sans aucun email). L'hébergeur était bloqué sans savoir pourquoi. Désormais : email motivé, calqué sur `claim.service.refuserClaim`.
- `c1e2dde` — **frontend** : `handleRefuserCentre` aligné sur `handleRefuser` (prompt motif) et rebranché sur la nouvelle route ; **suppression des 2 onglets permissifs** du dashboard principal (~320 lignes) ; remplacés par une carte « N dossier(s) à valider → » (compteur = `/admin/claims` + `/admin/centres/pending`) ; 4 helpers + 2 types supprimés de `lib/admin.ts`.
- `8267482` — **suppression backend** : les 4 routes `/centres/admin/*` et les 4 méthodes de `centre.service` (`getClaimsPending`, `validateClaim`, `getCentresPending`, `validateCentrePending`). **Les routes sont supprimées CÔTÉ SERVEUR**, pas seulement masquées dans l'UI → la surface qui permettait de contourner le justificatif n'existe plus du tout.

⚠️ **PIÈGE — HOMONYMIE** : `admin.service.getCentresPending` porte le **même nom** que la méthode supprimée de `centre.service`, mais c'est une **autre méthode, bien vivante** (consommée par l'écran strict et par le harness d'intégration). À ne jamais confondre.

**Recette prod (14/07, 17h43)** : centre `RECETTE 14-07` créé **sans SIRET** → rattaché à **Chalet Le Sauvageon** (organisation existante, **pas** d'organisation fantôme) → visible dans l'admin → refusé avec motif « TEST » → **email reçu avec le motif**. Les deux fixes de la journée validés d'un coup.

---

**Analyse d'origine (13/07), conservée :**

Différences réelles entre `centreService.validateClaim` et `claimService.validerClaim` : la première ne touche ni `compteValide`, ni `emailVerifie`, ni `isPrimary`, **n'envoie aucun email à l'hébergeur**, et active *tous* les centres PENDING du user (toutes orgs) là où l'autre se limite à l'organisation du membership.

⚠️ Les deux `getCentresPending` (centre.service vs admin.service) **ne renvoient pas la même liste** — c'est pour ça que certains centres apparaissent dans un onglet et pas dans l'autre.

**Décision prise (non exécutée)** : `claim.service` devient la source unique. Ordre impératif : (1) assouplir `claimService.validerClaim` pour accepter `EN_ATTENTE_DOCUMENT` sur action admin explicite — **sinon on supprime la seule capacité de débloquer un hébergeur ex-nihilo** ; (2) rebrancher `/centres/admin/*` en préservant URLs **et** shapes de réponse (le dashboard admin ne doit pas bouger) ; (3) supprimer les méthodes de `centre.service`. Ne jamais inverser cet ordre.

### 3. Node 20 en fin de vie — le build va casser — ✅ RÉSOLU (vérifié 2026-08-24)

Le buildpack Scalingo avertit à chaque déploiement :

> *Node.js 20.20.2 is now End-of-Life (EOL). […] In a future buildpack release, this warning will become a build error.*

~~`frontend/package.json` → `"engines": { "node": "20.x" }`.~~ **Bumpé** : `frontend/package.json` déclare désormais `"node": "24.x"`. Plus d'échéance EOL bloquante.

---

## ✅ RÉSOLU 14/07/2026 — TRIAL 30J (ex-🟠, identifié 13/07)

**RÉSOLU par `0a23953`** — et le problème était plus large que décrit ci-dessous : il n'y avait pas *un* déclenchement fautif, mais **4 comportements divergents** selon le chemin d'activation :

| Chemin | Avant | Après |
|---|---|---|
| `auth.login` | Pilotage 30j, **sans regarder `centre.statut`** | source unique, **garde `statut ACTIVE`** |
| `admin.validerHebergeur` | **COMPLET** 30j, **sans `trialStartedAt`** (invisible du cron) | source unique |
| `claim.validerClaim` | **aucun essai** | source unique |
| `admin.activerCentre` | **aucun essai** | source unique |

**Fix à la source** : `demarrerOuAlignerTrial(prisma, email, userId)` dans `trial.helper.ts` (fonction pure, `email` typé structurellement → aucun cycle de module), appelée aux 4 sites. Politique validée :
- compte payant (mandat Mollie **ou** `modePaiement VIREMENT`) → aucun essai ;
- abonnement offert / manuel (ACTIF sans trial ni mandat — Sauvageon, Alticlub, Pôle Montagne) → aucun essai ;
- **essai en cours → ALIGNEMENT** du nouveau centre sur la même date de fin (jamais de prolongation) ;
- **essai expiré → le centre suivant est payant** (décision Théo) ;
- sinon → Pilotage 30j.

**Cascade évitée** : sans l'alignement, la revendication du 2e centre de Louise (Chambéret) aurait ouvert un **2e essai** à une date différente de Valloire (08/08). L'essai serait devenu **renouvelable à l'infini** en revendiquant un centre de plus.

**Ni l'option A ni l'option B ci-dessous n'ont été retenues** — les deux traitaient le symptôme sur un seul chemin. Analyse d'origine conservée ci-dessous pour mémoire.

---

**Symptôme (analyse du 13/07)** : le trial 30j Pilotage s'active au **premier login**, alors que le centre est encore `PENDING` et le claim en `EN_ATTENTE_DOCUMENT`. L'hébergeur voit son essai gratuit s'écouler **sans pouvoir rien faire** — `envoisBloques` reste `true` tant que le centre n'est pas `ACTIVE` : ni devis, ni email externe.

**Constaté en recette prod (13/07)** : compte `recette-exnihilo-13-07` — inscription 14h51, **trial démarré 14h54** (email « Nouveau trial », expiration 12/08), justificatif déposé 14h55, claim validé ~15h30. Entre 14h54 et 15h30, le compte à rebours tourne dans le vide. Sur un vrai prospect validé le lundi après une inscription du vendredi soir, c'est **10 % de l'essai perdu** sans avoir touché au produit. Jamais validé → 100 % perdu, sans que le prospect comprenne pourquoi.

**Cause on-code** : `AuthService.activerTrialPremiereConnexion()` (`auth.service.ts`) filtre sur `trialStartedAt: null`, `mollieMandatId: null`, `abonnementStatut: INACTIF`. **Aucune condition sur `centre.statut`.** Un centre `PENDING` déclenche donc son trial au premier login.

**Non détecté avant** : tous les hébergeurs existants (Sauvageon, Choucas, Pôle Montagne, Louise/PULSE) ont été activés à la main, hors du parcours nominal. Le premier ex-nihilo réel sera le premier à le subir.

**Deux options, à trancher à froid** :
- **A. Démarrer le trial à l'activation du centre** (`claim.service.validerClaim` / `admin.service.activerCentre`) plutôt qu'au login. Le plus juste métier. ⚠️ Vérifier d'abord qu'aucun autre chemin ne dépend du déclenchement au login (magic link, refresh token, invitation).
- **B. Ajouter `statut: 'ACTIVE'` au `where` de `activerTrialPremiereConnexion`.** Une ligne. Le trial démarre au premier login **après** activation. Moins élégant (le trial peut ne jamais démarrer si l'hébergeur ne se reconnecte pas), mais quasi sans risque de régression.

**Priorité** : à traiter avant le premier vrai self-signup hébergeur. Pas de client impacté aujourd'hui.

---

## ✅ RECETTE — lots du 14/07 : BACKEND PROUVÉ + PROD VALIDÉE À LA MAIN

**✅ 1. Recette backend automatisée — T1→T7 TOUS VERTS** (harness `backend/scripts/integration-trial-centres.mjs`, commit `8b1f764`).
Postgres 16 **jetable** en Docker (le harness refuse de démarrer si `DATABASE_URL` ne pointe pas la base jetable), schéma via `prisma db push`, **vrais** `CentreService`/`AdminService`/`ClaimService` consommés depuis `dist/`, vraies contraintes SQL. Seul `EmailService` mocké. Container détruit après run.

| Test | Résultat |
|---|---|
| T1 `createCentre` sans SIRET | ✅ centre PENDING rattaché à l'organisation existante ; **aucune org neuve** ; membership VALIDE intact ; aucun essai |
| T2 visibilité admin | ✅ sort dans **`/admin/centres/pending`** |
| T3 activation + alignement | ✅ `trialStartedAt` et `abonnementActifJusquAu` **identiques** au centre existant ; **aucune prolongation** ; aucun essai 30j ouvert |
| T4 essai expiré | ✅ centre ACTIVE **sans essai** (DECOUVERTE / INACTIF) |
| T5 gardes clients | ✅ Mollie / VIREMENT (Choucas) / abonnement offert (Sauvageon) → **aucun essai** posé |
| T6 seconde société | ✅ SIREN différent → organisation **distincte** + `EN_ATTENTE_DOCUMENT` |
| T7 atomicité | ✅ P2000 en transaction → **0 org, 0 centre, 0 membership** : rollback total |

⚠️ **Lecture honnête de T4** : « payant » signifie en réalité **`DECOUVERTE` / `INACTIF`** — le plan gratuit bridé, **pas** une facturation. **Rien ne dit à l'hébergeur qu'il doit souscrire.** À requalifier au premier cas réel.
→ Le harness dépend du `dist/` (build préalable obligatoire) → candidat item **4.8 CI minimale**.

**✅ 2. Recette manuelle prod — FAITE le 14/07 à 17h43**
Centre `RECETTE 14-07` créé depuis l'UI hébergeur, **SIRET laissé VIDE** :
- → rattaché à **« Organisation : Chalet Le Sauvageon »** (affiché à l'écran) — **aucune organisation fantôme** ✅
- → **visible** dans `/dashboard/admin/claims` → section « Nouveaux centres à valider » ✅ *(avant ce matin : invisible, donc jamais activable)*
- → **Refuser** + motif « TEST » → **email reçu avec le motif** ✅ *(avant cet après-midi : refus totalement silencieux)*
- → carte « Dossiers à valider → » du dashboard principal fonctionnelle ✅

**❌ Reste à faire :**
1. **Purger `RECETTE 14-07`** (centre en SUSPENDED, invisible partout, sans impact — mais à nettoyer) :
   `BEGIN; SELECT id, nom, statut FROM centres_hebergement WHERE nom = 'RECETTE 14-07'; DELETE FROM centres_hebergement WHERE nom = 'RECETTE 14-07'; COMMIT;`
   ⚠️ **NE PAS** toucher au membership ni à l'organisation (ceux de Sauvageon).
2. **Purge S3** : objets `kbis/` de la recette ex-nihilo du 13/07 (bucket `liavo-uploads`).
3. **🔴 `scalingo pgsql-console` ne s'ouvre plus** (voir bloc dédié ci-dessous).

---

## ✅ RÉSOLU 16/07/2026 — ACCÈS SQL PROD (c'était la CLI périmée)

**Cause réelle** : CLI Scalingo **1.44.1 périmée** → `db-tunnel` figeait silencieusement. **Fix** : mise à jour 1.47.0 (réinstall manuelle cli.scalingo.com). Tunnel OK, `SELECT now()` validé en prod le 16/07.

**Ni** la box pro du bureau (test 4G négatif), **ni** psql/PATH (17.9 OK), **ni** la clé SSH `id_rsa`. La note du 14/07 « pgsql-console fonctionne (pipe stdin) » était **fausse depuis PowerShell**.

**Procédure réutilisable** (2 fenêtres : `db-tunnel` + `$env:PGPASSWORD` puis `psql -h 127.0.0.1 -p 10000 -U liavo_backe_8243 …`) → détaillée dans SESSION_STATE 16/07.

⚠️ **Reste** : régénérer le mot de passe DB (il a circulé pendant le debug) — dashboard → PostgreSQL → Settings → Reset password.

<details>
<summary>Ancien diagnostic (14/07) — conservé pour archive</summary>

## 🔴 ACCÈS SQL PROD CASSÉ (14/07) — non résolu

**Symptôme** : `scalingo -a liavo-backend pgsql-console` et `db-tunnel` ne renvoient **RIEN** (aucune sortie, aucune erreur). `echo`, `psql --version` et `scalingo login --api-token` fonctionnent.

**Point commun des commandes qui échouent** : elles doivent **contacter le réseau / ouvrir un tunnel SSH**. Celles qui rendent la main immédiatement fonctionnent.

**Cause éliminée** : `psql` était absent du PATH → **corrigé en dur** (PATH utilisateur + `C:\Program Files\PostgreSQL\17\bin`). `psql --version` → 17.9 ✅. **Le problème persiste**.

**Pistes non instruites** : CLI Scalingo en **1.44.1** (warning « out-of-date, update to 1.47.0 » à chaque commande) — la plus probable ; clé SSH `id_ed25519` / agent SSH ; port 22 sortant bloqué ; proxy / antivirus.

**Contournement connu** : la console SQL est aussi accessible **depuis le dashboard Scalingo** (app → Addons → PostgreSQL), sans terminal ni tunnel.

**Bloquant à terme** : tout SQL prod passe par là (rattrapages, backfills, purges).

</details>

---

## 🟠 CONTRE-TEST EN ATTENTE — multi-centre sur les exports (à passer au 2e centre de Louise)

**Contexte** : le 12/07, les 3 exports de Pilotage → Comptabilité (CSV factures, CSV versements, ZIP PDF) sont passés de `<a href download>` à axios, ce qui rétablit l'envoi du header `X-Centre-Id`. **Avant ce fix, tout hébergeur multi-centre exportait TOUJOURS son premier centre, silencieusement** (Pôle Montagne = 2 centres actifs).

**Ce qui est prouvé** : `localStorage['liavo-centre-actif']` est bien renseigné (même en mono-centre) et l'interceptor axios pose le header → le maillon cassé est refait. **Ce qui ne l'est pas** : le changement effectif de contenu quand on bascule de centre — impossible à tester sur Sauvageon (mono-centre : le fallback « premier centre possédé » donne le bon résultat par accident, le test serait aveugle).

**→ Dès que le 2e centre de Louise (Chambéret / Corrèze) existe** : basculer de centre via le `CentreSelector`, relancer les 3 exports, **vérifier que le contenu change**. Si non, le fix n'a pas pris. Ne pas laisser une cliente le découvrir à notre place.

---

## 0. État du produit au 18/06/2026

### Ce qui est en production et fonctionne

**Cœur produit (~90% livré)** : flow séjour DIRECT complet (création → devis → signature → facturation acompte/solde/avoir → envoi email PDF), flow COLLABORATIF (invitation → acceptation → espace partagé), facturation Factur-X EN 16931, arrondi TTC-first systémique, ajustement devis post-acompte, CRM hébergeur (pipeline dérivé automatique du devis le plus avancé, contacts, rappels, activités, kanban), planning couleurs par statut (5 états PMS), page liste séjours avec filtres/recherche/badges non-lus, module rentabilité (factures prestataires, ventilation par séjour, marges), page équipe/collaborateurs (permissions par module), dashboard global multi-centre, dashboard réseau (KPIs, scoring, invitation), import APIDAE (81 LMDJ + 54 IDDJ), catalogue public, landing page, autorisations parentales, journal séjour parents, page abonnement (frontend, non connectée au PSP).

**Architecture UX séjour (~70% livré)** : 4 composants extraits (SejourHeader, TabDevisFacturation, TabNotes, TabParticipantsSaisieDirecte), migrations schema faites (notesInternes sur Sejour, sejourId sur Rappel et ActiviteClient), page liste séjours livrée.

**Infra** : Scalingo Paris (2×M + PG Starter 512M = ~36€ HT/mois), OVH Object Storage Gravelines, Brevo FR, DNS OVH. Stable.

### Ce qui ne fonctionne PAS encore

- **Sécurité** : LOT 0 fait (trust proxy + throttle). LOTs 1 à 5 = 0 ligne de code. IDOR critiques ouverts.
- **Monétisation** : 0€ de revenu. Pas de PSP, pas de gating, pas de PlanGuard. Enum PlanAbonnement manque PILOTAGE.
- **Intégrations** : aucune (pas d'iCal, pas d'export CSV, pas de webhooks, pas de Chorus Pro service).
- **Notifications** : l'hébergeur ne reçoit pas de notification quand l'organisateur poste un message collab.
- **Labels** : termes scolaires encore présents dans le body de page.tsx séjour (TIER1 Ch.4 non terminé).

### Données production

- ~63 séjours/événements (Sauvageon)
- ~40 factures émises
- 1er client signé hors Sauvageon : Les Choucas (2 mois gratuits, plan Complet)
- 3 centres Pôle Montagne actifs (trial 6 mois)
- 81 centres LMDJ importés, 54 centres IDDJ importés
- Compte démo réseau : `demo-lmdj@liavo.fr` / `LMDJ2026!`

### Échéances

| Date | Événement |
|---|---|
| 30/06/2026 | CA LMDJ — Marie porte le dossier LIAVO |
| 01/09/2026 | Obligation réception e-invoicing (toutes entreprises) |
| ~Sept 2026 | Potentiel démarrage pilote LMDJ (si accord CA) |
| 31/12/2026 | Clôture 1er exercice LIAVO SASU |
| 01/09/2027 | Obligation émission e-invoicing (PME) |

---

## 1. Priorités P0 — Bloquant adoption (faire IMMÉDIATEMENT)

### 1.1 ~~Notif hébergeur messages collaboratifs~~ — ✅ LIVRÉ

### 1.2 ~~LOT 1 sécurité — IDOR ownership helper~~ — ✅ LIVRÉ

### 1.3 ~~LOT 3 sécurité — Storage privé + URL signées~~ — ✅ LIVRÉ

**Total P0 : TERMINÉ.**

---

## 2. Priorités P1 — Forte valeur (semaines 2-4, avant ou juste après CA LMDJ)

### 2.1 ~~LOT 2 sécurité — Auth hardening JWT refresh~~ — ✅ LIVRÉ

### 2.2 ~~Migration enum PILOTAGE~~ — ✅ LIVRÉ

### 2.3 ~~PSP Mollie SEPA~~ — ✅ LIVRÉ (mode live, webhook validé, première échéance Choucas ~17/07)

### 2.7 ~~Refonte planning ↔ groupes many-to-many~~ — ✅ LIVRÉ (07/07)

Table de jointure `PlanningActiviteGroupe`, refonte `genererPlanningIA` (1 activité par cluster au lieu de N), UI multi-select dans TabPlanning, PDF adapté. Anne (Choucas) et Yves (Pôle Montagne) peuvent maintenant construire leurs plannings 6 mois avant les inscriptions, avec activités rattachées à plusieurs groupes (ex: "Escalade Groupe 1 + Groupe 3"). Voir SESSION_STATE 07/07 + `docs/audit-planning-cascade.md`.

### 2.4 TIER1 Ch.4 — Labels universels — ~1j (amorcé)

**PARTIEL.** Amorce 08/07 : 2 libellés scolaires neutralisés dans `PricingTable.tsx` (« Signature électronique directeur » → « en ligne », « Espace collaboratif hébergeur + enseignant » → « partagé »). **Reste** : les termes scolaires dans le body de `sejour/[id]/page.tsx` en contexte EVENEMENT.

### 2.5 ~~Export CSV factures~~ — ✅ LIVRÉ (Comptabilité dans module Pilotage, BOM UTF-8)

### 2.8 ~~Export ZIP des factures PDF~~ — ✅ LIVRÉ (12/07)

`GET /pilotage/export/factures-pdf[/preview]` (`@RequirePlan('COMPLET', strict)`). ZIP des PDF Factur-X déjà stockés sur OVH + `_factures.csv` en index + `_PDF_MANQUANTS.txt` si des PDF manquent (deux causes distinguées : jamais généré / introuvable au fetch). `StorageService.zipFromUrls()` **générique** (`{nom, url}`) — l'extension aux **factures prestataires** (`FacturePrestataire.fichierUrl`, le comptable veut les achats autant que les ventes) coûtera ~30 lignes. **Zéro dépendance ajoutée** (`pizzip` déjà présent), compression STORE, fetch par lots de 5, plafond dur 300 factures.

**Effets de bord assumés** : les **avoirs** sont désormais inclus dans l'export CSV factures (un avoir est une pièce comptable — son exclusion rendait le CSV faux) + **colonne `Type`** ajoutée. Les 3 exports passent par axios (fix multi-centre, cf. bloc 🟠 ci-dessus).

**Recette prod** : Sauvageon 62 factures / 62 PDF, ZIP ~10 s. **Non recetté en prod** : les avoirs (0 en base) → couverts par tests unitaires uniquement, à revérifier au premier avoir émis.

### 2.6 LOT 4a — Cookie httpOnly — EN PAUSE

Reverté. Root cause : axios 1.13.6 + turbopack fetch adapter ne forward pas `credentials: 'include'` cross-origin. Solution recommandée : Next.js rewrites. Helmet livré.

**Total P1 restant : 2.4 labels (~1j) + 2.6 httpOnly (~1j quand Next.js rewrites en place).**

---

## 3. Priorités P2 — Nice to have (juillet-août)

| # | Chantier | Effort | Trigger |
|---|---|---|---|
| 3.1 | ~~LOT 5 — Purge IBAN git~~ | ✅ | LIVRÉ |
| 3.2 | Refonte page Devis envoyés (tableau filtrable/triable) | 2-3j | Quand 100+ devis en base |
| 3.3 | Flux iCal lecture seule (`GET /centres/:id/calendar.ics`) | 0.5j | 1er hébergeur qui demande |
| 3.4 | SC7 — Notif centres APIDAE non inscrits | 2-3h | Après validation commerciale LMDJ |
| 3.5 | Concept Réponse PDF (adhérents LMDJ Découverte) | 1j | Si accord CA LMDJ |
| 3.6 | Fix "dont X€ via réseau" (mauvais fichier global→hebergeur) | 0.5j | Quick fix |
| 3.7 | Intégration APIDAE LMDJ (1 ligne dans syncApidae) | 15min | Quand credentials Amandine reçus |
| 3.8 | SSO APIDAE OAuth2 | 0.5j | Quand credentials APIDAE Connect reçus |
| 3.9 | Onboarding première connexion (flag premiereConnexion) | 1-2j | Avant ouverture grand public |
| 3.10 | ~~Responsive mobile~~ | 1 nuit + fixes | — | ✅ LIVRÉ (Fable 5 overnight, drawer mobile, planning responsive, sweep ~60 pages, déployé 03/07) |
| 3.11 | ~~Multi-photos catalogue centre (galerie fiche)~~ ✅ FAIT (vérifié 2026-08-24 : `imagesUrls String[]` dans `schema.prisma`, invariant `imageUrl = imagesUrls[0]`) | 1-2j | Remonté par Louise/PULSE 16/07. Aujourd'hui `CentreHebergement.imageUrl` = **une seule** photo (pas de table `photos_centre`, pas de champ tableau) → fiche pauvre pour un organisateur qui compare. Option reco = champ `imagesUrls String[]` (`imageUrl` reste la couverture, rétrocompatible, 1 seul ALTER). Candidat Fable 5 overnight. Stopgap zéro-dev à dire au client : mettre les photos dans la **brochure PDF** de la fiche (`brochureUrl` existe). |

---

## 4. Priorités P3 — Dette technique (selon trigger)

| # | Chantier | Effort | Trigger | Statut |
|---|---|---|---|---|
| 4.1 | ~~Modules devis partagés~~ | 1 nuit | — | ✅ LIVRÉ (3 modules extraits : devis-calculs.ts, useDevisLignes.ts, DevisEditor.tsx — 949 ins / 982 del, delta -33 lignes). Les 3 fichiers DevisBuilder restent séparés mais partagent la logique commune. Merge complet → quand prochaine modif devis. |
| 4.2 | ~~DashboardShell unification~~ | 1 nuit | — | ✅ LIVRÉ (HebergeurShell + TopBarShell, dashboard/layout.tsx routeur de shell, 11 commits + 1 fix). Suivi : double bandeau sous-pages organisateur/admin à nettoyer. |
| 4.3 | ~~Découper page.tsx séjour~~ | 1 nuit | — | ✅ LIVRÉ (194KB → 20KB, -86%, 9 composants extraits : TabMessages, TabPlanning, TabGroupes, TabDocuments, TabBudget, TabProjetPedagogique, TabJournal, TabParticipantsCollab, InviteOrganisateurCard). |
| 4.4 | LOT 6 maintenance continue (logs, HTML injection emails, omit Prisma) | Au fil de l'eau | Quand on touche les fichiers | |
| 4.5 | Double bandeau sous-pages organisateur/admin | 0.5-1j | Quand on touche ces sous-pages | Suivi Run 3 DashboardShell |
| 4.6 | ~~escapeHtml notifications.service.ts + collaboration.service.ts~~ | 30min | — | ✅ LIVRÉ (constaté 08/07 : `utils/escape-html.ts` consommé par notifications, collaboration, devis et email.service). Reliquat : supprimer la branche locale `fix/escape-html-emails`. |
| 4.7 | Migration Float→Decimal montants devis/factures | 1-2j | **Audit prod à exécuter** (décision 08/07 : audit seul, sans migrer) | ◐ Étape 1 prête : `docs/AUDIT_FLOAT_DECIMAL.md` (requêtes de contrôle prod lecture seule Q1-Q5 + grille de lecture). Théo exécute en pgsql-console, colle les résultats → décision de migration sur le rapport d'écarts. Coût réel = code (Prisma.Decimal ≠ number). **NE PAS faire en Fable 5 overnight** — données prod en jeu. |
| 4.8 | CI minimale GitHub Actions (tsc + build) | 0.5j | Post-pitch | Diagnostic Fable 5. Actuellement zéro CI, gate = discipline manuelle. Candidat Fable 5 overnight. |
| 4.9 | Tests unitaires code financier (devis-calculs.ts, SequenceService, formule acompte) | 1-2j | Post-pitch | ◐ PARTIEL (08/07) : SequenceService couvert par la suite invariants (13 tests, scopes/monotonie/apercu/P2002) + PlanGuard, centre.helper, trial login, cron alertes — 136 tests backend au total. Reste : devis-calculs.ts (frontend, pas de harness Jest côté front) + formule acompte. |
| 4.10 | ~~Supprimer jspdf (dépendance morte, 0 import)~~ | 5min | — | ✅ FAIT (constaté 08/07 : absent des package.json frontend et backend). |
| 4.11 | ~~npm audit fix ciblé + décision xlsx~~ | 1h | — | ✅ FAIT (08/07) : 13 vulns → 3. `npm audit fix` (axios & co patchés), next 16.1.6→16.2.10 (advisories middleware/cache), `@types/react-pdf` supprimé (dépendance morte, 0 import, portait toute la chaîne pdfjs-dist vulnérable). Résiduel assumé : **xlsx (haute) = risque ACCEPTÉ** (décision Théo 08/07 — parsing limité aux fichiers de l'hébergeur authentifié sur ses propres données) ; postcss (2 modérées) = vendored dans Next, non actionnable sans casser (upstream). |
| 4.12 | ~~Extraction helpers partagés frontend (StatutBadge ×4, KpiCard ×3, formatDate ×35 redéfinitions)~~ | 1 nuit | — | ✅ LIVRÉ (16/07, Fable 5 overnight, **poussé**). 31 définitions dédupliquées, net −55 lignes : formatDate 25/25 → `src/lib/utils.ts` (`formatDate(date, style, fallback?)`, `style` OBLIGATOIRE = pas de défaut silencieux), KpiCard 2/3 → `src/components/KpiCard.tsx`, StatutBadge 4/4 → `src/components/StatutBadge.tsx` (mappings conservés aux sites d'appel). tsc + build verts par famille. Recensement + exceptions : `docs/refacto-helpers-4.12.md`. Sous-fusion assumée (signer T12:00:00 timezone, pilotage/ca KpiCard, ~55 inline JSX, PDF) — exceptions motivées. **2 dettes révélées → 4.22 et 4.23.** |
| 4.22 | Quirk `accent` hex sur KpiCard admin (couleur non appliquée) | 15min | Cosmétique, quand on touche le dashboard admin | Révélé par 4.12. `app/dashboard/admin/page.tsx` passe `accent="#C87D2E"` (couleur hex) là où `KpiCard.accent` attend une **classe CSS** → classe invalide ignorée → la carte censée être ocre s'affiche en gris/noir par défaut. **Pas une régression** (bug préexistant, préservé tel quel par la dédup pure). Fix : soit corriger l'appelant (classe Tailwind au lieu du hex), soit rendre `accent` tolérant au hex (via `style`). |
| 4.23 | Double format de période réseau vs organisateur/hébergeur | 30min | Si besoin d'uniformité visuelle | Révélé par 4.12. `demandePeriode` (`app/dashboard/reseau/page.tsx:26`) et `afficherDatesDemande` (organisateur/hébergeur) produisent des libellés de période légèrement différents (« Janv » vs « Jan », séparateurs, emoji 📅 absent côté réseau, pas de `noteDateFlexible`). Volontairement NON fusionnés par 4.12 (doute → pas de risque). À unifier seulement si l'incohérence visuelle gêne. |
| 4.13 | ~~Extraction DEPT_TO_REGION + statuts devis constants backend~~ | 0.5j | — | ✅ LIVRÉ (16/07, Fable 5 overnight, **poussé**). 3 familles : **Sets statuts devis** 16 littéraux → 3 constantes (`STATUTS_DEVIS_RETENUS` ×9, `STATUTS_DEVIS_ENGAGEANTS` ×5, `STATUTS_DEVIS_EN_COURS` ×2) dans `src/devis/devis-statuts.constants.ts`, composition prouvée identique ; **durée trial** `TRIAL_DUREE_JOURS = 30` dans `trial.helper.ts` (littéral seul, les 2 calculs d'expiration restent distincts) ; **DEPT_TO_REGION** 1/4 copies seulement (voir 4.24). tsc + build + **187 tests** verts par famille (la suite a grandi : 187 + 3 todo, pas 136). Recensement + preuves : `docs/refacto-constantes-4.13.md`. Sous-fusion prudente (modifiables/annulables gardés séparés, faux positifs 30 écartés). **2 suites → 4.24 et 4.25.** |
| 4.24 | Convergence DEPT_TO_REGION Corse/DOM (matching région) | 0.5j | **Décision métier** — avant push national LMDJ/IDDJ | Révélé par 4.13. `public.service.ts` (−Corse −DOM) et `sejour.service.ts` (−DOM) ont une table DEPT_TO_REGION **incomplète** → un centre corse/DOM peut **ne pas être notifié** des demandes ciblées sur sa région. Probablement un **oubli, pas un choix**. NON corrigé par 4.13 (dedup pure ≠ changement de comportement). Impact aujourd'hui **nul** (aucun centre Corse/DOM), mais **trou à combler avant tout réseau national**. Fix = pointer les 2 services sur la table complète centralisée. À arbitrer par Théo. |
| 4.25 | ~~§4.13-bis : dédup Sets de StatutSejour~~ | 15min | — | ✅ LIVRÉ (16/07, Fable 5 overnight, **poussé**). 7 littéraux → **3 constantes** dans `src/sejours/sejour-statuts.constants.ts` : `STATUTS_SEJOUR_CONFIRMES` (CONVENTION, SOUMIS_RECTORAT, SIGNE_DIRECTION, DECLARE_TAM — pilotage ×3 + centre), `STATUTS_SEJOUR_COLLABORATIFS` (CONVENTION, SIGNE_DIRECTION — collaboration ×3), `STATUTS_SEJOUR_DIRECT` (OPTION + COLLABORATIFS, dérivée par spread préservée — collaboration ×2). Le run a trouvé une **3ᵉ composition** non anticipée au recensement §4.13 (DIRECT). tsc + build + 187 tests verts par composition. Sous-fusion prudente (SUBMITTED+CONVENTION+SIGNE_DIRECTION de la rétrogradation, `notIn:['DRAFT']` gardés séparés). Preuves : `docs/refacto-statuts-sejour-4.25.md`. |
| 4.14 | IBAN endpoint public getDevisPublicByToken | — | **Décision : ne pas fixer** | Diagnostic Fable 5. centre.iban exposé via GET /devis/public/:token (sans JWT). Décision 03/07 : le token UUID v4 EST l'auth, l'IBAN est nécessaire pour le PDF devis côté client, le retirer casserait DevisPDFButton sur la page de signature. Risque réel = faible (token indevinable). Le vrai fix IBAN = chiffrement en base (backlog existant). |
| 4.15 | ~~Erreurs `Failed to find Server Action` à chaque deploy~~ | 0.5j | — | ✅ FAIT (08/07, version non-destructive décidée par Théo) : `global-error.tsx` détecte l'erreur Server Action périmée (best-effort sur le message) et affiche « Une nouvelle version de LIAVO a été déployée » + bouton Recharger — **jamais de reload() automatique**. Les autres erreurs gardent le rendu générique. |
| 4.16 | Upgrade @react-pdf/renderer (dépendance fantôme pako) | 0.5j | Post-pitch | 05/07 : @react-pdf/pdfkit 4.1.0 lib/pdfkit.browser.js importe pako/lib/zlib/* sans déclarer pako. Fix actuel = pin pako@1.0.11 exact en dépendance directe (commit 8a60079). Vérifier si release react-pdf récente déclare pako correctement → retirer le pin. NE PAS passer pako en ^2/^3 (exports field bloque les subpaths). |
| 4.17 | Qté 0 affiche "0" dans PDF devis (lignes option Alticlub) | 15min | Si Alticlub s'en plaint | Workaround option = ligne qty 0 avec PU TTC visible (livré 05/07). Résidu cosmétique : "0" et "0,00 €" dans qty/totaux. Fix : afficher "—" quand qty=0 dans DevisPDF/FacturePDF/page signer. 3 lignes × 3 fichiers. |
| 4.19 | `claimFromCatalogue` : résolution d'organisation par le CHEMIN, pas par la DONNÉE | 0.5j | **Après recette du 14/07** | 14/07 : même défaut que `createCentre` (corrigé, cf. 🟡 §1) — le membership VALIDE prime sur toute autre résolution, et le `findFirst` est **sans `orderBy`** → organisation **arbitraire** si l'hébergeur a deux memberships VALIDE. Un hébergeur à deux sociétés verra son centre rattaché à la mauvaise structure. **NON corrigé le 14/07 volontairement** : c'est le chemin que Louise emprunte pour Chambéret cette semaine — pas de modification sans recette. Fix : même ordre que `createCentre` (SIRET → membership VALIDE le plus ancien → dédup). |
| 4.20 | Cron : 2 mails d'alerte le même jour pour un compte multi-centre aligné | 15min | Quand un compte multi-centre atteindra J-21 | 14/07 : l'alignement du trial (`0a23953`) donne la MÊME date de fin à tous les centres d'un compte → le cron enverra une alerte **par centre**. Non bloquant, cosmétique. Fix : grouper les alertes par `userId`. |
| 4.21 | `refreshFacturePdf` régénère le PDF d'une facture **déjà émise** | à arbitrer | — | Dette du 12/07, **reconfirmée le 14/07** : un versement enregistré sur une ancienne facture régénère son PDF **et son XML Factur-X** à partir de l'état courant du code. Le snapshot en base (`emetteur_*`, montants) ne bouge pas → pas de dérive de contenu. Mais **un PDF de facture qui change après émission est douteux** vis-à-vis de l'immuabilité d'une pièce comptable. Question comptable, pas technique. |
| 4.18 | Refonte `regrouperParCreneau` dans journal public | 30min-1h | Si un client se plaint des badges groupes absents dans le journal public | 07/07 : la fonction parse les suffixes de titre ("Escalade — G1") pour afficher les badges groupes. Marchait avec l'ancien schéma (activités dupliquées par groupe, suffixe automatique). Ne marche plus avec la refonte m2m (1 activité par cluster, titre sans suffixe, groupes dans une table de jointure). Impact : dégradation cosmétique du journal public pour les nouveaux plannings. Fix : ajouter `groupes` au select backend `journal-public.controller.ts:39` + refondre `regrouperParCreneau` (page.tsx L89-133) pour lire les groupes structurés. Voir `docs/audit-planning-cascade.md` §C.2. |

---

## 5. Chantiers conditionnels (SI accord CA LMDJ)

Ces chantiers ne sont codés QUE si le CA LMDJ du 30/06 donne un accord de principe.

| # | Chantier | Effort | Source |
|---|---|---|---|
| 5.1 | Validation réseau avant dispatch (statut `EN_VALIDATION_RESEAU`) | 2-3j | Débrief Marie §1 — BLOQUANT pour LMDJ |
| 5.2 | Motif obligatoire refus centre + enseignant | 1j | Débrief Marie §3 |
| 5.3 | Multi-classes (niveauClasse string → string[]) | 0.5j | Débrief Marie §4 |
| 5.4 | Split maternelle/PMI (agrementPMI, filtre findOpen) | 1-1.5j | Débrief Marie §5 |
| 5.5 | Ratio demandes/devis par centre (enrichir KPIs réseau) | 0.5j | Débrief Marie §8 |
| 5.6 | Capture demandes 73/74 hors LMDJ sur dashboard réseau | 0.5-1j | Débrief Marie §2 |
| 5.7 | CRM hébergeurs côté réseau (prospection adhérents) | 2-3j | Débrief Marie §7 |
| 5.8 | Pricing bundlé LMDJ (business decision, pas du code) | — | Débrief Marie §10 |

**Total conditionnel : ~10j si tout est fait. Échelonnable en 3 sprints.**

---

## 6. Chantiers hors scope été (backlog)

Documentés pour mémoire. Ne pas commencer avant PMF.

- ~~Module Pilotage enrichi~~ — ✅ LIVRÉ (CA, remplissage, comparaison N-1, ventilation produit)
- Chorus Pro NestJS service (habilitation AIFE, ChorusProService)
- Webhooks événementiels (1-2j)
- Flow "Transmettre au gestionnaire" facture (token public, 2-3j)
- SC-MULTI-ORGANISATEURS (SejourCollaborateur)
- SC-PDF-DEVIS-EXTERNE (typeDevis UPLOAD_EXTERNE)
- SC-IMPORT-PARTICIPANTS (CSV Pronote)
- Intégration PMS (Mews, Amenitiz) — V2+
- ~~Convention configurable par centre~~ — ✅ LIVRÉ (couverture LIAVO + PDF centre mergé via pdf-lib)
- ~~Contrat événement~~ — ✅ LIVRÉ (guard Sauvageon, contratUrl persisté, exposé client+hébergeur)
- ~~App mobile PWA~~ — Responsive mobile livré 03/07. PWA (manifest + service worker) reste en backlog.
- Marketplace activités
- Appel d'offres transport
- **Grille tarifaire dégressive (besoin Yves/Pôle Montagne, 05/07)** — tarif/élève évoluant selon effectif + gratuités encadrants par palier (cf. fichier Cotation Ste Thérèse : 78 él. = 469€/él. + 6 gratuités → 65 él. = 482€ + 5). Options évaluées : PJ PDF (zéro dev), section grille sur devis (champ structuré + rendu PDF), paliers sur ProduitCatalogue (auto-calcul). Décision 05/07 : reporté, requalifier SI d'autres adhérents LMDJ expriment le besoin. Distinct du besoin "lignes option" Alticlub (résolu autrement).
- Forge française (Gitea OVH) — quand 2e dev ou appel d'offres public

---

## 7. Calendrier cible

```
Semaine 23/06 - 27/06 (avant CA)
├── Jour 1 : TIER1 Ch.1 notif hébergeur (1h) + début LOT 1 IDOR (ownership.helper.ts)
├── Jour 2-3 : LOT 1 IDOR (finir les ~30 call sites + tests)
├── Jour 4-5 : LOT 3 storage privé backend (getSignedUrl, endpoint /files/:key)
│
30/06 ★ CA LMDJ
│
Semaine 30/06 - 04/07
├── LOT 3 storage privé frontend (SecureFile, 14 call sites) + tokens expiration
├── LOT 2 JWT refresh (backend + frontend interceptor)
│
Semaine 07/07 - 11/07
├── Migration PILOTAGE + PSP choix + intégration (3-7j selon PSP)
│
Semaine 14/07 - 18/07
├── PSP fin + PlanGuard + gating frontend
├── Labels universels + export CSV factures
│
Semaine 21/07 - 25/07
├── LOT 4 httpOnly + Helmet
├── Si accord LMDJ : validation réseau (P0 LMDJ)
│
Août
├── P2 au fil de l'eau (iCal, refonte devis envoyés, notif APIDAE)
├── Si LMDJ : chantiers 5.2→5.7
├── LOT 5 purge IBAN
```

---

## 8. Métriques de suivi

| Métrique | Cible fin juillet | Cible fin août |
|---|---|---|
| Findings CRITIQUE restants | 0 | 0 |
| Findings HAUTE restants | ≤ 3 | 0 |
| PSP connecté + 1er paiement test | ✅ | ✅ |
| Centres payants | 1 (Les Choucas) | 3-5 |
| MRR | 49€ | 150-250€ |
| IDOR ownership helper déployé | ✅ | ✅ |
| Storage privé déployé | ✅ | ✅ |

---

## 9. Décisions de référence

| Sujet | Décision | Date |
|---|---|---|
| Grille tarifaire | 0/39/59/79€ HT/mois (Découverte/Essentiel/Complet/Pilotage), remise 17% annuel | 30/06/2026 |
| PSP | Mollie SEPA (EU). Mode live activé. Stripe écarté. | 30/06/2026 |
| CRM pipeline | Dérivé automatique côté frontend, plus de pipeline manuel | 18/06/2026 |
| Sécurité gate dur | LOTs 0-5 TOUS livrés. LOT 6 maintenance au fil de l'eau. | 30/06/2026 |
| Chantiers LMDJ | Conditionnés à l'accord CA 30/06, pas avant | 18/06/2026 |
| Positionnement | LIAVO = infrastructure technique modernisant la centrale, pas remplacement | 10/06/2026 |
| Commission réseau | 10% des abonnements LIAVO dès 2027 | 11/06/2026 |
| IBAN endpoint public | Token UUID v4 = auth suffisante, IBAN nécessaire pour PDF client. Ne pas retirer. Vrai fix = chiffrement en base. | 03/07/2026 |
| Float→Decimal devis | Chantier post-pitch, vérification données prod obligatoire, interdit en Fable 5 overnight | 03/07/2026 |
| TODO abonnement (devis.service:52, sejour.service:981) | Choix commercial conscient : ne pas réactiver tant que flux paiement pas stable (post-17/07 Choucas) | 03/07/2026 |
| Diagnostic dette technique | Source : audit Fable 5 03/07/2026, validé par lecture code MCP. Items intégrés en 4.6–4.14. | 03/07/2026 |
| Lignes option devis (Alticlub) | PAS de champ typeLigne (chantier ~10 fichiers écarté). Solution : ligne qty 0 + colonne PU TTC ajoutée et uniformisée sur DevisPDF, page publique signer, FacturePDF (7 colonnes : Désignation/Qté/PU TTC/TVA%/PU HT/Total HT/Total TTC). PU TTC dérivé du HT stocké, jamais persisté. | 05/07/2026 |
| Build local turbopack (pako) | Root cause : dépendance fantôme pako dans @react-pdf/pdfkit browser build. Fix : pin pako@1.0.11 exact (dépendance directe). 4 boutons PDF restructurés (extraction composant + dynamic import) au passage — amélioration structurelle, pas le fix. Build Scalingo n'a jamais été cassé. | 05/07/2026 |
| Grille tarifaire Yves | Reporté. Découplé du besoin Alticlub. Requalifier si signal d'autres adhérents LMDJ. | 05/07/2026 |
| Planning ↔ groupes m2m | Refonte complète via table de jointure `PlanningActiviteGroupe` (Option A), rejet des Options B (activités dupliquées) et C (array Postgres). `genererPlanningIA` refondu : 1 activité par cluster au lieu de N. Titre simplifié (plus de suffixe " — G1"). Audit exhaustif post-refonte : 0 cascade à fixer. Backfill à 0 confirmé (feature jamais utilisée en prod jusqu'ici). | 07/07/2026 |
| Migration SQL manuelle Prisma | Ne JAMAIS créer de sous-dossier dans `prisma/migrations/` pour autre chose que des migrations Prisma standards (nommage `YYYYMMDDHHMMSS_nom` + fichier `migration.sql`). `prisma migrate deploy` scanne tous les sous-dossiers et plante avec P3015 sinon. Pour les migrations SQL manuelles à référencer dans le repo : `prisma/manual-migrations/` (norme LIAVO) ou `docs/migrations/`. | 07/07/2026 |
| Ordre déploiement SQL + code | Toujours exécuter la migration SQL prod AVANT le push du code correspondant. Sinon fenêtre de risque : le nouveau backend démarre sur une base sans la structure attendue, crash au boot ou au premier `SELECT`. Confirmé 07/07 avec la refonte m2m (crash P3015 pendant ~10 min). | 07/07/2026 |
| xlsx vulnérable (4.11) | Risque ACCEPTÉ, documenté : pas de fix npm, parsing limité aux fichiers uploadés par l'hébergeur authentifié sur ses propres données → exposition réelle faible. Ni migration exceljs ni CDN SheetJS. À requalifier si le parsing s'ouvre à des fichiers tiers. | 08/07/2026 |
| Erreurs deploy Server Actions (4.15) | Version NON-destructive : error boundary (global-error.tsx) détecte l'erreur et propose « Recharger la page » — jamais de reload() automatique (saisie en cours). L'IDDJ n'est pas le trigger, le fix est fait pour lui-même. | 08/07/2026 |
| Float→Decimal (4.7) | Audit d'abord, migration ensuite (ou pas) : requêtes de contrôle prod dans `docs/AUDIT_FLOAT_DECIMAL.md`, décision sur le rapport d'écarts. Jamais en overnight. | 08/07/2026 |
| **Essai gratuit — source unique** | `demarrerOuAlignerTrial` (trial.helper.ts) est le **SEUL** point de démarrage, appelé par les 4 chemins d'activation (login, `validerClaim`, `activerCentre`, `validerHebergeur`). Plus jamais de logique de trial ailleurs. Un centre **PENDING ne consomme jamais** son essai. | 14/07/2026 |
| **Essai multi-centre — alignement** | Un compte = **un** essai. Un nouveau centre s'**aligne** sur la date de fin de l'essai en cours (jamais de prolongation, jamais de 2e essai). Essai **expiré** → le centre suivant est **payant directement**. Empêche l'essai « renouvelable à l'infini » en ajoutant des centres. Le self-service **+14j** (`demander-extension`) reste la seule soupape. | 14/07/2026 |
| **Invariant centre PENDING** | *Tout centre créé en PENDING doit être visible dans au moins une liste admin* (`/admin/claims` **ou** `/admin/centres/pending`). Un centre invisible = un hébergeur bloqué sans issue. À vérifier à chaque modification d'un chemin de création de centre. | 14/07/2026 |
| **Résolution d'organisation** | L'organisation d'un centre se déduit de la **DONNÉE** (SIRET → SIREN), pas du **CHEMIN** (« l'hébergeur est-il validé ? »). Ordre : (1) SIRET saisi → dédup SIREN ; (2) membership VALIDE le plus ancien ; (3) dédup textuelle / création. Le `claimStatut` dérive de la **relation user × organisation résolue**. | 14/07/2026 |
| **SIRET à l'ajout d'un centre** | Reste **OPTIONNEL** (pas de durcissement du formulaire). La règle de résolution ci-dessus suffit à fermer le trou. | 14/07/2026 |
| **Factures Sauvageon à 9 chiffres** | **Option A — ne rien faire.** Les 62 factures (11/06→01/07) portent `emetteur_siret = 953632031` (un SIREN). Le XML les déclarait `schemeID 0002` = SIREN → **cohérent**. Le SIREN identifie correctement l'entreprise, les clients ont payé. Réécrire un snapshot de facture émise (B) ou émettre 62 avoirs (C) : **écartés**. Question posée au comptable. Les **futures** factures sont correctes (backfill + `schemeID` dérivé). | 14/07/2026 |
| **`schemeID` Factur-X** | Dérivé de la **valeur réelle**, jamais codé en dur : 14 chiffres → `0009` (SIRET), 9 chiffres → `0002` (SIREN), longueur aberrante → **bloc omis** (mieux qu'un identifiant mal typé). Échéance réception e-invoicing : **01/09/2026**. | 14/07/2026 |

---

## 10. Chantiers issus du run onboarding backend (07/07 — déployé en prod)

**Livré 07/07** : branche `feat/onboarding-register-trial` (26 commits) mergée et déployée. Compte hébergeur utilisable dès l'inscription (compteValide=true au register, kill switch conservé), trial 30j Pilotage auto à la première connexion (garde `abonnementStatut INACTIF` protège les clients existants), centres PENDING opérables (SUSPENDED seul bloquant), gates anti-phishing `assertEnvoiExterneAutorise` (10 sites, exception destinataire=soi-même), cron alertes branché (`@Cron` 8h Paris, garde `ENABLE_CRON`), justificatif ex-nihilo dans le tunnel claim (membership `EN_ATTENTE_DOCUMENT`), `Devis.dateEnvoi` (migration + backfill), endpoint `GET /centres/onboarding-status`. Recette régression Sauvageon validée 4/4. SQL memberships→VALIDE exécuté (5 clients).

**Livré 08/07** : phase 2 onboarding (§10.11) déployée — branche `feat/onboarding-phase2` (6a `envoisBloques` backend + 6b-1 modale test/réel + 6b-2 encart pré-envoi & message `extractApiError` + 6b-4 « Aller plus loin »). Recette prod 3/3 sur compte ex-nihilo frais. **3 bugs attrapés au smoke-test et corrigés (branches dédiées mergées)** : (1) **permissions PENDING** — `getUserCentrePermissions` gaté sur ACTIVE → proprio d'un PENDING en 403 partout, sidebar cassée pour tout nouvel ex-nihilo ; fix = aligné SUSPENDED-only + spec 6 cas (`fix/permissions-centre-pending`) ; (2) **lien devis** — étape 5 checklist → page suivi sans bouton créer ; fix = `onboarding-status` renvoie `sejour.id`, étape 5 → `/devis/nouveau?sejourDirectId` (`fix/onboarding-devis-link`) ; (3) **double bandeau** claim + PENDING redondants pour ex-nihilo ; fix = masque le claim si un centre PENDING affiche le sien (`fix/onboarding-double-banner`). Le parcours claim (centre catalogue ACTIVE + `userId` posé immédiatement) n'était touché par aucun des trois. **FeatureHint ×4 différé** (code mort sans placement + 4 ancrages diffus 119 KB).

**Livré 08/07 (suite)** : **chantier 10.1 COMPLET, deadline 26/09 NEUTRALISÉE.** Protection Choucas en 2 moitiés — code (enum `ModePaiement`, cron exclut les VIREMENT en null-safe, facture `emettre()` échéance/mention dynamiques, `facturerCentre` prolonge + marque VIREMENT, badge admin, relance admin J-30 virement, self-service `POST /abonnements/demander-extension` +14j + bouton) et données (Choucas marqué `mode_paiement='VIREMENT'` en prod). **Edge cases justificatif soldés** : (a) claim sans fichier et (b) logoUrl null PDF **déjà corrects** (lus sur code, aucun fix) ; (c) pied de checklist **fixé** (`OnboardingChecklist` dérive pied + séparateur d'`envoisBloques` au lieu de `centreValide` → cohérent pour revendication catalogue). **Compte de test 08/07 neutralisé** (`trochenrc@gmail.com`, centre `7882bda3…` : SUSPENDED + INACTIF + `compteValide=false`). Workflow : retour direct sur main (les branches par fix = détour). Détail SESSION_STATE 08/07 (suite).

| # | Chantier | Détail | Priorité / Deadline |
|---|---|---|---|
| 10.1 | ~~**Gestion admin des abonnements (virement/BdC)**~~ ✅ **LIVRÉ + DÉPLOYÉ 08/07** | Enum `modePaiement` + cron exclut les VIREMENT (null-safe), facture `emettre()` échéance/mention dynamiques, `facturerCentre` prolonge depuis fin actuelle + marque VIREMENT, `getAbonnements` + badge admin, relance admin J-30 virement, self-service `demander-extension` +14j (endpoint + bouton). Choucas marqué VIREMENT en prod. | ✅ **DEADLINE 26/09 NEUTRALISÉE** (code + données en place) |
| 10.2 | Onboarding phase 2 — frontend | 4a : messages register, CTA trial obsolète, parsing `CENTRE_EN_VALIDATION`, badge centre PENDING. 4b : checklist activation + welcome modal (consomme `onboarding-status`, repli localStorage). 4c : parcours test guidé (devis à sa propre adresse). 4bis backend : preview facture non-émise (`apercu()`) + `POST /abonnements/demander-extension` (+14j sur demande → notif admin). | Avant démarchage IDDJ |
| 10.3 | Extraction PlanningPDF | Stash `extraction PlanningPDF en cours` à dépiler → `npm run build` frontend → commit dédié si vert. Code actuellement dupliqué (composant inline + fichier extrait commité). | Hygiène, avec 10.2 |
| 10.4 | .gitignore | Ajouter `build-error*.txt` (frontend). | Avec 10.2 |
| 10.5 | Email renouvellement annuel | Montant ignore les suppléments multi-centre (+39€/centre) — faux pour Pôle Montagne (YAKA+Florimont). | Avec 10.1 |
| 10.6 | ~~Compte test recette~~ ✅ **FAIT 08/07** | Compte ex-nihilo `trochenrc@gmail.com` (centre `7882bda3…`) créé pour le smoke-test puis neutralisé en prod : `SUSPENDED` + `abonnement_statut INACTIF` + `compteValide=false` (transaction, réversible, jamais ciblé par le cron). | ✅ |
| 10.7 | CLI Scalingo | 1.44.1 obsolète (timeouts constatés sur `restart`), mettre à jour vers 1.47.0. | Hygiène |
| 10.8 | Vérif cron J1 | Après 8h le 08/07 : `scalingo logs | findstr cronQuotidien` — 3 lignes attendues. Si rien : le restart post-`env-set ENABLE_CRON` n'a pas pris, relancer `restart`. | 08/07 matin |
| 10.9 | **HOTFIX faille claim** | Gate fondé sur le claimStatut du propriétaire (pas le statut centre) — branche fix/gate-claim-validation. Faille prouvée (Brevo 21:31 : devis émis vers tiers par claimant non validé). Re-test ZZTEST à confirmer. | 07/07 nuit — déployé |
| 10.10 | Boîte admin contact@liavo.fr | Notifs délivrées en temps réel par Brevo mais relève avec heures de retard (probable Gmail POP3 sur OVH). Fix : redirection MX ou IMAP. Radar de secours : /dashboard/admin/claims chaque matin. | Avant démarchage |
| 10.11 | ~~Prompt 6 — parcours test + découverte~~ ✅ **LIVRÉ 08/07 (partiel)** | Modale de choix séjour test/réel (client=soi), encart proactif pré-envoi (`envoisBloques`), message revendication via `extractApiError`, « Aller plus loin » (3 liens). **FeatureHint ×4 DIFFÉRÉ** (code mort sans placement + ancrages diffus 119 KB — à poser en amortissant). Recette prod 3/3. | ✅ sauf FeatureHint |
| 10.12 | Nettoyage ZZTEST | Procédure détaillée en session state (UI puis SQL). APRÈS re-test hotfix. | 08/07 |

| Sujet | Décision | Date |
|---|---|---|
| Priorité 1 roadmap invalidée | "Premier paiement Choucas ~17/07 = validation Mollie bout-en-bout" : Choucas paie par BdC mairie (Pilotage annuel 690€), ne passera JAMAIS par Mollie. Extension manuelle posée (17/10). Premier vrai test Mollie = Alticlub (fin trial 10/09) ou Pôle Montagne (01/12). | 07/07/2026 |
| Frontière de sécurité onboarding | L'accès au centre n'est plus la frontière (PENDING opérable) : ce sont les gates d'envoi + le filtre ACTIVE du catalogue public. `compteValide` = kill switch par requête, `SUSPENDED` = kill switch centre. | 07/07/2026 |
| Checklist onboarding | 5 étapes dérivées des données (jamais de flags manuels) : profil, catalogue, conformité (justificatif+IBAN), 1er séjour, 1er devis envoyé. Pas de suppression : repli visuel localStorage. Activation = premier devis envoyé. | 07/07/2026 |
| Cron + deadline 10.1 | Cron laissé ALLUMÉ (option 1). ~~10.1 à livrer avant le 26/09~~ → **10.1 LIVRÉ + DÉPLOYÉ le 08/07, deadline 26/09 NEUTRALISÉE** : le cron exclut désormais les centres `modePaiement=VIREMENT` (dont Choucas, marqué en prod) de toutes les alertes d'essai. Choucas recevra à la place une relance admin J-30 (~17/09) pour ré-émettre la facture BdC. | 08/07/2026 |
| « Démarchage IDDJ » périmé | Le label « avant démarchage IDDJ » (§10.2/10.10) ne reflète plus la réalité : IDDJ est **attentiste/prospectif** (Robin Baladi, CA à consulter), pas un partenaire actif. Réseau actif = **LMDJ** (Théo admin depuis AG 01/06). Cadrage réel des chantiers restants : « rendre le produit solide avant d'ouvrir à de vrais partenaires », pas « pour IDDJ ». | 08/07/2026 |
| permission.helper aligné PENDING | `getUserCentrePermissions` ne gate plus sur `ACTIVE` mais sur `SUSPENDED` (proprio/collab d'un PENDING obtient ses permissions), aligné sur `getCentreForUser`. Bug trouvé au smoke-test 08/07 : sidebar cassée + 403 en masse pour tout nouvel hébergeur ex-nihilo. Envois externes toujours gatés séparément. | 08/07/2026 |

---

**Ce document est la source unique de priorisation pour l'été 2026. Les autres docs restent comme archives de décision.**